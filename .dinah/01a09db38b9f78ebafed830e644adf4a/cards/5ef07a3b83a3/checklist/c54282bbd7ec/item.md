---
kind: decision
state: resolved
column: 0d86ad99cdbc
ts: 2026-09-14T02:17:45Z
ordinal: 22
note: "Dinah declares four item states and obsolete is not among them: `ItemStates = []string{ItemPending, ItemResolved, ItemVerified, ItemFailed}` (internal/bench/item.go:62), and `dinah set <item> state obsolete` answers `dinah.unknown-value Dinah does not accept obsolete in state. The values state takes are: pending, resolved, verified, failed.`\n\nEvery other landing was tried on a scratch copy and each is worse. Pending is what `dinah file` produces and there is no way to file an item already closed, so pending is the default outcome of doing nothing; a pending decision or open question refuses EVERY claim on its card even when it names no column at all, reproduced as `unresolved-item this card carries the item 74c34624f87b, which is not resolved`, which freezes the card with nothing on it explaining why. A pending acceptance criterion naming no column is genuinely inert, also reproduced, but reads as outstanding work nobody has verified, which is a false statement about the card. `failed` says the check ran and did not pass. `verified` is a plain falsehood.\n\n**The card that removes the need for this is dinah-472**, \"A checklist item's four states cannot say 'let this through anyway' and cannot say 'this no longer applies'\". Read on 2026-09-14: it carries both missing states as one question about what the state set lacks, it argues why one word for both would repeat the conflation that made `failed` ambiguous, it records that dinah-449 carries four such items and that the prose workaround does not become permanent by being convenient, and it marks the second state as the one on the cutover's path. It sits in Intake at priority next, severity major, and a `relates_to` link from it to this card was verified present rather than assumed (link id 1852). An earlier version of this note cited dinah-472 for something it did not then cover, and that was a promise with nothing behind it.\n\n**What makes the return mechanical rather than remembered.** The heading is a fixed literal, `## Retired checklist items (no Dinah state for these yet)`, and the census that finds it is ANCHORED to a whole line: `grep -rlE '^## Retired checklist items \\(no Dinah state for these yet\\)$' <workbench>/cards/*/card.md`. Corrected on 2026-09-14, because the plain text search this note carried before also matches a card that merely mentions the phrase, and this card's own spec discusses it at length. Demonstrated on two throwaway files, one holding the heading and one only describing it: plain returned both, anchored returned one. AC-16 requires the heading and the anchored census and fails an unanchored one. Recorded forward for whoever specs dinah-472, since this card may not write another card's contract: that card should carry a criterion requiring the census to return zero once the state exists."
---
An Andoneer item in the obsolete state does not cross as a Dinah checklist item. It crosses into the carried card's body under a fixed, greppable heading, and a single command over the workbench enumerates every card carrying one at any later time.