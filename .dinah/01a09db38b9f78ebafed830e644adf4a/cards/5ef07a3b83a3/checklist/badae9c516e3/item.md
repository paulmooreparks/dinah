---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:43Z
ordinal: 3
note: Verified 2026-09-14 by one dinah export against the migrated workbench, found by discovery rather than by a named directory, and compared element by element against the ratified table. The columns array holds exactly 14 elements. gate_items is absent for acceptance, build-queue, design-queue, done, intake and triage (six); the string "out" for agent-code-review, agent-design-review, implement, operator-code-review, operator-design-review, spec and test (seven); and the boolean true for merge (one). No element carries a value outside that mapping. Six, seven and one, which is fourteen. dinah export is the read used because ColumnView publishes no hold member, so dinah columns cannot answer this at all.
---
Every column's hold matches the ratified table, checked over the whole flow by one command rather than column by column. `dinah export` is run against the real workbench from `c:\Users\paul\source\repos\dinah`, letting discovery find it rather than naming a workbench directory, and its `columns` array is compared element by element against the spec's "Which columns hold, and in which direction" table. The comparison asserts the array holds exactly 14 elements, and that the `gate_items` member is: absent for `intake`, `triage`, `design-queue`, `build-queue`, `acceptance` and `done`; the string `"out"` for `spec`, `agent-design-review`, `operator-design-review`, `implement`, `agent-code-review`, `operator-code-review` and `test`; and the boolean `true` for `merge`. Six absent, seven out, one on. A run reporting a different element count, an unexpected member, a missing one, or any value outside that mapping fails, and the run names every element it found disagreeing rather than reporting a bare verdict. `dinah export` is the read used because `ColumnView` publishes no hold member (internal/verb/read.go:17 to :71), so `dinah columns` cannot answer this question at all; `exportColumn` (internal/bench/interchange.go:100 to :107) writes `true`, `"out"` or `"both"` and omits the member for off. This fails today: no column declares a hold, so eight of the fourteen expectations go unmet.