---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:48Z
ordinal: 12
note: "Archiving an attachment works and is a frozen compatibility path (cmd/dinah/compat_test.go:62 carries cards/<id>/archive/attachments/<id>/), and nothing undoes it.\n\nThe evidence is a guard in the test suite rather than a count of verbs. cmd/dinah/compat_test.go:128-130 declares unwrittenEvents, whose single entry exempts contract.EventRestored with the written reason \"archive has no inverse verb in the command surface, so nothing restores an entity\". The coverage alarm at compat_test.go:224-235 consults that table: an event internal/contract declares and the sample fixture carries no line of fails the build unless the table names it and gives the reason. The repository therefore carries this fact in a maintained place, which a figure typed into a spec is not.\n\nThe guard does not go red on the day a restore verb lands, and its own comment says so at compat_test.go:265-272 (\"caught in neither direction\", naming EventRestored). So the present state was checked directly as well: `grep -niI restore cmd/dinah/commands.go internal/mcp/tools.go` at 178e264 answers nothing on either file, and those are the sole declaration sites for the CLI's verbs and the MCP surface's tools.\n\nRound 1 of this spec supported the same conclusion with two hand-typed counts, and both undercounted. The MCP grep's character class excluded underscores, so it enumerated only the tool names carrying none and read every underscored name as absent rather than as unrecognised; the CLI grep missed `help`, which is declared with no group. The conclusion survives, and the counts are gone from the card rather than corrected, because a number typed into a spec goes stale and a guard does not.\n\nThe decision: an Archive item would give a reader a second irreversible act while implying it was the recoverable one, and recovery would mean moving a directory by hand in a layout the reader has never seen. bench.OpRestore, bench.RestoreTarget and contract.EventRestored sit in the storage layer with no verb above them. If a restore verb is ever added, adding Archive here is a small follow-on card against a real undo."
---
The attachment row offers Delete alone, and does not offer Archive.