---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:51Z
ordinal: 50
note: "Round 2's version of this decision made `applyDropVerdicts` take `draggedRows` as a parameter, which is a caller-supplied count in the one path that most needs the guarantee D-13 establishes, and the reviewer showed it was worse than a broken invariant: `dragPayloadsFor` had already discarded the unresolved row, so the fourth entry AC-15 requires had no `ref` anywhere in the function's arguments and could only be a fiction. `BulkEntry.ref` is the field the whole accounting rests on, so a synthetic one is the accounting lying in the field that was supposed to make lying impossible. The repair keeps the conclusion and changes the mechanism. `dragRowsFor` answers a `DragRow` per dragged element, a card row carrying its payload and any other row carrying its tree label, `applyDropVerdicts` runs `runBulk` over that list, `selected` is `rows.length` again, and `draggedRows` is gone.\n\nNarrowed at round 5, comment 7558 finding 1. Round 4's note claimed the discard was closed \"at the root\", and that was true of `dragRowsFor` and false one layer further out: §8 still had `offerDrag` wrap only the card rows' payloads, so the non-card rows were discarded at the mime entry instead, and `applyDropVerdicts` received three rows where AC-15 demanded four. The root is the boundary, not the function. D-22 takes that decision and §8 declares what crosses; this decision now stands on the narrower claim it can actually support, which is that no caller supplies a count to the drop path.\n\nWhat survives from round 2 unchanged is the reason the collecting host is wrapped inside the drop path rather than outside it: `crossWorkbench` calls `host.showError` per payload today, and a five-card cross-workbench drop would otherwise show five toasts with a summary on top of them. That wrapping is now `runBulk`'s, not the drop path's, so the drag gets it by using the same entry point every other command uses."
---
A drag's `selected` is the number of rows dragged, and `dragRowsFor` answers one row per dragged element so that no caller supplies that number.