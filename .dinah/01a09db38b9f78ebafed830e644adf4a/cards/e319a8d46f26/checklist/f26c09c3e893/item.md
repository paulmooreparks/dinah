---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:53Z
ordinal: 62
note: "§3a's sweep found four shipped sentences that a run raises once over rows they do not describe: `dialog.move.placeholder` (\"Move {ref} to\"), `dialog.block.reasonPrompt` (\"Why is this card blocked?\"), `dialog.card.copiedRef` (\"Copied {ref}\") and `dialog.workbench.copiedPath` (\"Copied {path}\").\n\nThe cheap answer was to reword each into something count-neutral, \"Move the selected cards to\", \"Why are these cards blocked?\", \"Copied\". It costs no new keys, and it was rejected for two reasons. It makes the single-row gesture worse, which is the gesture nearly every reader makes nearly every time, and a reader who right-clicks one card and is asked \"Why are these cards blocked?\" has been told something untrue in the other direction. And it changes four shipped English sentences, so all eight catalogues need new translations anyway, which is the cost the alternative was supposed to avoid.\n\nWhat ships is four new keys, `dialog.move.placeholder.many`, `dialog.block.reasonPrompt.many`, `dialog.card.copiedRef.many` and `dialog.workbench.copiedPath.many`, each reached only when more than one row resolved, with the existing singular unchanged and still reached at one row. The branch is `resolved.length` inside `ask` and the finished count inside `successMessage`, which is the same count every other branch on this card reads, so no command needs a rule of its own.\n\nAll four keep an unpluralised `{count}` rather than the catalogue's `(s)` form, because none is reachable at one, and their context notes say so, so that a later reader does not \"correct\" them. `dialog.attachment.delete.confirm.many` and `dialog.bulk.allSucceededNotes` take the `(s)` form instead, because nothing stops a later card reaching either at one.\n\nAC-13 and AC-14 pin which key each count reaches for Move and Block; AC-35 does the same for the two copy commands."
---
Four sentences the round-6 sweep reached get a plural key beside the singular one, rather than being reworded into one sentence that suits both counts. `dialog.attachment.delete.confirm.many` is the fifth such pair and was minted at round 4, so section 3a's table lists five repaired sentences and section 7 mints five plurals standing beside a shipped singular.