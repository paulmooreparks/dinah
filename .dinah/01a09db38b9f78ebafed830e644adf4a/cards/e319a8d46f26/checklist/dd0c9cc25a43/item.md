---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 30
note: "New at round 5. It exists because §3a's counting rule was applied across §2's table rather than only to the two commands round 4 had touched, and Delete Attachment was the third site: the table commits it to `oneInput` with one confirmation naming a count, and the catalogue's `dialog.attachment.delete.confirm` names one filename and one reference and can fill nothing from a run over several. §7 mints `dialog.attachment.delete.confirm.many` and §7 names `askDeleteAttachmentConfirmation` as the entry's `ask`, so the name exists rather than being gestured at, and AC-16's runtime total carries the eighth key.\n\nThis is AC-7's shape applied to a second command, and the reason it is a separate criterion rather than a clause of AC-19 is that AC-19 drives the `fanOut` family and Delete Attachment is `oneInput`.\n\nWhat it compares at the moment it runs: the strings a fake host recorded against the strings a localizer built over the English catalogue produces for those keys and values, plus the spawn log and the returned report. The 2-rather-than-3 assertion is the whole point: it fails against an implementation that fills the count from `rows.length`, which is the defect finding 2 caught in Move. The singular clause is what stops an implementation routing one attachment through the plural key, which would be a grammatical regression against what ships today.\n\nPlant, NOT PERFORMED: fill the count from the targeted row list rather than from the resolved list `runBulk` hands `ask`. The first clause's message assertion should redden on the filled value while the second clause, whose resolved and targeted counts differ, reddens on the key instead. A reader must confirm the plant compiles, which it should, since both lists are `readonly` arrays and only the expression being measured changes."
---
The `ROW_COMMAND_TABLE` entry whose id is `dinah.tree.deleteAttachment`, invoked over two attachment rows and one column row, calls `confirmDestructive` exactly once, asking with `dialog.attachment.delete.confirm.many` filled with 2 rather than 3 and with `dialog.attachment.delete.action` as the affirmative label, and spawns two deletes. Invoked over one attachment row and two column rows it calls `confirmDestructive` exactly once with the existing `dialog.attachment.delete.confirm`, filled with that attachment's filename and reference, and spawns once. A declined confirmation in either case spawns nothing and returns a report whose `cancelled` is true and whose `selected` is 3.