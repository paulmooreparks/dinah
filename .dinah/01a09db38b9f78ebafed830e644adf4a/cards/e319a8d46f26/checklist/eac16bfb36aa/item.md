---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:47Z
ordinal: 15
note: "Rewritten at round 5 against comment 7558 finding 1, which is the blocker. Round 4's version drove `applyDropVerdicts` with a hand-built four-row list, and under round 4's §8 the mime entry carried only the card rows' payloads, so the criterion went green over a list the shipping wiring never builds. The subject is now the round trip: `offerDrag` into a recording sink, the recorded value through `dragRowsFrom`, and that answer into `applyDropVerdicts`. Nothing in the chain is typed by the test.\n\nWhat it compares at the moment it runs: the row list `dragRowsFor` returns, the number of `set` calls the sink recorded, the list `dragRowsFrom` returns from the recorded value, the spawn log, the returned report's `selected` and its per-entry kinds and `ref`s, and the fake host's total call count. The `ref` assertion on the skipped entry is the half that catches a synthetic fourth entry: an implementation that pads the report to reconcile fails it. The zero-host-call assertion is what proves D-19's silence rather than a mere absence of spawns.\n\nWhat this does not prove is that `extension.ts` wires the two halves this way; AC-29 asserts that separately over the AST, the same division AC-3 makes on the registration boundary.\n\nTwo plants, PERFORMED rather than reasoned about, against a reduction of the §8 shapes in `C:/dinah-scratch/dinah-490-spec5/proto/drag.mjs` driven by `proto/ac15.mjs`. Both leave the types valid, because each filters a `readonly DragRow[]` into a `readonly DragRow[]`, so neither can fail `tsc --project tsconfig.test.json` before the unit run starts, which is how two of AC-3's round-3 plants died.\n\n1. Make `dragRowsFor` drop the non-card rows, `return rows.filter((r) => r.kind === \"card\")`. Performed: the four-row assertion and the skipped-entry `ref` assertion redden, the round-trip assertion reddens with them, and the no-card-row and `dragRowsFrom`-refusal clauses stay green.\n2. Make `offerDrag` wrap only the card rows, `wrap(rows.filter((r) => r.kind === \"card\"))`, which is exactly the shape round 4's §8 specified. Performed: the round-trip clause reddens ALONE, at the four-row assertion on `dragRowsFrom`'s answer, while `dragRowsFor`'s own clause stays green. That is the plant that matters, because it is the finding reproduced, and its isolated red is the evidence that this criterion now sees the boundary rather than driving around it.\n\nBoth runs printed an assertion count, so a plant that silently ran nothing would read as a smaller sweep rather than as a pass."
---
The drag round trip is driven end to end with no hand-built row list. `dragRowsFor` over three card rows and one column row answers exactly four `DragRow` values in the dragged order, three of kind `card` and one of kind `other` whose `ref` is that row's tree label. `offerDrag` over those same four elements, into a recording sink whose `wrap` is the identity, sets exactly one entry; `dragRowsFrom` over the value that sink recorded answers four rows, one of them `other`; and `applyDropVerdicts` over THAT answer, onto a column of the same workbench, spawns three moves carrying that column and returns a report with `selected` 4 holding three `done` entries and one `skipped` whose `ref` is the column row's label. A drag holding no card row sets no entry at all, and `dragRowsFrom` answers `undefined` for a bare `DragPayload`, for an array of bare payloads, and for an array holding only `other` rows. A drop over the same four rows onto the cards' own column, where every card's verdict is `ignore`, spawns nothing and calls no member of the host at all.