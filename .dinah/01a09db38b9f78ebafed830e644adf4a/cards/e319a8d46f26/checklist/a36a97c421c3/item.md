---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:47Z
ordinal: 13
note: "PERFORMED in round 5 for the count clauses, against `docs/specs/dinah-490-plant-reductions/move.mjs` driven by `ac13.mjs`. PLANT C keys branch 2 on the targeted count, which is the round-4 wording, and reddens clause 4a; PLANT D has branch 4 answer undefined, which reddens clause 4b alone. Both were run and both reddened only where claimed. The scripts now exit non-zero on a red run, so a later round can read the status rather than the output.\n\nThe placeholder clauses are new at round 6 and are NOT PERFORMED, because `dialog.move.placeholder.many` does not exist yet. Their plant, for Test: fill the placeholder from `dialog.move.placeholder` in both branches and watch the three-card clause redden while the one-card clause stays green. The isolated red is what proves the criterion reads which key was used rather than merely that a placeholder was passed.\n\nRound 5's text said only that `pick` was called once, which a placeholder naming one card of five satisfies. That is the defect comment 7560 finding 2 reported, and the two placeholder clauses are the repair.\n\nCompares the fake host's `pick` and `showError` call log, the spawn log, the returned report and `summaryFor`'s answer against expected values. `sharedLegalMoves` also gets its own direct test over an empty input, a single input, and inputs whose intersection is empty, because a function answering the empty array for everything would satisfy the middle clause on its own."
---
Move over three cards whose legal-move sets share exactly one column calls `pick` exactly once, its items are the shared destinations keyed by `move.column`, its placeholder is `dialog.move.placeholder.many` filled with 3, and the three move spawns all carry the chosen column. Over one card with legal moves it calls `pick` once with `dialog.move.placeholder` filled with that card's reference, which is today's behaviour unchanged. Over three cards sharing no column it calls `pick` zero times and `showError` once with `dialog.move.noSharedDestination` filled with 3. Over one card with no legal moves it calls `showError` once with `dialog.move.noLegalMoves` filled with that card's reference. Over a selection of two cards that share no column and two column rows, it calls `showError` once with `dialog.move.noSharedDestination` filled with 2, not 4, and returns a report whose `selected` is 4. Over a selection of three column rows and no card, it spawns zero times, calls `pick` and `showError` zero times, and returns a report with `selected` 3 holding three `skipped` entries over which `summaryFor` answers a `warning` naming succeeded 0 and skipped 3, which is the same answer AC-11's second half pins for Claim over that selection.