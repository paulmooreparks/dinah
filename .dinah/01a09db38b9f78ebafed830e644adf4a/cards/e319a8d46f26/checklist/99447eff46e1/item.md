---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 33
note: "PERFORMED at round 6, against `docs/specs/dinah-490-plant-reductions/perimeter.mjs` driven by `acPerimeter.mjs`, clauses 1 and 2 of that script. Baseline 9 assertions, 0 red, exit 0.\n\nPLANT E is the one that matters and it is round 5's shipped mechanism, which intercepts `showError` alone. It reddens both clauses, and the failure text is the defect comment 7560 reported, printed verbatim by the run: the pull clause reports five shown messages, four `dialog.pull.empty` toasts and the summary, and the check clause reports four, three `dialog.workbench.checkFindings.toast` warnings and the summary. PLANT F, which intercepts `showError` and `showInfo`, reddens the check clause alone and leaves the pull clause green, which is the isolated red proving each clause reads its own channel rather than both reading one.\n\nThe level clause is not decoration. A summary that escalated four empty pulls to a warning would satisfy a count-only assertion and would be wrong, and one that reported three workbenches with defects as informational would be wrong in the other direction, so each clause pins the level its own notes imply.\n\nWhat a false failure would look like, and why it cannot happen: the clauses count calls on a fake host rather than matching text, so a re-worded catalogue entry cannot redden them, and the message assertions name catalogue keys rather than English.\n\nWhat this does not prove: that `checkWorkbench` and `pullColumn` are actually wired into `ROW_COMMAND_TABLE` entries that reach `runBulk`, which is AC-3 and AC-19, and that the real hosts carry the members the reduction gave them, which is AC-31."
---
Two multi-row runs whose per-row path reports informationally produce exactly one message each. Pull over five column rows of which four answer an empty pull calls the real host's `REPORT_CHANNELS` members exactly once in total; that call is `showInfo`; its message is `dialog.bulk.allSucceededNotes` filled with count 5 and notes 4; the report carries four notes, all of level `info`; and the channel receives four lines, one per empty column, each naming its own column. Check Workbench over three workbench rows that each reach `offerOutput` with a findings toast calls the real host's `REPORT_CHANNELS` members exactly once in total; that call is `showWarning` carrying `dialog.bulk.allSucceededNotes` and the `dialog.openOutput.label` action; `revealOutput` is called zero times during the loop; and the report carries three notes, all of level `warning`.