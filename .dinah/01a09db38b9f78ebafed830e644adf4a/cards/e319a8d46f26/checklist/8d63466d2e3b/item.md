---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:47Z
ordinal: 12
note: "Compares the folder arguments recorded by the fake host, as a set and by call count. The all-refused half is deliberate: runVerb checkpoints on a refusal today because a refusal often means the board moved under the reader, and that reasoning has to survive the change. An implementation that checkpoints per row fails the count with five; one that checkpoints only on success fails the all-refused half with zero."
---
A five-card run spanning two workspace folders calls `checkpoint` exactly twice, once per distinct folder, and does so on a run where every one of the five spawns answered a refusal.