---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 34
note: "NOT PERFORMED, because the plants need the post-card command modules to parse. The plant, for Test: add one `host.showInfo(...)` call to any command module and confirm the count reddens and the failure message names the new site; then point the walk at an empty directory and confirm the exactly-18 assertion reddens rather than passing over nothing, which is the vacuous-sweep failure this board has shipped twice.\n\nThe figure is derived rather than remembered, and here is the derivation so a reviewer can check it rather than trust it. At `2aba8fc`, `grep -rn '\\.showError(\\|\\.showInfo(\\|\\.showWarning(' src/` returns 18 call sites: cardCommands 5, columnCommands 1, creationCommands 2, dragAndDrop 1, pullCommands 2, runVerbCommand 4, workbenchCommands 3. This card removes two, `copyCardRef`'s and `copyWorkbenchPath`'s `showInfo`, which §3a replaces with `finish` and `successMessage`; and adds two, Move's `showError` for `dialog.move.noSharedDestination` and New Card's `showError` for `dialog.bulk.oneRowOnly`. 18 minus 2 plus 2 is 18, across the same 7 files, and the floor is stated at 6 so that a file legitimately losing its last call does not redden the sweep while a walk that read almost nothing still does.\n\nWhat this criterion is for, and what it is not. It does not prove any call is correct, and a wrong count is not a defect in itself. It is a tripwire: a per-row report site added after this card cannot land silently, because its author has to raise the figure and, in doing so, decide whether the new site belongs inside a run. That is the weaker half of the perimeter and it is weaker on purpose, because the stronger half, AC-31, cannot see a module that speaks through a host rather than through `vscode.window`.\n\nWhy 18 rather than a computed floor: a floor would pass a run that lost sites, and losing a site is how a command stops reporting at all. The exact figure is asserted in this one place on the card, and the spec names this criterion instead of quoting it."
---
A unit test walks every `.ts` file under `editors/vscode/src/` except `bulk.ts` and `extension.ts` with the TypeScript compiler API, collects every call expression whose callee is a property access whose name is a member of `REPORT_CHANNELS`, read from `src/reporter.ts` at test time, and asserts it collected exactly 18 calls across at least 6 files. It reports the collected call sites by file and symbol in its failure message, so a run that reddens names what changed rather than only that a number moved.