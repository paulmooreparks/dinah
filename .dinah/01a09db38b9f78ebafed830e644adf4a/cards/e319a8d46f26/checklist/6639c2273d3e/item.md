---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:47Z
ordinal: 11
note: "Compares the spawn log, the returned entry list and the fake host's call log against expected values. The second half is the important one: a run that attempted nothing and reported nothing is the \"reports success because it never looked\" failure, and this is the assertion that makes it impossible. An implementation that filters the selection and then returns early on an empty list passes the first half and fails the second. The entry-list assertions are what tie the skipped rows to enumerated members rather than to a counter: a skipped row is a row of the input that `runOverRows` recorded, so the report adds up by construction and cannot report a skip it cannot name. The third half is round 2's blocker 3: with the host switch keyed on rows surviving the filter rather than on rows targeted, this case takes the real host, the refusing card shows its own toast, and the summary then speaks over it, which is exactly the toast storm D-7 exists to prevent. Because `runBulk` switches on `rows.length`, which is the targeted count and is the same number the summary's single-row case reads, the two cannot both speak. Red run to produce at Test: change the switch to read the resolved list's length and watch the `showError` zero assertion redden while the `showWarning` assertion stays green, which is the two halves reporting independently."
---
A selection of two card rows and one column row handed to Claim spawns exactly twice, carries the two cards' references, and returns a report with `selected` 3 and exactly three entries, two `done` and one `skipped`; the summary shown is the `dialog.bulk.partial` warning naming skipped 1. A selection of three column rows handed to Claim spawns zero times and still shows that warning, with selected 3, succeeded 0 and skipped 3, and returns three `skipped` entries. A selection of one card row and two column rows handed to Claim, where the one card refuses, calls the real host's `showError` zero times and `showWarning` exactly once, so exactly one message reaches the reader.