---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:47Z
ordinal: 9
note: "Compares the call log of a fake host against expected call counts and expected localized strings, in a pure test. Three halves and none of them is redundant. The first catches an implementation that leaks per-row toasts or reports the wrong counts, and the zero-count assertion on `showError` is what proves the collecting host is actually in the path rather than merely constructed. The second catches one that warns on a run where nothing went wrong. The third is the one that closes round 1's blocker: filling `allSucceeded` from `selected` passes the second half and fails here, because a forced-`selected` report no longer adds up and `summaryFor`'s reconciliation arm must throw on it. That report is obtained by spreading a real one and overriding `selected`, for the reason AC-23's note gives: a hand-built literal would be refused by the mark arm instead and would satisfy this half for the wrong reason. Note that Archive is excluded from the `appendLines` assertions above, since §4 gives it a line for every attempted row including the ones that finished; this criterion is written over a `fanOut` command such as Claim, which under D-17 lines only the rows that did not finish, so a five-card run with two refusals and nothing skipped writes exactly two lines."
---
A five-card run with two refusals calls the real host's `showError` zero times, `showWarning` exactly once with `dialog.bulk.partial` filled with succeeded 3, selected 5, refused 2 and skipped 0, and `appendLines` with exactly two lines, one naming each refused reference. A five-card run with no refusals calls `showInfo` exactly once with `dialog.bulk.allSucceeded` filled with `{count}` 5, and `showWarning` and `appendLines` zero times; the test also asserts that the value filled into `{count}` came from the run's done count by running the same case with a report whose `selected` has been forced to 6, and asserting `summaryFor` throws rather than answering 6.