---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 32
note: "PERFORMED at round 6 and re-performed at round 7, against `docs/specs/dinah-490-plant-reductions/perimeter.mjs` driven by `acPerimeter.mjs`. Baseline runs 9 assertions with 0 red and exit 0.\n\nRound 7 changed the reduction so the three shapes differ in the prompt members they carry, because round 6's table gave all three the same member list and built each with the same `realHost({ pick, input, confirmDestructive })`, which made the shape name a label on a loop that ran one case three times. The prompt comparison on two of those three was `undefined === undefined`, a universal claim over an empty set, and it would have passed against a wrapper that intercepted everything on those two hosts. The reduction now carries the member lists from the source, confirmed by reading `cardCommands.ts`, `workbenchCommands.ts` and `columnCommands.ts` at `2c427e8`: only `CommandHost` declares `pick`, `input` and `confirmDestructive`. The clause now asserts what it compared, and the empty case reports zero.\n\nPLANT G hands `ask` the collecting host instead of the real one and reddens the refusal clause alone, 1 red of 9, exit 1: the reader is shown nothing at all, because the cancelled report makes the summary silent too. That is the failure this clause exists to catch, and it is invisible to any assertion stated over the summary.\n\nPLANT E intercepts `showError` alone, which is round 5's shipped shape, and reddens all three host-shape clauses, 5 red of 9. PLANT F intercepts `showError` and `showInfo` and reddens all three again on the `showWarning` half, 4 red of 9. PLANT H reddens 1 of 9. All four figures are unchanged by the round-7 edit, re-run at round 7, and every plant exits 1.\n\nThe identical-function clause on the prompt members is the refusing partner: a wrapper that intercepted everything would satisfy the capture clauses and fail this one, so a guard cannot pass by being too strict. That matters here, because a collected `confirmDestructive` would silently accept a destructive act nobody confirmed. Its reach is `CommandHost` alone, which is where `confirmDestructive` lives, and that is the honest limit rather than a gap to be papered over.\n\nWhat this does not prove: that the three host interfaces really carry the members the reduction gave them, which is AC-31's heritage clause, and that a command's act reaches the collecting host at all, which is AC-33."
---
`runBulk` over two card rows whose `ask` states a refusal calls the real host's `showError` exactly once with that refusal, and `summaryFor` over the returned report answers level `none`, so the reader gets the refusal and nothing else. Separately, `collectingHost` is driven over a host of each of the three shapes, `CommandHost`, `WorkbenchCommandHost` and `ColumnCommandHost`, each built with the members that shape actually declares in the source rather than with one member list repeated three times: for each shape every member named in `REPORT_CHANNELS` is called on the wrapped host, the real host's corresponding members are called zero times, the drained result carries the `showError` text under `errors` and the `showInfo` and `showWarning` texts under `notes` tagged `info` and `warning` in call order, and the wrapped `showWarning` answers `undefined`. The prompt-identity clause is then asserted over the prompt members that shape declares, and the number of members compared is asserted per shape, so a shape declaring none reports zero compared rather than reading as a pass on `undefined === undefined`. `CommandHost` is the only shape carrying `pick`, `input` and `confirmDestructive`, so it compares three and the other two compare none; for those two the prompt half of this criterion proves nothing, and AC-31's heritage and return-type clauses are what hold their shape instead.