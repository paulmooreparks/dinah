---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 35
note: "PERFORMED at round 6, against `docs/specs/dinah-490-plant-reductions/perimeter.mjs` driven by `acPerimeter.mjs`, the AC-35 clauses. Baseline 9 assertions, 0 red, exit 0.\n\nPLANT H is the shape round 5 shipped: the copy commands report per row rather than declaring `finish` and `successMessage`. It reddens the two-row clause alone, 1 red of 9, exit 1, and the failure shows both what the clipboard received, two separate writes rather than one joined one, and the three messages the reader was shown.\n\nWhy the single-row clause is here beside the plural one. The plural key is easy to reach by making the copy always plural, which would regress the one-row gesture to \"Copied 1 card references.\" and satisfy any assertion stated over the multi-row case alone. Pinning both counts is what forces the branch, and it is the same accepting-case-beside-the-refusing-case shape this board requires of a guard.\n\nWhy this needs a criterion at all: round 5's AC-19 `oneCall` arm pinned \"one host call carrying both rows' values newline-joined\", which under `dialog.card.copiedRef` (\"Copied {ref}\") fills a one-card sentence with a multi-line list. Comment 7560 finding 2 named it. The repair is that the copy family stops reporting per row entirely and the summary carries its sentence, which is §3a's `finish` and `successMessage`, recorded as D-23.\n\nWhat this does not prove: that the clipboard write reached the real system clipboard, which no unit layer can prove, and that `dialog.card.copiedRef.many` reads well in German or Hindi, which nobody on this project can adjudicate."
---
Copy Reference over two card rows calls `copyToClipboard` exactly once with the two references newline-joined in selection order, calls the real host's `REPORT_CHANNELS` members exactly once in total, and that one call carries `dialog.card.copiedRef.many` filled with count 2. Copy Reference over one card row calls `copyToClipboard` once with that reference alone and shows `dialog.card.copiedRef` filled with that reference, which is today's behaviour unchanged. Copy Path over two workbench rows behaves the same way over `dialog.workbench.copiedPath.many` and `dialog.workbench.copiedPath`. In every one of those four runs, the per-row act calls no member of `REPORT_CHANNELS` at all.