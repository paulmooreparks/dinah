---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 29
note: "New at round 5, against comment 7558 finding 1. AC-15 drives `offerDrag`, `dragRowsFrom` and `applyDropVerdicts` as a chain, and that chain is worth nothing unless `extension.ts` is the same chain. This is the drag-side twin of AC-3's iterated-identifier clause, written for the same reason: round 4 shipped a criterion that drove a function the wiring never fed, and a criterion tying the driven thing to the registered thing is what stops that recurring.\n\nWhat it compares at the moment it runs: node counts, parameter names and identifier text in `extension.ts`'s AST as parsed at test time, never strings in a file. The parameter-identity assertions are the half that matters, because a `handleDrag` that declares `source` and then passes a literal, or a `handleDrop` that calls `dragRowsFrom` on something other than its own data transfer, satisfies a call-count check and wires nothing. The last clause is a real sweep with a real floor of zero: those three names are how the singular drag reaches the editor today, and a diff that adds the new path while leaving the old one live would pass every other clause on this card.\n\nThree plants, NOT PERFORMED, because performing an AST assertion means building the post-card `extension.ts` and this round built only the pure `dragAndDrop` shapes. Each is written to leave the file compiling, which is the failure mode that killed two of AC-3's round-3 plants; a reader must confirm that before trusting any of them, by running `tsc --project tsconfig.test.json` on the planted file and checking it produces no diagnostic.\n\n1. Change `handleDrag`'s call to `offerDrag([], DRAG_MIME_TYPE, dataTransfer, wrap)`, keeping the parameter declared. The first-argument-identity assertion should redden while the call-count assertions stay green. Compiles, because `readonly TreeElement[]` accepts an empty array literal.\n2. Bind `dragRowsFrom`'s result and then pass a freshly built list to `applyDropVerdicts` instead. The same-name assertion should redden while the `dragRowsFrom` call-count assertion stays green. Compiles as long as the substituted list is a `DragRow[]`.\n3. Leave a call to `applyDropVerdict`, the singular function, beside the new one. The last clause should redden while every other clause stays green. This is the one a real diff is most likely to produce, since the old call is deleted by hand."
---
A unit test walks `editors/vscode/src/extension.ts` with the TypeScript compiler API and asserts the drag boundary is wired to the functions AC-15 drives. It finds the object literal passed as `dragAndDropController` and asserts it holds exactly one `handleDrag` property and exactly one `handleDrop` property. It asserts that `handleDrag`'s body holds exactly one call to `offerDrag` and that the identifier passed as its first argument is `handleDrag`'s own first parameter. It asserts that `handleDrop`'s body holds exactly one call to `dragRowsFrom`, that the expression passed to it reads the `DRAG_MIME_TYPE` entry off `handleDrop`'s own `dataTransfer` parameter, that the call's result is bound to a name, and that the same name is passed as the first argument of the one call to `applyDropVerdicts` in that body. It asserts that `extension.ts` holds no call to `dragPayloadFor`, `classifyDrop` or `applyDropVerdict`, the three singular-drag functions this card replaces or demotes to an internal call.