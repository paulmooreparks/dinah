---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 26
note: "This is the perimeter check: the summary and the host switch have exactly one caller each, so a future command cannot compose its own message or choose its own host without this going red. What each assertion compares at the moment it runs is a set of file paths, collected by walking `src/` with the compiler API and keeping the files whose AST holds an identifier of that name, against 1 for the first two and against a floor of 5 for the third.\n\nThe `runBulk` floor is tied to the thing being checked rather than to a file count, and round 4 corrects what it was credited with. Round 3's note said it catches a walk that read nothing \"while the two exactly-one assertions would both have passed\", and that is false: over a walk that read nothing, `summaryFor` appears in zero files, and zero is not one, so those assertions fail too. What the floor actually catches is the narrowed walk, which is the likelier mistake and the one nothing else here sees. A walk scoped to `src/bulk.ts` alone finds `summaryFor` in exactly one file, `collectingHost` in exactly one file, and `runBulk` in one file, so both exactly-one assertions pass on a sweep that never looked at a single command module. The floor is five rather than the true number so that a later card adding or merging a command file does not redden a criterion it has nothing to do with; §3a names the callers and there are more than five of them today.\n\nThe limit, stated so nobody credits it with more: this reads identifiers in the source rather than proving at runtime that no other path shows a message, and a command that called `vscode.window.showWarningMessage` directly would walk past it. What closes that gap is AC-25's mark plus the fact that `CommandHost` is the only handle a command module holds, since no command module imports `vscode` at all.\n\nTwo plants at Test, each with a real red and a real green. Add a `summaryFor` call in `cardCommands.ts` and watch the first exactly-one assertion redden with two files while the `runBulk` floor stays green. Then narrow the walk to `src/bulk.ts` alone and watch the `runBulk` floor redden with 1 against 5 while both exactly-one assertions stay green, which is the narrowed-walk case the floor exists for."
---
A unit test walks every `.ts` file under `editors/vscode/src/` with the TypeScript compiler API and asserts that the identifier `summaryFor` appears in exactly one file and that the identifier `collectingHost` appears in exactly one file, both of them `bulk.ts`, and that the identifier `runBulk` appears in `bulk.ts` and in at least five other files.