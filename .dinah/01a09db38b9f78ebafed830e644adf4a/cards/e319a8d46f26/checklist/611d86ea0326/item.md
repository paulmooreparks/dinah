---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 28
note: "The silent voice is a real exception to \"a run that attempted nothing still answers\", and an exception that any caller may declare is not an exception. D-19 records why the drop path gets it and why no menu command does; this is the assertion that holds the declaration to that one place, so a later command cannot borrow the silence to quieten a noisy summary, which is precisely how this kind of exception spreads.\n\nWhat each assertion compares at the moment it runs is a list of property-assignment nodes collected from `src/`'s ASTs, against the literal 1 for the `emptyRun` half and against a floor of 1 for the `skipReason` half. Comparing the initialiser's node kind rather than searching the file for the word `silent` is what keeps it from firing on a comment or a message key.\n\nRound 4 corrects what the `skipReason` floor was credited with. Round 3's note said it catches a walk that read nothing \"while the exactly-one assertion on `emptyRun` would have passed by vacuity\", and that is false: over a walk that read nothing the `emptyRun` count is zero, which is not one, so that assertion fails too. What the floor actually buys is a count for the walk's other half. The two halves collect different property names, and a collector that is right about one and mis-keyed about the other reads plenty and reports nothing for the half that is broken, which is the shape the workbench's rule about giving each half its own count was written for.\n\nTwo plants at Test, each with a real red and a real green. Set `emptyRun: \"silent\"` on Claim's deps and watch the exactly-one assertion redden with two sites while the `skipReason` floor stays green. Then change the `skipReason` collector to look for a property name no `BulkDeps` literal carries, and watch the floor redden with zero while the `emptyRun` assertion stays green at one."
---
A unit test walks every `.ts` file under `editors/vscode/src/` with the TypeScript compiler API, collects every property assignment whose name is `emptyRun` and whose initialiser is the string `"silent"`, asserts it collected exactly one, and asserts that one sits in `dragAndDrop.ts`. The same walk asserts it collected at least one property assignment named `skipReason`, so a walk that matched nothing fails rather than reporting one silent site and no sites at all.