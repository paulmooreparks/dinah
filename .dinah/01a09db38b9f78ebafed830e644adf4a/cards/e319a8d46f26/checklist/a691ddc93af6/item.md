---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:52Z
ordinal: 54
note: "Three conventions, none of them minted here. `dialog.workbench.checkFindings.toast` writes \"{count} defect(s)\" and `dialog.runVerb.excludedSeparator` writes \"{count} command(s)\", so the plural rides that shape; round 1's \"1 of 5 selected rows are done\" was wrong English and left a German or Hindi translator agreeing an article and a verb with a number nobody can see, which is the counterexample \"A word that agrees with an interpolated token the sentence never sees\". No sentence carries a code span, because confirmDestructive passes its message to showWarningMessage, documented as taking a string with no promise of Markdown, and dialog.attachment.delete.confirm is the same-family precedent that carries none. And no sentence quotes anything, so the German needs no quotation mark: the counterexample \"A quotation mark a translator reaches for by reflex\" records that U+201C, German's closing mark, is banned while U+201E is not, and names a sentence needing no mark as better than the mixed pair.\n\nRecorded so nobody credits a guard that is not here, twice over. Searching this tree for U+201C and for a scan naming it found neither, so that ban is enforced by the pre-commit scan the counterexample describes and by nothing in editors/vscode/test/. And the article rule is the same: round 2 found this decision and §7 both crediting TestNoEnglishSentenceTakesAnArticleBeforeAPlaceholder with holding these keys, when that test lives in internal/msg/msg_test.go and sweeps the Go catalogue's Keys() and BaseEntry. A repo-wide search for that test name at 4c33c2c returned one definition and no caller, nothing in internal/msg reads editors/vscode, and nothing under editors/vscode/test/ mentions articles at all. The English here follows the rule and a later key that broke it would not be caught, which is the honest statement and the same one this decision already made for the quotation-mark ban. §9 records that minting such a guard is a card of its own and that nobody has asked for one.\n\nCorrected at round 5, comment 7558 finding 2. `dialog.move.noSharedDestination` keeps {count} unpluralised because §5 reaches it only when more than one card RESOLVED to a card context, and its context note says so. Round 4 said \"was targeted\" here, which under §3a's vocabulary counts the rows the reader aimed at and would have let the branch be reached with one resolved card, making the unpluralised form wrong English as well as a false sentence. §3a now states the rule once: a count inside `ask` is a resolved count by construction, because `ask` is handed only the resolved list.\n\nThe eighth runtime key, `dialog.attachment.delete.confirm.many`, minted at round 5 by that same sweep, takes the `(s)` form rather than the unpluralised one, because nothing stops a later card reusing it over a single attachment."
---
The minted copy follows the catalogue's existing `(s)` convention for counts, carries no code span, needs no quotation mark in any language, and takes no article before a placeholder; the last of those is met by the author, because no guard on this surface enforces it.