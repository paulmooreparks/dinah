---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:48Z
ordinal: 16
note: "Compares each parsed catalogue's key count against a literal at test time. These two literals are the only copies of those figures anywhere on this card: the spec's §7 names this assertion rather than quoting either number.\n\nThe runtime figure rose by five at round 6, from 126 to 131, and the figure was re-derived rather than adjusted: today's counts were re-read at `2aba8fc`, whose `src/locales/` the card has not touched, with `python -c \"import json;print(len(json.load(open('src/locales/en.json',encoding='utf-8'))['entries']))\"` answering 118 and `python -c \"import json;print(len(json.load(open('package.nls.json',encoding='utf-8'))))\"` answering 41. Thirteen new runtime keys gives 131; one new manifest key gives 42. Note the shape: `en.json` is `{ tag, entries }`, so a count taken over the file's top level answers 2 and a count taken over `entries` answers 118, and the assertion reads `entries`.\n\nThe five keys added at round 6 are `dialog.bulk.allSucceededNotes`, `dialog.move.placeholder.many`, `dialog.block.reasonPrompt.many`, `dialog.card.copiedRef.many` and `dialog.workbench.copiedPath.many`. The first comes from §3b's notes case; the other four come from §3a's sweep over the sentences a run raises once, reported as comment 7560 finding 2.\n\nNOT PERFORMED, and the reason is that the plant is trivially real rather than that it is hard: adding or removing one key from `en.json` reddens this assertion and nothing else, which Test performs in one edit.\n\n---\n\nCORRECTED AT AGENT CODE REVIEW, 2026-09-13, by the reviewer, under the rule that a criterion whose text is wrong while the code is right is repaired rather than pushed back.\n\nThe original third clause read: \"and the whole existing extension unit suite runs green unedited.\" That clause is impossible against the design this same card ratified. D-25 makes all three host interfaces extend `ReporterHost`, which adds three required members to `CommandHost`, two to `ColumnCommandHost` and one to `WorkbenchCommandHost`, so every typed host literal in the existing tests stops compiling until it gains them; and §3a removes `copyCardRef`'s and `copyWorkbenchPath`'s `showInfo` outright, so the tests driving those two functions cannot survive either. The criterion asked for something the spec forbade, which is a defect in the criterion and not in the diff.\n\nThe replacement keeps everything the clause was for and drops only the impossible word. The l10n half is unchanged and was honoured: none of the five named files is touched. The new sentence bounds what an edit elsewhere may be (fixture widening, following a moved call site, reading a moved module), forbids weakening an assertion, and requires a moved assertion's subject to be re-established rather than dropped.\n\nThe reviewer verified that requirement rather than taking it. Eight existing test files carry edits (`cardCommands`, `columnCommands`, `creationCommands`, `dragAndDrop`, `manifest`, `pullCommands`, `runVerb`, `workbenchCommands`); `manifest.test.ts` is purely additive, and `columnCommands`, `pullCommands` and `runVerb` are fixture widening with no assertion touched. Every changed hunk in the other four was read. Three assertions changed meaning, all of them the openAttachment channel-line content checks, and that is recorded as a finding on the card rather than blessed here. Three changed shape with their meaning re-established elsewhere, and each replacement was read: the two copy toasts are now asserted end to end through the real `ROW_COMMAND_TABLE` entry in `rowCommands.test.ts`, over both the singular and plural keys, which is stronger than what they replaced. Everything else is call-site rewiring with the assertion preserved word for word."
---
A new unit test asserts that `editors/vscode/src/locales/en.json` carries exactly 131 entries under `entries` and that `editors/vscode/package.nls.json` carries exactly 42 keys, failing on either. No assertion in `l10n-keys.test.ts`, `l10n.test.ts`, `l10n-coverage.test.ts`, `l10n-placeholders.test.ts` or `l10n-staleness.test.ts` is edited by this card. An existing test file outside that set may be edited only to widen a host fixture to the members `ReporterHost` adds, to follow a call site to the function that replaced it, or to read the module a resolution moved into; no existing assertion is weakened, and an assertion whose subject this card moves is re-established over the new subject. The whole extension unit suite runs green.