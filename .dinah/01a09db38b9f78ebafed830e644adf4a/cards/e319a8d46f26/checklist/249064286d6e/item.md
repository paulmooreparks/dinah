---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:46Z
ordinal: 3
note: "Rewritten at round 4 (comment 7556, finding 2), and corrected in place at round 4 review in two respects: the criterion now names the identifier the loop iterates, and two of its three arming plants are replaced with plants that compile.\n\nRound 3's version was written to a file that held the id-to-function mapping three ways, and it could only read arity because two of its twelve calls passed a loop variable. §1 now puts the whole mapping in `src/commandTable.ts`, a module importing no vscode symbol, and `extension.ts` registers in exactly two shapes: one loop over `ROW_COMMAND_TABLE`, and one standalone call per `noRow` command. Both literals in the criterion's text are the only statements of either figure anywhere on this card; the spec's §1 names this criterion instead of quoting them.\n\nWhy the iterated identifier is named. Round 4's whole justification for D-21 is that the test drives the list the editor registers from rather than a copy of it, and the round-3 wording did not carry that: it asserted the remaining call was a loop calling `invoke` on \"the value it is iterating\" without asserting what that value is. A loop over a second array of the same shape satisfies every AST assertion, leaves the set assertion comparing `ROW_COMMAND_TABLE` against `SELECTION_POLICIES` with nothing registering from it, and hands AC-19 a table nothing wires. Naming `ROW_COMMAND_TABLE` as the iterated expression closes the last link, so the chain runs manifest.test.ts (contributes.commands = TREE_COMMANDS), AC-2 (SELECTION_POLICIES keys = TREE_COMMANDS), this criterion (ROW_COMMAND_TABLE ids = the non-noRow half, and extension.ts registers by iterating exactly that identifier), AC-19 (drives those entries), with no link comparing a value against the thing that produced it.\n\nWhat each assertion compares at the moment it runs. The call-count, arity and iterated-expression assertions compare node counts, parameter-list lengths and identifier text in extension.ts's AST as parsed at test time, not strings in a file. The noRow classification compares the first argument's identifier, resolved through the constants imported from src/identity.ts, against the policy read from src/selection.ts. The two-parameter assertion compares the identifiers passed to the `targetsFor` call against the handler's own parameter names, so a handler that declares a second parameter and then ignores it fails: that is the half worth insisting on, because a declared-and-ignored selection satisfies an arity check and still acts on one row. The set assertion compares two arrays of strings read from two modules at test time, so a command given a table entry but no policy fails as unexpected and one given a policy but no entry fails as missing.\n\nThe derivation, read at 4c33c2c and re-confirmed at round 4 review. `extension.ts` holds twelve `register` calls today: two loops wiring nine and three commands, six standalone calls for row commands whose handler is an inline arrow, and four for the commands that read no row. Eighteen row commands plus Archive is nineteen, and nineteen plus the four noRow commands is the roster AC-2 pins. After this card the nineteen are registered by the one loop and the four keep their own calls, which is five. Confirmed independently: `TREE_COMMANDS` holds 22 entries, `ROW_COMMANDS` holds 18, and the manifest contributes 22 commands.\n\nThree plants, each with its own red, to be performed at Test rather than reasoned about. Two were corrected at round 4 review because they do not compile, and `npm run test:unit` runs `tsc --project tsconfig.test.json` before it runs anything, so a plant that fails to typecheck produces a type error instead of the named assertion's red. Both replacements were compiled under this extension's own flags with the repository's own typescript and produce no diagnostic.\n\n1. Make the loop's handler pass the selection to `targetsFor` as an expression rather than as the parameter itself, `targetsFor(element, selection?.slice(0, 0), elementKey)`, keeping both parameters declared. The \"both are passed to targetsFor\" assertion reddens because the second argument is no longer the identifier; the arity assertion and the noRow group count both stay green. The round-3 recipe here was \"delete the second parameter from the loop's handler\", which leaves `selection` unresolved and fails with TS2304 before the test runs; deleting the argument as well fails on `targetsFor`'s arity instead, so no spelling of that plant compiles.\n2. Change one `noRow` command's registration to a handler that declares a parameter and uses it, `async (element) => { void element; return; }`. That call is still classified into the noRow group by its first argument, so the group count stays 4 and the no-parameter assertion on that group reddens. The `void element` is load-bearing: `tsconfig.json` sets `noUnusedParameters`, so a declared-and-unused parameter fails with TS6133 before the test runs.\n3. Delete one entry from `ROW_COMMAND_TABLE`: the set assertion reddens in the missing direction while the AST assertions stay green, since the loop is one call whatever it iterates. This one compiles unchanged and is kept as round 4 wrote it.\n\nWhat this does NOT prove: that any handler's declared policy matches what it does, which AC-19 covers for the `fanOut` family, and that VS Code invokes a registered handler with the arguments `canSelectMany` declares, which §2 states plainly is beyond the unit layer. `assertCommandsFullyRegistered(TREE_COMMANDS, registeredIds)` at the end of `activate()` already covers the roster half at run time, and `registrationGuard.ts`'s own header states what that runtime check cannot prove."
---
A unit test walks `editors/vscode/src/extension.ts` with the TypeScript compiler API and collects every call to the local `register` helper. It asserts it collected exactly 5, so a walk that matched nothing fails rather than reporting a clean sweep. It asserts that exactly 4 of those calls pass as their first argument an identifier naming a command id whose `SELECTION_POLICIES` entry declares `policy: "noRow"`, and that each of those 4 registers a handler declaring no parameter. It asserts that the one remaining call sits inside a `for`-`of` statement whose iterated expression is the identifier `ROW_COMMAND_TABLE` imported into `extension.ts` from `src/commandTable.ts`, and that the handler it registers declares two parameters, passes both to `targetsFor`, and calls the `invoke` member the loop bound from that iterated value. Separately, and reading both modules at test time rather than the AST, it asserts that the id set of `ROW_COMMAND_TABLE` in `src/commandTable.ts` equals the set of `SELECTION_POLICIES` keys whose policy is not `noRow`, reporting each direction on its own.