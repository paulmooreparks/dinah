---
kind: decision
state: resolved
column: 0d86ad99cdbc
ts: 2026-09-14T02:18:52Z
ordinal: 58
note: "Taken at round 5, against comment 7558 finding 1, which is a design call rather than a wording repair and is why the card came back rather than being corrected in place.\n\nWhat is actually true of the boundary, read off the editor's own declarations rather than assumed. `handleDrag` is declared `handleDrag?(source: readonly T[], dataTransfer: DataTransfer, token: CancellationToken)` and `handleDrop` is declared `handleDrop?(target: T | undefined, dataTransfer: DataTransfer, token: CancellationToken)`. The drop side has no selection argument and no other channel, so anything the drop is to know about the gesture has to be put in the data transfer by the drag. `DataTransferItem.value` is documented as custom data whose \"original object can be retrieved so long as the extension that created the DataTransferItem runs in the same extension host\", and `handleDrag` documents that the items are preserved when dropped on another item in the same tree. Both handlers are ours, one host, one tree. Nothing here rests on a measurement or on undocumented behaviour, and neither declaration restricts the value to a single record.\n\nRound 4's §8 wrapped \"the payloads of those rows\", and only card rows have payloads, so the column header was gone before the drop code ran and AC-15's four-row list was a list the product never builds.\n\nThe two defensible answers, and why this one. Either the report speaks only about what crossed and says so, or the whole gesture crosses. Narrowing the report is honest and was rejected: it makes `selected` mean the targeted rows in every command on this card except one, and §3a exists precisely so that two routes cannot count differently. A reader who drags four cards and a column header and is told four rows finished has been told nothing about the row Dinah ignored, and silent narrowing is the shape the operator ruled against when he said multi-select must not act on rows the reader cannot see it acting on. Carrying the whole list costs one callback signature and buys one meaning of `selected` across the card.\n\nThe cast is the consequence that had to be taken with it. `DataTransferItem.value` is declared `any`, and nothing documents whether the editor itself puts anything under the recommended tree mime type, which this design may assume in neither direction. A single record survived a blind cast because every field was checked before use; a list does not, because the first thing the drop does with it is iterate. So `dragRowsFrom` validates the shape and answers `undefined` for anything it does not recognise, which is `dragPayloadFor`'s own conservative-miss discipline moved to the boundary. This is a repair to a real hazard the card created, not a widening: without it the card ships a loop over an unvalidated `any`.\n\nAC-15 now drives the round trip and AC-29 ties `extension.ts` to it, which is the same pairing AC-3 and AC-19 make on the registration boundary. Both of AC-15's plants were performed; the reduction is committed at `docs/specs/dinah-490-plant-reductions/`."
---
The drag's mime entry carries every dragged row, non-card rows included, so the drop path reports on the whole gesture rather than on the part of it that had a payload. `offerDrag`'s wrap callback is redeclared to take the row list, and `dragRowsFrom` reads it back with a shape check instead of a cast.