---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:49Z
ordinal: 31
note: "NOT PERFORMED, because the plants need a post-card `extension.ts` and a `src/reporter.ts` to parse, neither of which exists. What a reader must confirm before trusting it: that the walk really matched, by reading the collected count in the failure message on a deliberate miss.\n\nThe plants, for Test. First, bind an eleventh `vscode.window.show*Message` call on one of the hosts, to a member outside the two declared sets, and confirm both the count clause and the name-set clause redden; a plant that only raises the count proves less, because a wrong member with the right count is the case this criterion exists to catch. Second, delete `extends ReporterHost` from `ColumnCommandHost` and confirm the heritage clause reddens alone. Third, point the walk at a file holding no such call and confirm the exactly-10 assertion reddens, because an assertion over an empty collection is how this board has shipped a vacuous sweep twice.\n\nThe count is derived from the post-card world per factory rather than as a delta off one remembered binding, which is how round 6 arrived at 7. At `2aba8fc`, `grep -rn 'vscode\\.window\\.show' src/` returns fourteen lines, thirteen of them calls in `extension.ts` and one a mention inside a comment in `src/creationCommands.ts`. Six of the thirteen show a message: `showErrorMessage` once at line 223, `showInformationMessage` at 226 and 304, and `showWarningMessage` at 284, 307 and 333.\n\n| Factory | Message calls today | After ReporterHost |\n|---|---|---|\n| `commandHost` | showError, showInfo, confirmDestructive = 3 | plus showWarning = 4 |\n| `workbenchCommandHost` | showInfo, showWarning = 2 | plus showError = 3 |\n| `columnCommandHost` | showWarning = 1 | plus showError, showInfo = 3 |\n| total | 6 | 10 |\n\nThe two routes to the figure agree. Six today plus the four message calls this card adds, `CommandHost`'s `showWarning`, `WorkbenchCommandHost`'s `showError`, and `ColumnCommandHost`'s `showError` and `showInfo`, is ten; and four plus three plus three is ten. `appendLines` and `revealOutput` are two further bindings this card adds and they are deliberately outside the count, because they bind to `channel.appendLine` and `channel.show`, which are neither on `vscode.window` nor ending in `Message`.\n\nTen is forced rather than chosen, and an implementer meeting a mismatch must resolve it upward. The return-type clause forbids a shared `reporterBindings` helper spread into all three factories, so the per-factory duplication is the design and the count follows from it. Binding fewer members would make a criterion demanding a smaller number pass while leaving `WorkbenchCommandHost` and `ColumnCommandHost` short of `ReporterHost`, which is exactly the hole this perimeter exists to close.\n\nWhat this criterion cannot see, stated so nobody credits it with more: it reads what `extension.ts` binds, not what the editor does with it, and it says nothing about a module that reaches the reader some way other than a `vscode.window` message call. AC-34 is the other half, and `test/unit/layers.test.ts`'s existing vscode-import guard is what makes the pair reach the whole of `src/`."
---
A unit test walks `editors/vscode/src/extension.ts` with the TypeScript compiler API and collects every call expression whose callee is a property access on `vscode.window` and whose property name ends with `Message`. It asserts it collected exactly 10, so a walk that matched nothing fails rather than passing. The 10 are the message bindings of all three host factories after this card, `commandHost` 4, `workbenchCommandHost` 3 and `columnCommandHost` 3, and the figure is 10 rather than lower because the return-type clause below forbids a shared bindings helper, so each factory has to spell its own. For each call it finds the nearest enclosing property assignment and asserts that property's name is a member of `REPORT_CHANNELS` or of `PROMPT_CHANNELS`, both read from the compiled `src/reporter.ts` at test time rather than from a literal in the test, and asserts the set of property names it collected is exactly `showError`, `showInfo`, `showWarning` and `confirmDestructive`. It asserts every one of the 10 sits inside a function whose declared return type is `CommandHost`, `WorkbenchCommandHost` or `ColumnCommandHost`, that all three of those interface declarations name `ReporterHost` in a heritage clause, and that `REPORT_CHANNELS` holds exactly 3 members and shares none with `PROMPT_CHANNELS`.