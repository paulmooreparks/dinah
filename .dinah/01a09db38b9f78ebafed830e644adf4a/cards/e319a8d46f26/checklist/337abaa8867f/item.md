---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:48Z
ordinal: 24
note: "This is round 2's first blocker made checkable in both directions. Round 2 gave a cancelled run no constructor at all, so every caller built its own report, and the obvious spelling, `{ selected: contexts.length, entries: [], cancelled: true }`, hit the reconciliation arm before the cancelled arm and made a declined Archive confirmation raise an exception. The accepting half pins that the cancelled report `runBulk` builds reconciles like any other, because it holds one entry per row. The refusing half pins that the shape which used to throw still throws, though the arm catching it has changed: round 2's throw came from reconciliation, and this round's comes from the mark, because a hand-built literal carries no `reportMark` and §3b resolves the mark arm first. So cancellation did not become a hole in either defence. It is also the accepting-partner rule running the other way: a `summaryFor` that answered `none` for anything carrying `cancelled: true` would pass the first half and fail the second. It compares the thrown-ness of the call rather than a value.\n\nRed run to produce at Test, corrected at round 3 review (comment 7556, finding 3). The recipe this note carried into round 3 was \"move the cancelled arm above the reconciliation arm\", and that plant cannot fail: with the mark arm at case 1, above both arms being swapped, the unmarked literal throws either way and the assertion stays green. The plant that works is to move the cancelled arm above the MARK arm, to the top of `summaryFor`. The unmarked literal then takes the cancelled case, answers `none`, and the second half reddens while the first stays green. Perform it rather than reading it."
---
`runBulk` over five rows whose `ask` answers `undefined` spawns nothing, answers a report whose `selected` is 5, whose `entries` are five `skipped`, and whose `cancelled` is true, and `summaryFor` over that report answers level `none` without throwing. `summaryFor` handed a hand-built `{ selected: 5, entries: [], cancelled: true }` throws.