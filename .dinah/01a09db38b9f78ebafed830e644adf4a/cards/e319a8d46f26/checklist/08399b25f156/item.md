---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:48Z
ordinal: 22
note: "Compares a set derived from the source against the set the table drove, so no figure for the number of outcome kinds is typed anywhere on this card. Round 2 found the spec claiming `runDinah` answers five kinds where `CliOutcome` has six (`ok`, `refused`, `stale`, `unreachable`, `spawn-failed`, `not-json`), and the repair is to stop counting by reading: a new arm added by a later card joins this test's subject set without anybody editing it, and it goes red until it is driven. The floor of 6 is what stops a walk that matched nothing reporting a clean sweep, and it is a floor rather than an equality so that adding an arm does not redden a card that has nothing to do with it.\n\nThis pins the definition of a failed row to \"the outcome's kind is anything other than ok\" rather than to \"the verb refused\", which is round 1's blocker: an implementer reading the old wording writes `outcome.kind === \"refused\"` and a run whose binary vanished records five successes and tells the reader everything is done. `stale` is the arm a reader meets when their binary is behind, and round 2 noted it was the one going unexercised. The all-ok run is the accepting partner and is what stops an implementation that calls every row failed from passing on the refusing halves alone. `refusalMessage` in src/cardCommands.ts renders every non-ok kind through its generic arm, so one call covers all of them and no new rendering is written.\n\nRed run to produce at Test, reworded at round 4 so that it names a plant that can be performed. The driven set is derived from the collected set, so there is no hand-written table to delete a case from; the plant is a `continue` in the driving loop for one collected kind. The set-equality assertion then reddens, comparing the set of kinds actually driven against the collected set minus `ok`, while the floor of 6 stays green because the collection is untouched."
---
A unit test walks `editors/vscode/src/cli.ts` with the TypeScript compiler API, collects the string literal of the `kind` property of every member of the `CliOutcome` union, and asserts it collected at least 6. For every collected kind other than `ok` it drives a five-row run whose third spawn answers that kind and asserts the report holds exactly one `failed` entry whose `failure` is `refusalMessage` of that outcome and that the summary is `dialog.bulk.partial` at level `warning` naming refused 1. It asserts that the set of kinds it drove equals the collected set minus `ok`. A five-row run whose every spawn answers `ok` produces `dialog.bulk.allSucceeded` at level `info`.