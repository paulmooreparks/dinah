---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:51Z
ordinal: 52
note: "Rewritten at round 4, because both halves of round 3's version were wrong in the same place. Round 3 said Archive joins the `flowCommands` loop so that the number of `register` calls is unchanged, and it said each loop body maps its rows through `contextFor` and hands the command \"the surviving list\". D-21 replaces the loops with one table, so the register count does change, and AC-3 states the new figure and is the only place it appears.\n\nThe second half is the one that mattered. \"The surviving list\" is a filter, and a filter in front of a command reopens round 2's third blocker for every command the loops wired: a selection of one card and two column rows would hand the command one context, `runBulk` would read a length of 1, take the real host, and the card's own refusal toast would fire under a summary that also spoke. AC-11's third half and AC-27 are written against the design without the filter, so an implementer building to round 3's declared signature would have reached Test with a design that could not satisfy its own criteria, which is a spec defect rather than a test finding.\n\nWhat replaces it is not a rule each caller has to remember. A `ROW_COMMAND_TABLE` entry's `invoke` takes tree rows and passes them to `runBulk` whole, and `resolve`, which the entry declares and `runBulk` runs, is the one place in this card that reads whether a row yielded a context. A row that yields none is recorded as skipped rather than dropped, so `selected` is the number of rows the reader aimed at, in every command, by construction rather than by discipline. That is the same replacement D-13 made when the counts came off the callers and D-20 made when the reporting did.\n\nArchive still needs no registration of its own, for the reason it never did: it composes the same `CommandContext` the other card commands compose, so there is nothing a standalone call would do differently."
---
Archive joins `ROW_COMMAND_TABLE` as one more entry rather than taking a registration call of its own, and no command filters its targeted rows before handing them to `runBulk`.