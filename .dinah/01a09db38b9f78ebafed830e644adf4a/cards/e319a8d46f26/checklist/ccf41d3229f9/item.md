---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:50Z
ordinal: 43
note: Twelve refusals from twelve cards is twelve toasts, and twelve checkpoints repaint one folder twelve times. `collectingHost` wraps CommandHost, captures `showError` and records `checkpoint` folders, and forwards everything else including the prompts. The summary is a warning carrying the `dialog.openOutput.label` action with `revealOutput` on assent, which is `offerOutput` in src/workbenchCommands.ts applied to a second family. Per-row detail reaches the output channel as untranslated English, matching the channel lines the two registration loops in src/extension.ts write today, which D-21 replaces with one. CommandHost gains showWarning, appendLines and revealOutput, each copied in shape from WorkbenchCommandHost. D-13 then takes the loop itself into bulk.ts, so the one message is composed from a report whose counts cannot disagree with the rows it enumerates.
---
A run of more than one row shows one message, suppresses the per-row refusal toast, and checkpoints once per distinct folder.