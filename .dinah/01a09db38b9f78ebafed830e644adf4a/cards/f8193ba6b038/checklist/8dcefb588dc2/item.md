---
kind: acceptance_criterion
state: verified
column: b69abf918c42
owner: holder
ts: 2026-09-14T02:16:30Z
ordinal: 10
note: "DISCHARGED AT MERGE, 2026-08-27. The conditional resolved to its second branch: pull request 122 for dinah-97 was still open when this card merged, so this branch landed as it stood and the roster obligation passes to dinah-97, whose AC-11 carries it and is gated the same way.\n\nVerified independently after the merge rather than taken from the merge agent's account. Trunk is at 3e855ce, this card's squashed commit. `cmd/dinah/args.go:80` on trunk now reads `\"help\": true, \"version\": true, \"format\": true,`, so the session-flag map carries `format`. Trunk's `cmd/dinah/args_test.go` contains no occurrence of `format` at all, because dinah-97's guard has not landed yet, so there is no roster for the entry to be missing from and nothing is red today.\n\nWhat that means for the other card, stated here because its merge agent will read this. dinah-97's branch is now behind trunk, and when it merges, its own guard meets a `sessionFlagNames` carrying `format` with a roster that does not. That guard reddens by name, which is the outcome it was written to produce. dinah-97 must merge trunk, add `\"format\"` to the roster in `TestParseArgsRecordsNoDomainCaptureForASessionFlag`, and re-run `go test ./cmd/dinah/` before it lands.\n\nEarlier history of this item, kept because the gate error is worth not repeating. It was filed at Agent Code Review cycle 2 after a reviewer computed the merge rather than predicting it, and confirmed that the merged state is caught by the suite rather than silently unguarded. Its gate was first set to Merge, on the correct reasoning that this is a merge-time obligation and the standing rule forbids gating on Done, and the wrong column: a gate blocks entry into its own column, so the card could not reach the stage meant to discharge it, and Test blocked rather than guessing. Corrected to Acceptance, which admits the card to Merge and holds it out of Acceptance until the obligation is met."
---
The trunk carries `format` in the roster of `TestParseArgsRecordsNoDomainCaptureForASessionFlag` (`cmd/dinah/args_test.go`, added by dinah-97 on pull request 122), because this card adds `"format"` to `sessionFlagNames` and that guard holds the roster and the map to the same membership in both directions. At Merge, check whether pull request 122 has already landed. If it has, this branch's merge adds `format` to the roster and re-runs `go test ./cmd/dinah/` on the merged result. If it has not, this card merges as it stands and the obligation is recorded on dinah-97 before it merges.