---
kind: decision
state: resolved
column: c9428b3bc921
ts: 2026-09-14T02:16:31Z
ordinal: 20
note: "The wire did move, on one of the two forms. dinah-287 renamed CardView.State to Column and its key from \"state\" to \"column\", StateTitle to ColumnTitle and \"state_title\" to \"column_title\", and Substate to State and \"substate\" to \"state\"; Instructions, LegalMove, Listing and Offer each renamed a bare State to Column and \"state\" to \"column\". A caller parsing canonical JSON therefore reads different keys after this card's branch merges, and that is trunk's change rather than this card's. The compact form is positional: every record is fields joined by \"|\" whose meaning comes from its position and from the record's kind token (fmt, rsp, card, wstream, instr, move, ctx, msgval, wb, aff, lst, off), and not one of those kind tokens is a word the rename touched. So no compact payload's bytes change, the record field order is unchanged, and fmt|compact|1 stays at 1 because the grammar a caller parses is the same grammar. The consequence for AC-3, AC-4 and AC-5 is that they still compare against the canonical answer as they always did; those tests decode both forms into the same Go structs and compare the structs, so the only thing that had to move was the decoder's mapping from position to field, alongside the encoder's. Remapping by meaning matters here because the rename is a rotation rather than a substitution: Substate became State while State became Column, so a mechanical find-and-replace of State to Column would have compiled and written the column identifier into the field that carries ready, active or blocked. Verified three ways: by reading each field's doc comment in internal/verb/library.go and internal/verb/read.go, by comparing a live `dinah --format compact claim` payload against the same claim's canonical JSON field by field, and by transposing the two in the encoder and watching the equivalence tests name it. The spec's record table under \"The compact grammar\" still spells those three fields state, state_title and substate; the code and its doc comment are now the accurate copy, and the spec's table wants the same edit whenever somebody with the authority to make it passes through."
---
The compact grammar carries no field names on the wire, so dinah-287's vocabulary rename moved the canonical JSON's keys and left the compact payload's bytes alone. Each of the seven reads was remapped by what the field means rather than by what it is called, and the record tables in cmd/dinah/compact.go's doc comment now read column, column_title and state where they read state, state_title and substate.