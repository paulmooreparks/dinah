---
kind: acceptance_criterion
state: verified
column: c9428b3bc921
ts: 2026-09-14T02:17:10Z
ordinal: 3
note: "Test: TestTheVocabularyMigrationSeesTheWorkbenchYouAreStandingIn, cmd/dinah/vocabulary_test.go (dinah init in a fresh directory, unwind to dinah-core/0.6, then `check --migrate-vocabulary` run from inside that directory with no flag). Rerun: `go test ./cmd/dinah/ -count=1 -run TestTheVocabularyMigrationSeesTheWorkbenchYouAreStandingIn -v`. Green after the fix.\n\nCycle 2 (Agent Code Review push-back, comment 6127 major): the two output assertions compared a path the head resolved through os.Getwd against one joined onto the fixture's t.TempDir() value. They now compare against `resolvedAnchor := resolvedDir(t, anchor)` at vocabulary_test.go:892, which reproduces the head's own os.Chdir/os.Getwd sequence (cmd/dinah/main_test.go:148), the remedy the corpus entry \"A test's expected path guessed at instead of reproduced\" prescribes and the shape main_test.go:5126 already uses.\n\nArmed twice after the repair, both with the exact command above:\n(a) the defect the test was written for. Deleting the `benchIn(root, false)` probe and its two fold blocks from `enumerate` in internal/bench/bench.go turns it red at vocabulary_test.go:917, :920 and :931, with the command printing \"Carried 0 workbenches across the column and state rename\", the card's reported defect verbatim. Restored from a byte-identical backup and reconfirmed green.\n(b) the repaired comparison itself. Replacing line 892 with `resolvedAnchor := anchor + \"-a-spelling-no-directory-answers-to\"` turns it red at :917 and :931, so both assertions rest on the resolved value rather than passing for some other reason. Restored and reconfirmed green."
---
A cmd/dinah test reproduces the card's exact repro (dinah init, unwind to a pre-vocabulary revision, dinah check --migrate-vocabulary run against the directory whose own .dinah holds the board) and asserts the workbench is reported (Migrated in preview, or actually migrated on apply) rather than reporting zero workbenches.