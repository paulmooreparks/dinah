---
kind: open_question
state: resolved
column: c9428b3bc921
owner: operator
ts: 2026-09-14T02:17:19Z
ordinal: 11
note: "Operator ruling, 2026-09-04, given directly in session: **record the finding here and file nothing against Andoneer.**\n\nHe was offered three shapes and chose the middle one. He declined filing it himself on that platform's own board, and declined having this session file it for him. So D-1's decision to leave the server-side route alone stands, and this question closes without a follow-up card anywhere.\n\n**The finding, recorded here because this card is now the only place it lives.** `list_workbench_documents` and `list_project_documents` return an empty result with no error when a document's body would exceed 262,144 bytes and the body is requested through the field selector. An agent reading that result sees an empty document rather than a refusal, and nothing tells it that another route serves the same document in full. That is not a hypothetical: an implement agent on dinah-332 drew exactly that conclusion, said the corpus was unreadable, and worked without it, while the next agent read the same document through `get_workbench_document` without difficulty and used what it found as the precedent for its main finding.\n\nThe route is Andoneer platform server code. A grep of this repository for its implementation returns nothing, which the design reviewer confirmed independently, so no agent working this workbench can repair it.\n\n**What this card does instead, and what it does not.** Rotating the corpus into size-bounded parts means the browsing route serves each part in one call, so the silent failure stops being reachable for this document. The route itself still fails the same way for any future document that outgrows the ceiling, on this board or any other backed by the same platform. That residue is deliberate and now has the operator's ruling behind it rather than an agent's.\n\nNothing further is needed from him on this question, and the card is otherwise clean."
---
D-1 declines, on the spec agent's own authority, to file a follow-up card against the Andoneer platform project for hardening the failing list_workbench_documents/list_project_documents route (refuse with a size+route message instead of returning empty). That platform is the operator's own (per portfolio: Andoneer is his private meta-project), and whether it is worth a card there is a cross-project backlog call no Dinah-workbench agent can make. Confirmed by grep: no Go implementation of list_workbench_documents exists in this repository, so the claim that this is genuinely out of reach from here is verified. Does the operator want that follow-up card filed against Andoneer, and if so, should this card or a human file it?