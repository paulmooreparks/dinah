---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:17Z
ordinal: 8
note: "RULED AT AGENT CODE REVIEW, 2026-09-10, and the criterion's text was corrected rather than the card sent back (andon-709). The original text required a whole-tree property that AC-9 forbids this branch from reaching, so the two criteria could not both be satisfied and the contradiction was in the criteria rather than in the code. What the code does is right: the false clause is gone from ResolvePath's doc comment, the replacement names no attachment, and the branch introduces no fresh claim of the kind. The one hit outside the permitted classes, `internal/guide/guides/query.md:142`, reads \"from a card, a column, this workbench, or a workstream down to the comment or the attachment it holds\", and the trailing clause is false of a workstream, below which nothing is addressable. `git log origin/main -- internal/guide/guides/query.md` puts that sentence on the trunk since 863b7c5 (dinah-457), so it predates this branch and is not this card's defect. dinah-470's spec was read on 2026-09-10 while it sat in Build Queue: it names only `internal/guide/guides/references.md` and the `param.*` catalogue keys, and never query.md, so it did not already cover the sentence. It was carried there as a second instance of the same class rather than filed as a card of its own, per the workbench's filing rule, and the corrected wording is written out in that comment. The implementer's own evidence is preserved: the sweep returns exactly one hit outside the permitted classes, and every other hit is the dinah.not-attachable machinery, a test asserting that refusal, a docs sentence about what a WORKBENCH contains, verb/read.go's comment correctly saying a workstream has no attachments collection, or this card's own new fixture description."
---
No sentence this branch ships says a workstream carries attachments. The clause is gone from `ResolvePath`'s doc comment at `internal/bench/resolve.go`, and running `grep -rniE 'workstream[^.]{0,120}attachment|attachment[^.]{0,120}workstream' --include=*.go --include=*.md .` over the whole tree returns only the `dinah.not-attachable` machinery that refuses such an attachment, the tests asserting that refusal, this card's own record, and the one pre-existing hit at `internal/guide/guides/query.md:142`, which AC-9 forbids this branch from touching and which Agent Code Review carried to dinah-470 on 2026-09-10. Paste the command's output into the verification note.