---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:18:28Z
ordinal: 16
note: "Corrected in the second Implement pass, twice. The first pass's closing count (\"Before: 16 in-class sightings, 14 false. After: 14 corrected, 0 false, 2 true\") was wrong, and the first version of this correction carried a count I had not established either. What follows is only what I checked line by line.\n\nTHE SWEEP. Run mechanically across cmd, internal, docs and README.md over a widened pattern (no verb, nothing writes/creates/reads/sets, no command writes/creates, not written by any verb, never written by). It returns 42 lines on this branch, and I read all 42 for meaning rather than for wording. 26 carry a claim in this class (no verb writes, creates or consults some thing); the other 16 are about other subjects (what reads a journal or a link, a normative CORE requirement, error-message templates, a historical spec sketch, wordplay on \"matches nothing\").\n\nWHAT THE 26 SPLIT INTO. 7 were false and are corrected in this diff. 2 were false and were corrected in the first pass (check_test.go:527 and :672). 13 are true and untouched. 3 are false and are NOT corrected here, for the reason below. 1 I did not establish either way, and I am saying so rather than counting it: internal/profile/conformance_test.go:27's CORE-STATE-6 claim that v0 exposes no verb that reorders a flow, which turns on whether reshape counts as reordering, and I did not settle it.\n\nTHE 7 CORRECTED HERE, each checked against the code rather than against another sentence. internal/bench/check_test.go's writeItem comment said no verb creates a checklist item, contradicting what this same diff writes into docs/design/format.md:1288 (bench.AddItem assigns the ordinal at item.go:97). cmd/dinah/row_sweep_test.go at three sites, on a checklist item, on a column (dinah column new creates one, runColumn at commands.go:1839) and on a card's body. internal/verb/reshape.go said re-kinding a column has no verb, where dinah set <column> kind does it: dinah get doing kind printed work, dinah set doing kind intake exited 0, dinah get doing kind printed intake. That comment's second clause, that re-kinding runs no gate, was tested separately and is still true, since the same set succeeded with a claimed card standing in the column. internal/verb/search_test.go said framing prose is written by no verb, where KindCard declares {Name: BodyField, Prose: true, Clearable: true}. And internal/verb/changes.go said nothing appends to an archived journal because no verb takes an archived card as its subject, where its own archiveEvents set two lines below carries contract.EventRestored; archiving and restoring a scratch card left both an archived and a restored line in that card's own journal.ndjson.\n\nTHE 3 NOT CORRECTED, recorded rather than fixed because the fix is not a comment edit. internal/profile/conformance_test.go's outOfReach table accounts for CORE-ITEM-1, CORE-ITEM-2 and CORE-ITEM-3 with \"v0 stores no structured item on a card\", \"the tool reports no structured item\" and \"no verb consults a structured item\". All three are false: dinah file stores one, dinah show reports one, and the move gate consults one, which is the behaviour this very card is about. Correcting them means driving those three statements with conformance tests instead of exempting them, which is a piece of work somebody would pick up on its own and is well outside this card's diff. The run is green either way, because the table only requires each statement to be accounted for. Flagged here for Agent Code Review to route.\n\nTwo of the 26 that a phrase search cannot reach are worth naming, because they make the workbench's own point: the sibling exemptions CORE-ITEM-1 and CORE-ITEM-2 say \"stores no\" and \"reports no\" and match none of the swept phrases. I found them only by reading around a line the sweep did return."
---
The stale-claim fold-in was applied to every false instance the sweep found rather than to the one instance the dispatch quoted; the second pass widened the sweep, corrected seven more, and recorded three it may not fix here.