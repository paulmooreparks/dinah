---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:18:28Z
ordinal: 14
note: "Rewritten in the second Implement pass, because the first version of this decision cited evidence that is false. It named check.slug-missing as a sibling putting {detail} last; that key reads \"column {detail} carries no slug, so it is reachable only by its identifier or its quoted title\", which is mid-sentence, so the citation did not support the claim and would have misled the next reader.\n\nThe rule the catalog actually keeps is about how many tokens the Detail carries, not where the placeholder sits. A one-token Detail sits mid-sentence in about forty check.* keys, check.slug-missing among them. Every multi-token Detail goes last, and check.go carries exactly four such composers: card.Ref(b.Slug) + \" \" + override.Column for check.unknown-tier-column (\"a card names a tier_at column this workbench does not carry: {detail}\"), and the three FindingUnknownLevel sites, TierField + \"@\" + column.Ref() + \" \" + column.Tier at check.go:430, TierField + \"@\" + override.Column + \" \" + override.Tier at :459, and axis + \" \" + stored at :568, all rendering as \"this workbench does not declare {detail}\". This card's Detail carries three tokens, so it goes last.\n\nThe English now reads \"a checklist item names a column that will never hold a card, because it either resolves to no column at all or resolves to one whose identifier is not what is stored: {detail}\". Run against a scratch workbench with two hand-edited items, `dinah check` printed \"... is not what is stored: scratch-1 0838df7d5b43 doing\" and \"... : scratch-1 b00b84dbf65d no-such-column\", closed with \"2 defects.\" at exit 5, and --json carried the unchanged Key and three-token Detail. German and Hindi were rewritten to the same shape and re-fingerprinted to 27409d9d6b02c475; the five skeletons carry the new English. No key was added or removed, so the catalog totals stay at 929 and docs/quick-start.md is untouched."
---
The English catalog text moves {detail} to the end of the sentence rather than shipping the spec's mid-sentence wording, because the check catalog's rule is a token-count rule.