---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:18:28Z
ordinal: 18
note: "Recorded forward rather than fixed at Implement, because Implement does not edit spec prose. Section 2 of the spec prescribes the catalog entry verbatim and still reads \"a checklist item on {detail} names a column that will never hold a card: it either resolves to no column, or resolves to one whose identifier does not match what is stored\". Agent Code Review's second finding ruled that wording against the catalog's token-count house rule, so the branch now ships \"a checklist item names a column that will never hold a card, because it either resolves to no column at all or resolves to one whose identifier is not what is stored: {detail}\", with German, Hindi and the five skeletons following it.\n\nThe spec prose field and the checklist are two separate stores and editing one never touches the other, so the second Implement pass changed the CHECKLIST only: D-4's text and note, AC-8's note, and this item. The spec prose is unchanged and disagrees with the branch on this one sentence. Nothing else in the spec is affected: the Detail composer, the Key, the finding constant and the sweep are all exactly as section 2 specifies.\n\nAGENT CODE REVIEW RULING, round 2. This is a recorded supersession and not a spec defect, so the card does not go back to Spec and this item is closed here rather than passed along a third time.\n\nThe reasoning. The spec's section 2 quotes one English sentence as a prescription. Agent Code Review then found the prescription wrong against a house rule the catalog keeps without exception, and I re-verified that rule myself this round rather than taking it from either prior note: internal/bench/check.go carries exactly four multi-token Detail composers, at lines 430, 452, 459 and 568, and all four render through catalog texts that end on {detail} (\"this workbench does not declare {detail}\" and \"a card names a tier_at column this workbench does not carry: {detail}\"), while a one-token Detail sits mid-sentence in check.slug-missing and check.unknown-column. The shipped catalog is what the tool prints and what the tests hold, so it is the authority for this key. A spec whose prescription was overruled by the review stage that exists to overrule it is a correct spec with a superseded line in it, and that is an ordinary outcome of the pipeline rather than a fault to route backwards.\n\nWhat that leaves. One sentence of spec prose disagrees with one catalog entry, the disagreement is recorded here, in D-4 and in this card's second WHAT SHIPPED note, and any reader who reaches the spec's section 2 reaches this item too. The operator may bring section 2's quoted English into line at his station if he wants the two stores to agree, and it costs him one edit. Neither Implement nor Agent Code Review holds a tool that writes spec prose, which is why it is stated as an option for him rather than done."
---
The spec's own prose still quotes the superseded English catalog sentence, and Implement did not edit it; the shipped catalog is the authority for this key.