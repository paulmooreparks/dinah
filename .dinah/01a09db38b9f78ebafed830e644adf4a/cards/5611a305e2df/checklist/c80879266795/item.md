---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:17:41Z
ordinal: 17
note: "Armed five times, each restored before the next. (1) title removed from the teaches list of the entry for 326, entry left standing: \"docs/quick-start.md:330 declares title: Release 0.2 and the file the binary wrote carries title: release-notes; either the document is stale or testdata\\quickstart-file-blocks.txt:19 declares that the block teaches the key\". (2) format added to that list: \"declares that the block at docs/quick-start.md:326 teaches format, and the two values agree, so the key does not need declaring\". (3) entry for 344 repointed at 345: \"the entry names docs/quick-start.md:345, and no `file` block opens there\". (4) entry for 344 deleted: \"docs/quick-start.md:344 opens a `file` block and testdata\\quickstart-file-blocks.txt carries no entry for it\". (5) unwritten deleted from the entry for 724: \"docs/quick-start.md:724: the sandbox carries no notes.txt for the block to be held against, and testdata\\quickstart-file-blocks.txt:21 does not declare the block unwritten\". The fifth is unambiguous because the ledger's rules run for every file block whether or not the comparison reaches it, which is stated in checkFileBlockEntries' doc comment. Restored, green."
---
The file-block ledger's rules hold. Arming, five runs of `go test ./cmd/dinah -run TestTheQuickStartMatchesTheTool`, each restored before the next: remove `title` from the `teaches=` list of the entry for the block at 326, leaving the entry standing, and watch it fail naming the document, the `title` line inside the block, the document's `Release 0.2`, and the file's `release-notes`; add `format` to that same list and watch it fail saying the two values agree, so the key does not need declaring; repoint the entry for the block at 344 at line 345, which carries no opening fence, and watch it fail naming the stale entry; delete the entry for the block at 344 and watch it fail saying the block carries no entry; delete `unwritten` from the entry for the block at 724 and watch it fail saying the sandbox carries no `notes.txt` for the block to be held against.