---
kind: decision
state: resolved
owner: holder
ts: 2026-09-14T02:17:42Z
ordinal: 30
note: The three are TestTheQuickStartCountsTheCommandsTheBinaryOffers, TestTheQuickStartWorkbenchFileDeclaresWhatTheBinaryWrites with its helper checkAnchorBlockDeclaresTheBinarysValues, and TestNoGuideCarriesATranscriptTheReplayDoesNotDrive. Each holds a single instance of a class the replacement holds generally, and keeping both would put one fact behind two mechanisms. The tree's one deliberate duplication, bannedTypography, is justified by a package boundary that does not exist here. AC-4, AC-11, AC-14, and AC-15 are the criteria that pay for the removals, and a reviewer who thinks the payment insufficient should say which removal to reverse. A fourth check that also reads a prose figure, assertTheGuideCountsItsOwnTable in main_test.go, is deliberately kept, because it counts the references guide's own drawn table rather than the binary, and the ledger records the dependency on it rather than duplicating it.
---
Three existing checks are removed because the new mechanisms subsume them, and each removal is paid for by re-running the removed check's own arming against its replacement.