---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:17:41Z
ordinal: 19
note: "Re-read independently by Test on 56db064, all four parts confirmed from the source rather than the record. (1) quickstart_test.go lines 35-41 no longer call a prose count unheld: the bullet names prose_figure_test.go, states the reach as nine nouns and 23 figures, and says a figure counting refusals or workstreams is still held by nothing. (2) prose_figure_test.go lines 17-56 carry all three required statements plus the two residuals, including the honest admission that two of the 23 entries sit in a holds=unreachable state that no check can revalidate. (3) prose-figures.txt opens with a header telling the writer to register the noun or write about a registered one \"because nothing will remind you\". (4) writeNarrativeFile's comment states the unshared-key rule. One residual nit, left rather than returned: the bullet at lines 60-65 still says the source checks are \"the whole of their coverage\" for the embedded guides, which is now marginally understated because the shows=table check compares a guide header against the tool's own rendering. The same bullet corrects itself two sentences later, and the clause was equally true before this card, so it is not worth a round trip."
---
The doc comments naming the blind spots tell the truth after the change, including about what is still not covered. Verdict: reading `cmd/dinah/quickstart_test.go` lines 29 to 66, the entry for "a count written out in prose" either is gone or names what now holds such a count; `cmd/dinah/prose_figure_test.go` carries a doc comment saying that a figure beside an unregistered noun is invisible, that nothing notices a new countable noun entering the corpus, and that a `by=` pointer proves the named test exists rather than that it holds the figure; `cmd/dinah/testdata/prose-figures.txt` opens with a header comment telling a writer to register the noun or write about a registered one, and refusing `counts=none` in the same breath; and `cmd/dinah/quickstart_test.go` says beside the frontmatter comparison that a key the document carries and the standing file does not is written through unchecked. A reviewer confirms this by reading the diff.