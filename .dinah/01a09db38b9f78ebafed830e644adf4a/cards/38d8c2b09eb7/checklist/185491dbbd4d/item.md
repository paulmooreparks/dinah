---
kind: decision
state: resolved
ts: 2026-09-14T02:17:39Z
ordinal: 13
note: internal/guide/guides/mcp.md:28 shows a profile revision the binary never emits and mcp.md:95 names three of the six members its own line 152 lists, so both are false statements shipping inside the binary, which is the class this card exists for, and each is repaired by removing an enumeration rather than by writing a fresh one. docs/design/surfaces.md:19-21 is excluded because correcting the tense on `dinah serve` and `dinah lsp` rules on whether those heads are still planned, which is a design decision rather than a transcription error, and the document's caveat at lines 12 to 14 already frames the section as design in progress. `show --fields` is excluded because docs/quick-start.md:4 promises every command and `show` is walked; a missing flag is an omission, and omissions are dinah-447's.
---
Both mcp.md items are taken; the surfaces.md tense and the missing `show --fields` walkthrough are not.