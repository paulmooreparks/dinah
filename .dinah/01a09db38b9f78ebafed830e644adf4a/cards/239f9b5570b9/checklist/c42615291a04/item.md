---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:50Z
ordinal: 2
note: "Verified on 3b9c51c. Test: cmd/dinah/address_sweep_test.go, TestEveryPrintedReferenceResolves cases \"show, attachments block\" and \"attachments command\". Command: go test ./cmd/dinah -run 'TestEveryPrintedReferenceResolves/(show,_attachments_block|attachments_command)'. Identity plant: cmd/dinah/render.go renderAttachments drew strconv.Itoa(attachment.Ordinal) in the first cell; built clean, and both cases went red at address_sweep_test.go:207, \"row 1 draws \\\"1\\\", and the reference for that row is \\\"fx-1/attachments/1\\\"\". Heading plant: the block's columns call swapped to s.columns(\"checklist\", \"state\", \"filename\", \"description\"); built clean, and both cases went red at address_sweep_test.go:193, \"draws the headings [\\\"State\\\" \\\"{column.checklist.filename}\\\" \\\"Description\\\"], wanted exactly [\\\"Ref\\\" \\\"File\\\" \\\"Description\\\"]\". One deviation from the recipe: s.columns takes a group and then one name per column, so swapping only the first heading key is not expressible without changing the group, and the plant changed the group. Both restored byte-identically (cmp clean) and green again."
---
The attachments block prints a reference in place of the bare position, under both `dinah show <card>` and `dinah attachments <ref>`. In cmd/dinah/address_sweep_test.go, TestEveryPrintedReferenceResolves cases "show, attachments block" and "attachments command" round-trip the first cell through `dinah path` and require exit 0, and a second assertion requires that cell to equal the AttachmentView.Ref the same command's --json run carries for the attachment on that row, paired by the row's position among the listing's attachments. A third assertion requires the block's heading row to carry exactly the texts of column.attachments.ref, column.attachments.filename and column.attachments.description in that order, so a Ref column added beside the # column rather than in place of it fails on the count. Arm the identity arm by filling the cell with strconv.Itoa(attachment.Ordinal), and arm the heading arm by swapping the block's first heading key for column.checklist.state; both compile, and neither refers to a catalogue key this card deletes.