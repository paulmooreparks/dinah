---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:17:53Z
ordinal: 35
note: "English \"Ref\", German \"Referenz\", Hindi \"संदर्भ\", source msg.Fingerprint(\"Ref\") = 9ff1e019feac1ee2 in both, and \"Ref\" with skeleton true in the five skeleton catalogues. The reasoning is D-16's and is not repeated: the attachments block is being brought into line with the checklist block, so it takes that block's word rather than a third spelling. This entry is filed separately from D-16 because the staleness contract asks for one item per key and a reader auditing one key should not have to read another key's item to find it. What is specific to this key is its context, which says the heading sits over the attachment's own reference in the attachments block of `dinah show` and in `dinah attachments`, both of which draw a heading row, so it does not carry `column.checklist.ref`'s sentence about a block that draws none."
---
`column.attachments.ref`: added; German and Hindi copied from `column.checklist.ref` on the same argument as `column.comments.ref`.