---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:53Z
ordinal: 33
note: "The document is a replayed transcript and round 1's spec named neither it nor the flag. Three changes reach it: workstream.line, card.line.workstreams and the workstreams listing's heading and first cell. At 22a35fc `grep -n 'autumn' docs/quick-start.md` prints 850, 851, 856, 868, 869, 871, 872, 884, 891, 894, 911, 920, 921, 931, 932, 937 and 938, of which the output lines that move are 851, 869, 872, 884, 921 and 932. Two more move that no grep for autumn can find because they carry no workstream name: line 882, the listing's heading row, and line 883, the rule row beneath it, whose widths are recomputed. Round 2's note listed 883 without 882 and attributed the whole list to the grep, which produces neither; those two lines were read out of the block. The regeneration decides the final list rather than this note. The prose half no guard reaches spans lines 940 to 942 rather than line 941, and the document wraps it mid-clause between \"nothing else\" and \"does\": \"A workstream names its kind in both of those commands, and nothing else does\" becomes false in both directions, since every screen comes to print the kind and workstream get, workstream set, join and leave all come to accept it. That wrapping is why AC-12 flattens the document's whitespace before testing for the clause, and round 2's raw containment test passed at 22a35fc before anything was edited. The replacement keeps the reason the sentence gives, which stays true, and splits the claim the way the code splits it: the general reference commands need the prefix because ResolvePath tries a bare handle against the columns and the cards first, and the workstream-only commands take either spelling through WorkstreamByRef. The implementer wraps the replacement to the document's own width, and it shares no run of words with the retired clause, so the wrapping chosen cannot satisfy or defeat AC-12. The alternative of leaving the sentence and filing a follow-up was rejected because the document is what a reader trusts first and the commit before this card's base was a sweep of six false documented statements. The new console block runs two reads that change nothing, so no later block in the narrative moves, and its output is written by the regeneration rather than typed into the spec."
---
docs/quick-start.md is regenerated with -update-quick-start, one prose sentence is rewritten by hand, and the claim that sentence makes is moved into a replayed block so a guard can hold half of it.