---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:52Z
ordinal: 27
note: "A table in a spec proves that somebody enumerated once. The human-side guard reads renderSitesInSource, the walk the package already declares over every s.table and s.tableLines call outside table.go, and the machine-side guard parses internal/verb for every struct carrying an identifier; each requires a site or a type to be swept or exempt with a ground from a closed set. That is the same shape dinah-456 D-18 settled for the MCP exemption table, and it is why the sweep can be trusted after the next card touches the renderer. Round 1 said the human-side guard would parse for every s.columns call, and D-14 records why that was wrong and what replaced it. The honest weakness is unchanged: a ground is a declaration a reviewer has to weigh rather than a fact the machine checks, which the guard says outright, and what it guarantees is that no drawing surface reaches a green build unargued."
---
The sweep's completeness is proved by two parser-derived guards with declared exemption grounds, rather than by the spec's table.