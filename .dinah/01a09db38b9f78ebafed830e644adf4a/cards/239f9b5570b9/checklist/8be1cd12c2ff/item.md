---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:51Z
ordinal: 12
note: "Verified on 3b9c51c. Tests: cmd/dinah/quickstart_test.go, TestTheQuickStartMatchesTheTool (green with no -update-quick-start in the invocation), plus the two added there, TestTheQuickStartDropsTheRetiredWorkstreamClause and TestTheQuickStartShowsBothWorkstreamSpellings. Command: go test ./cmd/dinah -run 'TestTheQuickStart'. Replay plant: renderWorkstreams drew s.slugCell(workstream.Slug) again in the first cell; built clean, and the run went red at quickstart_test.go:817 reading \"docs/quick-start.md:880: `dinah workstream` line 3 wanted: workstream/autumn  Autumn release  active  2, got: autumn     Autumn release  active  2\". Containment plant: the retired sentence restored in the document under a wrapping it has never used; the run went red at quickstart_test.go:1821 naming the clause. Block plant: the second command line of the new console block changed to `dinah contents workstream/autumn-2025`; the run went red at quickstart_test.go:1856. All three restored byte-identically (cmp clean) and green again. The regeneration wrote eight output lines across five replayed blocks plus the new block, and it also updated the catalogue coverage table at the foot of the document from 908 to 909, which is the two keys this card adds less the one it retires. THE PROSE CHANGE NEEDS A HUMAN'S EYE: lines 940 to 945 of docs/quick-start.md now read differently, and no automated check can say the replacement is true."
---
docs/quick-start.md is regenerated, and the one sentence in it this card makes false is gone. TestTheQuickStartMatchesTheTool in cmd/dinah/quickstart_test.go is green with no -update-quick-start in the invocation, which is the arm covering every transcript block the card moves. Two assertions are added to that file. The first reads the document, replaces every run of whitespace with a single space, and requires the flattened text not to contain "A workstream names its kind in both of those commands, and nothing else does"; the flattening is required rather than optional, because the document wraps that clause between "nothing else" and "does" and a raw containment test over the same phrase passes at 22a35fc before anything is edited. The second requires the document to carry a replayed console block whose command lines are `dinah workstream get workstream/autumn-2025 status` and `dinah contents autumn-2025`, which the replay then holds to what the head prints. Arm the replay arm by reverting the workstreams listing's first cell to workstream.Slug, which compiles and turns TestTheQuickStartMatchesTheTool red on the block whose command line is at 881 with a byte diff. Arm the flattened-containment arm by restoring the retired sentence anywhere in the document, in any wrapping, which is a document edit and turns that assertion red.