---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:52Z
ordinal: 23
note: "The cell is composed by cmd/dinah/render.go's workstreamRef, whose own comment calls it what a person types to reach one workstream a card lists, so it is an address. Leaving it bare while the listing prints the prefixed form would put two spellings of one entity on one screen, which is the disease this card treats. The cost is that every card line carrying a membership grows by the length of the prefix, and it falls on `dinah show` and on the line printed after an act, and on nothing else: s.workstreamsCell is reached only from renderCard at cmd/dinah/render.go:96, whose own doc comment names that as the single site every act prints its card line from. Round 1 of this note also named `ls`, `next` and `status`, and that was wrong; all three draw tables with no membership column, verified by run at 22a35fc on a workbench whose only card belongs to a workstream. The alternative considered was exempting the cell as a membership rather than a row of its own, which fails because the reader's next act on that value is to type it."
---
The card line's workstream cell changes with everything else rather than keeping the bare slug.