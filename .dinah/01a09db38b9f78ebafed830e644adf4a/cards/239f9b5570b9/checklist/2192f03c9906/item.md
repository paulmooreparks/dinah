---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:51Z
ordinal: 11
note: "Verified on 3b9c51c. Test: cmd/dinah/address_sweep_test.go, TestEveryPrintedReferenceResolves cases \"contents, containment table\" and \"tree, grouped table\". Command: go test ./cmd/dinah -run 'TestEveryPrintedReferenceResolves'. Two plants were needed, because the recipe the criterion gives reddens a different arm from the one it names. Recipe as written: treeFields returned node.Ref for a group node; built clean, and the run went red on eight rows, but at address_sweep_test.go:193 on the guide arm rather than the group arm, reading \"tree, grouped table row 1 draws \\\"  |--               column         2\\\", which carries no guide at all\", because a group node's Ref is empty and the row is then a guide prefix with nothing behind it, which guidedLead will not decompose. Second plant, aimed at the arm the criterion names: treeFields returned node.Axis for the group cell, which keeps the row non-empty and guided; built clean, and the run went red at address_sweep_test.go:207 reading \"tree, grouped table row 1 draws \\\"column\\\", and the reference for that row is \\\"intake\\\"\", plus five more. Both restored byte-identically (cmp clean) and green again. So both arms are armed, and the criterion's prediction about which one fires is wrong; recorded here rather than smoothed over."
---
The containment table both `dinah tree` and `dinah contents` draw is swept, with its drawing prefix stripped and its grouping rows skipped by declaration rather than by guess. In cmd/dinah/address_sweep_test.go the cases "contents, containment table" and "tree, grouped table" each run the command twice against one fixture workbench, once at the terminal and once with --json, walk the payload's Root.Children in pre-order, and pair node i with drawn row i. Each case asserts that the row count equals the node count; that guidedLead reports a guide on every row of the block, failing the row by name where it reports none, so a renderer that stops drawing guides fails loudly rather than having its whole cell read as a reference; that for a node whose Kind is not verb.NodeGroup the row's first cell, after guidedLead has stripped the guide prefix, equals that node's Ref and exits 0 through `dinah path`; and that for a node whose Kind is verb.NodeGroup the same stripped cell equals that node's Value, or the text of tree.unset where Value is empty, with no round trip attempted. Arm it by having treeFields return node.Ref for a group node instead of node.Value, which compiles and turns the group arm red on the first grouping row of `dinah tree`.