---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:50Z
ordinal: 4
note: "Verified on 6a8ad15 (round 2), previously on 3b9c51c. Test: internal/verb/read_ref_test.go, TestEveryChecklistItemCarriesAReferenceThatResolves. Command: go test ./internal/verb -run TestEveryChecklistItemCarriesAReferenceThatResolves. Plant: itemRef's unaliased branch returned the empty string; built clean, and the run went red at read_ref_test.go:86 (\"the item c00000000003 of kind \\\"risk\\\" carries no reference, so nothing on the row tells a reader how to name it\") and at read_ref_test.go:99 (\"the risk item is addressed \\\"\\\", wanted the unaliased \\\"fx-1/checklist/3\\\"\"). Restored byte-identically (cmp clean) and green again. The risk item is written by hand through the harness, since no verb files an undeclared kind. ROUND 2, the damaged-collection arm the code review found missing: internal/verb/tree_ref_test.go, TestShowAddressesAnItemBelowADamagedOneTheWayTheWalkDoes. Command: go test ./internal/verb -run TestShowAddressesAnItemBelowADamagedOneTheWayTheWalkDoes. Cite the two t.Errorf calls at tree_ref_test.go:123 and :132. Plant: Show's item loop took position from its own index (position := i + 1) instead of memberPosition, which is exactly what shipped in round 1; built clean, ran, and both assertions reddened, the first \"contents draws it as \\\"fx-1/checklist/3\\\" where show draws it as \\\"fx-1/checklist/2\\\"\" and the second \"that address reaches the item fdba4e55d426 instead\". Restored byte-identically (cmp clean) and green again."
---
Every checklist row carries an address, including an item whose kind is none of the three the format declares. In internal/verb/read_ref_test.go, TestEveryChecklistItemCarriesAReferenceThatResolves builds a card carrying one open question, one acceptance criterion and one hand-written item.md whose kind is "risk", then asserts for all three that ItemView.Ref is non-empty and that Bench.ResolveEntity(Ref) returns the item whose ID matches; it further asserts the risk item's Ref equals `<card>/checklist/3` and the criterion's equals `<card>/ac/1`, so an implementation that aliases everything or aliases nothing fails. Arm it by returning the empty string from itemRef's unaliased branch, which compiles and turns the non-empty assertion red.