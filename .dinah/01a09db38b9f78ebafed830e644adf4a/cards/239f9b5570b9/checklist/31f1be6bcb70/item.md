---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:53Z
ordinal: 31
note: "Two facts force this. The reference cell of the containment table carries the tree's drawing prefix, because withGuides at cmd/dinah/table.go:173 folds the prefix into the row's first field before the table is laid, so the cell reads \"`-- pb-1/attachments/1\" rather than the reference; guidedLead at cmd/dinah/output_check_test.go:286 already strips it from the same glyph constants the renderer draws with, so no new reader is written, and AC-11 requires the case to fail a row guidedLead reports no guide on rather than reading the whole cell. And the Entity cell cannot discriminate: verb.FieldColumn is \"column\" and bench.KindColumn is \"column\", and verb.DefaultChain groups on column first, so `dinah tree` prints \"column\" beside a grouping row and `contents` prints it beside a column row. The pairing walks the --json payload's Root.Children in pre-order against the drawn rows, and what makes that sound is one command's own path through the cli head rather than any cross-head declaration: runTree at cmd/dinah/commands.go:721 and runContents at :733 each take one *verb.Tree from one library call and hand that same value either to emitMachine or to renderTree, and treeRows at render.go:359 appends one row per node before recursing. Round 2 cited verb.CrossHeadIdentical here, which is declared at internal/verb/definition.go:596 over query, tree and changes and is a claim about the MCP head rather than about one command's two output modes; `contents` is not a member of it in any case, so the citation covered neither case and is withdrawn. What a grouping row's cell holds was not minted here: column.tree.reference's own English context already declares it as the value the group gathered at, which nothing addresses. The alternative was to exempt the site, and none of the four grounds is honest for a table that draws comments, items, attachments, cards and columns."
---
A grouping row of `dinah tree` is not a row the address rule reaches, and the sweep tells a grouping row from an entity row by pairing the drawn rows against the payload rather than by reading the Entity cell.