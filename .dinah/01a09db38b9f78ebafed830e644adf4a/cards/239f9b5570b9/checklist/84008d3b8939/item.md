---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:50Z
ordinal: 8
note: "Verified on 3b9c51c. Test: cmd/dinah/address_sweep_test.go, TestEveryEntityDrawingSurfaceIsSwept. Command: go test ./cmd/dinah -run TestEveryEntityDrawingSurfaceIsSwept. First plant: an uncalled method renderNotes appended to cmd/dinah/render.go drawing a table through s.columns with no case and no exemption; built clean, and the run went red at address_sweep_test.go:282 reading \"render.go:renderNotes.t#1 draws a table and is neither a case of TestEveryPrintedReferenceResolves nor a declared exemption\", which is the message the criterion predicted. Second plant, for the constructor arm: a third function plantedColumn() []tableColumn appended to the same file; built clean, and the run went red at address_sweep_test.go:284 naming it, \"the head declares [columns listColumn plantedColumn] returning []tableColumn, wanted [columns listColumn]\". Both restored byte-identically (cmp clean) and green again. The sweep covers 20 cases over 18 sites and declares 16 exemptions, which together account for all 34 sites renderSitesInSource returns."
---
No table this head draws can reach a green build without being swept or argued. In cmd/dinah/address_sweep_test.go, TestEveryEntityDrawingSurfaceIsSwept reads the roster from renderSitesInSource, the walk cmd/dinah/output_check_test.go already declares and TestEveryTableSiteIsRegistered already enforces, and requires every site it returns to be the site of a case of TestEveryPrintedReferenceResolves or an entry in the declared exemption list. It fails a site in neither, an exemption naming a site the walk does not find, and an exemption whose ground is outside the four declared constants. It carries one further assertion: that cmd/dinah declares exactly two functions returning []tableColumn, namely (*session).columns and listColumn, since renderSitesInSource reads a columns field only where it calls one of those two and reports any other shape as unresolvable. Arm it by adding an uncalled method to render.go that builds a table drawing an entity through s.columns with no case and no exemption, which compiles because an unused method is legal Go, and turns the test red naming the new site.