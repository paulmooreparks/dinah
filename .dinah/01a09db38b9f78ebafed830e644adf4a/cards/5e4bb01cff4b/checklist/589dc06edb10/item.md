---
kind: decision
state: resolved
ts: 2026-09-14T02:17:01Z
ordinal: 22
note: An LLM-graded check is non-deterministic and costs money and latency per call; a repository guard that can fail on a retry with no code change is a worse version of the "green on your machine is not green" problem, and running it over 631 keys x up to 8 catalogs on every push repeats the unbounded-sweep cost mistake the column instructions warn against by name. The judgment it would make is one dinah-249's own workflow already asks for whenever a translation's source goes stale; this decision names and records that pass rather than adding a second one, bounding it to the locale lines a single card's own diff touches.
---
The semantic (LLM) layer does not run as a go test and is never part of go test ./... or CI; it is carried by a per-card decision-checklist record that Agent Code Review checks for on any card touching a locale file.