---
title: The quick start never sends a reader to the guide written for them
column: 5ea2db0272fc
state: ready
severity: minor
priority: soon
---
The quick start replays the new topic listing correctly, so a reader sees `first-session` in the table, and the prose around that table never mentions it. Twelve lines further down the document sends a new reader to the layout guide instead, and the section on pointing an agent at a workbench, which is where `dinah mcp` is introduced, says nothing about the guide written for exactly that reader. dinah-164 updated the two replayed blocks because the tool's output moved under them and named the editorial question as deliberately out of scope, which was right: where a document sends its reader is a decision rather than a consequence. Somebody has to make it, and this card is where. Raised by the Test station on dinah-164 and resolved there as another card's question.
