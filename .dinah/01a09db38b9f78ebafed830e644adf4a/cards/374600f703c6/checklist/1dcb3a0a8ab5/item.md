---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 13
note: "The set was produced by the spec's own command over the tree, `grep -rInE '[A-Za-z0-9_/.-]+\\.(go|md):[0-9]+'` over *_test.go, *.txt, *.json and *.golden excluding editors/ and node_modules, and it returns the same five files: cmd/dinah/testdata/prose-figures.txt, cmd/dinah/testdata/guide-blocks.txt, internal/rename/sweep_test.go, cmd/dinah/references_guide_test.go and cmd/dinah/fields_guide_test.go. The last three are not ledgers, for the reasons the spec gives.\n\nEvery anchored entry of all four ledgers was opened and read rather than checked by arithmetic.\n\nprose-figures.txt: 28 entries keyed on a guide's line, and this diff edits no guide, so none moved. Its two Go-keyed entries were opened. internal/mcp/tools.go:308 still stands at the doc comment declaring injectedProperties, and that file is untouched by this diff. internal/verb/query.go:72 still stands at the doc comment declaring actPlane; query.go IS touched by this diff, and its first changed hunk is at line 126, below the anchor, which is why the anchor did not move. The spec expected neither to move and neither did.\n\nguide-blocks.txt: 21 entries keyed on a guide's file and the line its fence opens on. No guide is edited by this diff. docs/quick-start.md is edited (937 to 938 in eleven places) but is not a guide and carries no entry here, and the edit changes no line count in any case.\n\nThe two dinah-234 rekeyed ledgers: cmd/dinah/testdata/uncovered.txt names blocks of the rendering head as file:function#ordinal, and cmd/dinah/row_sweep_test.go's call-site registry names a place by its function and rank within it. This diff touches no cmd/dinah production file at all, only twelve cmd/dinah test files, so neither could drift; both were read and both point at what they declare.\n\ngo test ./... was run over the whole tree, twice on the branch. internal/profile passed in both, and it reads the whole tree and is named by no part of this diff. Whole-tree verdict: 18 packages, none failing, 2557 tests. The first of those two runs found two real ledger regressions, recorded under AC-13; this criterion's own four ledgers were not among them, and the two that did move (the internal/addressform resolver roster and the quick-start catalogue counts) are derived-figure ledgers rather than position-keyed ones, so they sit outside the set this criterion enumerates. That is a gap in the criterion's own command worth naming: a ledger recording a count about the code is not matched by a pattern looking for file:line.\n\nThe pull request's checks on every platform are read in WHAT SHIPPED rather than here."
---
The position-keyed ledgers are named, recomputed rather than reconciled, and the whole suite is run rather than the packages the diff names. The set was produced by a command over the tree rather than by memory: grep -rInE '[A-Za-z0-9_/.-]+\.(go|md):[0-9]+' over *_test.go, *.txt, *.json and *.golden, excluding editors/ and node_modules, returns five files. Two are ledgers: cmd/dinah/testdata/prose-figures.txt, whose 28 entries key on a document line and two of which also key on a Go source line (internal/mcp/tools.go:308 and internal/verb/query.go:72), and cmd/dinah/testdata/guide-blocks.txt, whose 21 entries key on a guide's file and the line its fence opens on. The other three matches are not ledgers: internal/rename/sweep_test.go uses synthetic in-test paths of the form a.go:3, and cmd/dinah/references_guide_test.go and cmd/dinah/fields_guide_test.go mention line numbers in prose comments. The two ledgers dinah-234 rekeyed, the call-site registry in cmd/dinah/row_sweep_test.go and the uncovered-statement allowlist in cmd/dinah/testdata/uncovered.txt, name a place by its function and rank within that function, so they drift when a function changes rather than when a line moves above it. The implementer opens every anchored entry in all four and reads what it now points at rather than checking the arithmetic, and records the result. Neither internal/mcp/tools.go nor internal/verb/query.go carries a ListIDs call, so this card is not expected to move either Go-keyed entry, and an entry that has moved anyway is a finding to report rather than a number to adjust. go test ./... is run over the whole tree including internal/profile, which no diff names and which reads the whole tree, and the pull request's checks are green on every platform rather than on one local run.