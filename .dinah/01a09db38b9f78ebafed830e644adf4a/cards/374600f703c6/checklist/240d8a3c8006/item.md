---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:34Z
ordinal: 2
note: "TestEveryCollectionListingReadsThroughTheOneReader in internal/bench/storage_readfailure_test.go. Resolve with: go test ./internal/bench/ -run TestEveryCollectionListingReadsThroughTheOneReader -v. It names the seven readers rather than counting them (assertion at storage_readfailure_test.go:159), refuses any other function classifying os.ReadDir's error (line 165), logs 445 functions examined and fails on zero, and pins readCollection at exactly one os.ReadDir and one os.Stat so a build deleting the classification everywhere fails. Armed 2026-09-11 by restoring listIdentifiers' os.IsNotExist branch on os.ReadDir's own error: red naming vocabulary.go and listIdentifiers, and the reader set short by that name; green again after a byte-identical restore."
---
readCollection is the only place in the shipped binary that decides whether a directory-read failure means absence, and ListIDs, ListWorkbenchIDs, listIdentifiers, heldLocks, resumableLift, interruptions and checkAttachmentFilename all read through it. Test: TestEveryCollectionListingReadsThroughTheOneReader in internal/bench/storage_readfailure_test.go, an AST sweep over the non-test files of internal/bench. It asserts that the functions calling readCollection are exactly ListIDs, ListWorkbenchIDs, listIdentifiers, heldLocks, resumableLift, interruptions and checkAttachmentFilename, naming them rather than counting them, so a function added to that set and a function dropped from it both redden. The set is seven rather than three because sections 3, 4, 5 and 6 route heldLocks' root read, resumableLift, interruptions and checkAttachmentFilename's payload read through the shared reader alongside the three collection listings. It further asserts that no function other than readCollection applies os.IsNotExist or errors.Is(..., fs.ErrNotExist) to a value produced by os.ReadDir, and logs the number of functions examined, failing when that number is zero. The succeeding case is pinned in the same run: readCollection itself is asserted to make exactly one os.ReadDir call and exactly one os.Stat call, so a build that simply deleted the classification everywhere fails. Plant that compiles and runs: restore listIdentifiers' os.IsNotExist(err) branch on os.ReadDir's own error; the test reddens naming vocabulary.go and the function.