---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 9
note: "TestTheNumberAndOrdinalMintersRefuseACollectionTheyCannotRead in internal/bench/ordinal_readfailure_test.go, four subtests. Resolve with: go test ./internal/bench/ -run TestTheNumberAndOrdinalMintersRefuse -v. NextNumber is (int, error) at bench.go:2088 and nextOrdinal is (int, error) at ordinal.go:67. Three unreadable fixtures, each proven by plantUnreadable's own os.ReadDir: the live cards collection, the archived one with the live half readable, and a comments collection. Succeeding case in the same run: against a live card numbered 7 and an archived card numbered 9, NextNumber answers 10, which also proves it still reads the archived half; against two stamped comments, nextOrdinal answers 3. Armed 2026-09-11 twice. The spec's plant, nextOrdinal discarding its error: red at ordinal_readfailure_test.go:63 with \"nextOrdinal answered 1 for a collection it could not list\". A second plant, NextNumber discarding its error: red at ordinal_readfailure_test.go:52 with \"NextNumber answered 2 for an archived collection it could not list\", which is the damage class exactly, a new card stamped with a number the archived card already carries. Green after each byte-identical restore."
---
The two sites that authorise a write refuse rather than answering one, and NextNumber refuses on the archived half as well as the live one. NextNumber has the signature (int, error) and nextOrdinal has the signature (int, error), and both return their error. Test: TestTheNumberAndOrdinalMintersRefuseACollectionTheyCannotRead in internal/bench/ordinal_readfailure_test.go. Three unreadable fixtures, each proven with a direct os.ReadDir first: the live cards collection, the archived cards collection with the live one readable, and a comments collection. The archived case is the one that matters, because it is the only route that reaches a write: internal/verb/beyond.go:67 follows NextNumber with bench.ClaimID against b.CardsRoot() alone, so an unreadable live collection aborts the filing on ClaimID's own os.MkdirAll while an unreadable archive aborts nothing. Each of the three asserts its function returns a non-nil error rather than 1. The succeeding case is pinned in the same run: against a readable cards collection already holding a card numbered 7 and a readable archive holding a card numbered 9, NextNumber answers (10, nil), which also proves it still reads the archived half at all; against a readable comments collection already holding two stamped members, nextOrdinal answers (3, nil). A build returning an error unconditionally fails both. Plant that compiles and runs: restore nextOrdinal to discard its error and return highest+1; the test reddens with "nextOrdinal answered 1 for a collection it could not list".