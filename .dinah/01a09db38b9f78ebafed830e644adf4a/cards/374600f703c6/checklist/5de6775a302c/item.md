---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 12
note: "Performed as narrowed, on head 25b5f60, in a throwaway workbench at C:/dinah-scratch/dinah-439-impl2/ac14 with DINAH_HOME=C:/dinah-scratch/dinah-439-impl2/dinah-home. The operator's ruling narrowing this criterion and resolving OQ-1 stands on the card as OQ-1's resolution note; it is not restated here.\n\nAbsent half, against card ac14-1 whose comments directory does not exist: `dinah path ac14-1/comments` exit 0, printing the absolute path; `dinah show ac14-1/comments` exit 0, printing \"ac14-1/comments holds nothing.\"; `dinah contents ac14-1/comments` exit 0, printing \"ac14-1/comments contains nothing.\".\n\nPlanted half, same session, with a plain file written at that comments path: `dinah contents ac14-1/comments` exit 4, reporting \"unreachable readdir <path>: The system cannot find the path specified.\", which is the criterion's requirement. Recorded rather than required, and unchanged as the ruling expects: `dinah path` exit 0 printing the same composed path, having touched no disk, and `dinah show` exit 0 printing the planted bytes.\n\nNone of the three repeats a reassuring sentence in the planted case, which is the class this card removes. Citation, resolvable in one command: `go test ./internal/bench/ -run TestReadCollection -count=1`, whose assertions in internal/bench/storage_readfailure_test.go are what make `contents` report rather than deny; the six CLI invocations above are the terminal-level companion and are reproducible from this note."
---
The absent-means-empty rule still holds end to end at a terminal, which is the behaviour dinah-455 shipped and this card must not regress. In a throwaway workbench under the scratch directory with DINAH_HOME pointed at a scratch path, against a card that has never carried a comment and whose comments directory does not exist on disk: `dinah path <card>/comments` exits 0 and prints the absolute path, `dinah show <card>/comments` exits 0 and prints the holds-nothing sentence, and `dinah contents <card>/comments` exits 0 and prints the contains-nothing sentence. The failing companion runs in the same session: with a plain file planted at that comments path, `dinah contents <card>/comments` exits non-zero and reports the unreachable read rather than repeating the reassuring sentence. Record all four invocations with their exit codes, and record what `dinah path` and `dinah show` do in the planted case as well, because neither reads a directory and neither is expected to change: `path` composes a path string without touching the disk, and `show` prints the planted bytes, which is a file standing where a collection belongs rather than a failure presented as a confident negative. Both halves are needed, because the passing half alone would be satisfied by code that never reports anything and the failing half alone by code that reports failure for everything.