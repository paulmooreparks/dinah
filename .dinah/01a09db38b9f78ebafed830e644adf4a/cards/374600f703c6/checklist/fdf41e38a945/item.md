---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 14
note: "TestCheckReportsADuplicateCardNumber in internal/bench/check_readfailure_test.go, five subtests. Resolve with: go test ./internal/bench/ -run TestCheckReportsADuplicateCardNumber -v. Case 1 asserts exactly two findings of key FindingCardNumberDuplicate with Detail \"7\", one naming the live anchor and one the archived anchor, and asserts each path was seen (check_readfailure_test.go:132), which is how it proves both halves were read. Case 4 builds the planted anchor path from the CardAnchor constant and takes its expected key from unreadableCardFinding applied to the error LoadCard actually returns for that fixture, then asserts Path is the card's DIRECTORY. Case 5 asserts the live half is named by exactly one finding. Armed 2026-09-11 with all three prescribed plants, each confirmed to build first: len(paths) > 2 gives \"two cards carry number 7 and dinah check reported 0 findings: []\"; the unconditional continue on LoadCard's error gives \"the archived card whose anchor will not load drew 0 findings of key check.card-vocabulary-retired: []\"; appending CardAnchor to the unreadable finding's path gives the two spellings side by side at check_readfailure_test.go:202. Green again after each byte-identical restore."
---
dinah check reports a card number two cards share, across both halves of the collection, reports an archived card whose anchor it could not load, and reports nothing on a workbench where every number is unique. Test: TestCheckReportsADuplicateCardNumber in internal/bench/check_readfailure_test.go. It builds a workbench whose live half holds a card numbered 7 and whose archived half holds a different card also numbered 7, calls Bench.Check, and asserts that the findings carry exactly two entries with Key == FindingCardNumberDuplicate and Detail == "7", one naming the live card's anchor path and one naming the archived card's, because reporting only the second sighting would name the archived card alone and that is the innocent one. That pair of paths is also how the test proves both halves were read. checkCardNumbers returns ([]Finding, error) and nothing else, so there is no examined count for a test to assert, and a sweep that read only the live half could not produce a finding naming an archived anchor; asserting both paths appear is therefore the count, and a half that read nothing fails the assertion rather than passing quietly. The succeeding case is pinned in the same run and it is what stops a build reporting duplicates everywhere: the same workbench with the archived card renumbered to 8 returns no finding of that key at all. A third case asserts the sweep's own read failure is not swallowed: with a plain file planted at the archived cards collection, proven unreadable with a direct os.ReadDir first, Check returns a non-nil error rather than a clean bill. A fourth case asserts the detector does not go quiet on the half that makes the damage reachable, which is the defect the detector itself exists to report wearing the detector's own uniform. With the archived half holding one card whose anchor is absent or will not load, the fixture building that anchor path from the CardAnchor constant rather than from a literal, Check returns a finding whose Key is what unreadableCardFinding gives for the error and whose Path is that card's DIRECTORY rather than its anchor file. The test asserts the directory spelling specifically, because the live walk writes Path: dir at check.go:268 and check.go:273 and one key must not print two spellings depending on which half found the card; a finding naming the anchor file fails this case even though its key is right. It returns without a non-nil error, because one unloadable card leaves the sweep able to finish while a collection nobody can list does not. The duplicate finding is unaffected and keeps the anchor path, which the same test asserts in its first case. The fourth case also asserts the live half is NOT double-reported: with an unloadable card in the live half instead, exactly one finding names it, which is Bench.Check's own card walk reporting it, and checkCardNumbers adds no second. Plant that compiles and runs: change checkCardNumbers to report only when len(paths) > 2; the test reddens with "two cards carry number 7 and dinah check reported nothing". A second plant for the fourth case: restore the unconditional continue on LoadCard's error; the test reddens with the archived card's directory unreported. A third plant, which is what arms the path-spelling assertion: append CardAnchor to the unreadable finding's path; the test reddens naming the two spellings.