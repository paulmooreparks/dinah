---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 8
note: "TestCheckReportsACollectionItCouldNotRead in internal/bench/check_readfailure_test.go, three subtests. Resolve with: go test ./internal/bench/ -run TestCheckReportsACollectionItCouldNotRead -v. The partial slice is asserted at the library boundary only (check_readfailure_test.go:85); neither caller of Check is touched and no exit code is asserted, so dinah-462's question stays dinah-462's. FINDING on the spec's arming recipe: the prescribed plant, discarding the error at the live cards read with a blank identifier, COMPILES AND LEAVES THE GUARD GREEN, because Check reads the live cards collection at three further places (checkAttachmentsWithoutAMount's workbench-mount walk, checkCardNumbers, and interruptions' own readCollection) and any of them still reports. The substituted plant breaks the behaviour at its source, ListIDs answering (nil, nil) on a read failure: red at check_readfailure_test.go:82 with \"Check returned no error for a workbench whose archived cards collection it could not read, reporting [{...workstreams/f00000000001 Key:check.missing-anchor...}]\", which also shows the gathered finding surviving; green after a byte-identical restore."
---
Bench.Check returns a non-nil error, together with the findings it had already gathered, when any collection in its walk cannot be read. Test: TestCheckReportsACollectionItCouldNotRead in internal/bench/check_readfailure_test.go. It builds a workbench carrying exactly one genuine defect that Check reports, plants a plain file where one collection belongs, proves that collection reads as a failure, then asserts both that Check returns a non-nil error and that the returned findings slice still carries that one genuine finding. The partial slice is asserted at the library boundary and nowhere else, because both callers of Check, internal/verb/read.go:1452 and internal/verb/read.go:1464, discard the findings on a non-nil error today and this card does not change them; what a reader is told about a partial report is dinah-462's to decide. The succeeding case is pinned in the same run: the same workbench with every collection readable returns the same one finding and a nil error. No exit code is asserted. Plant that compiles and runs: discard the error at check.go:258 with a blank identifier; the test reddens with "Check returned no error for a workbench whose cards collection it could not read".