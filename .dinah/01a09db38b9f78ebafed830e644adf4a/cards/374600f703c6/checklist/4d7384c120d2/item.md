---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:34Z
ordinal: 4
note: "TestTheContainerMigrationRefusesALockStateItCannotRead in internal/bench/container_readfailure_test.go. Resolve with: go test ./internal/bench/ -run TestTheContainerMigrationRefusesALockStateItCannotRead -v. All three writers are driven through the memberWalk seam, giving a readable workbench root whose member walk reports a failure, and each compares a recursive path listing taken before and after BEFORE it asks about the error, so a build that proceeded is caught by what it did to the disk. finishContained additionally compares the anchor text. Succeeding case in the same run: with the seam at its default the three still remint, lift and stamp a healthy workbench. Armed 2026-09-11 by dropping remintInPlace's err check so it proceeds with an empty held slice: red at container_readfailure_test.go:156 with \"the workbench directory was renamed while its lock state was unreadable: path 2 reads \\\".dinah/01a08ed4cd6f71adb7d383239663f007\\\" and read \\\".dinah/0199a1b2c3d47abc8000000000000001\\\" before\", which is the listing assertion rather than the missing error; green after a byte-identical restore."
---
The container migration refuses rather than renaming when it cannot read the lock state, at all three heldLocks callers, and it is proven against the failure shape that actually occurs in the field. Test: TestTheContainerMigrationRefusesALockStateItCannotRead in internal/bench/container_readfailure_test.go. For each of remintInPlace, liftIntoContainer and finishContained it asserts the call returns a non-nil error and that the filesystem is unchanged, comparing a recursive path listing taken before and after, so the criterion tests that nothing was renamed rather than only that an error appeared. At least one of the three is driven through the memberWalk seam from AC-3, giving a readable workbench root whose member walk reports a failure, because a workbench root that is itself a plain file is not a workbench and never reaches these callers in the field, while a member subtree that exists and will not read is the trigger this card is named for. The succeeding case is pinned in the same run: the same three calls against a healthy unlocked workbench, with memberWalk left at its default, still succeed and still perform their rename or stamp, and the listing afterwards shows the expected new layout. Plant that compiles and runs: drop the err check in remintInPlace so it proceeds with an empty held slice; the test reddens on the before-and-after listing with "the workbench directory was renamed while its lock state was unreadable" rather than merely on a missing error.