---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 7
note: "TestTheInterruptionSweepReportsACollectionItCannotRead in internal/bench/finish_readfailure_test.go. Resolve with: go test ./internal/bench/ -run TestTheInterruptionSweepReportsACollectionItCannotRead -v. The succeeding half asserts exactly one interruption (finish_readfailure_test.go:37) rather than non-emptiness, so a build reporting an error for every collection and one reporting none both fail. FINDING on the spec's arming recipe: the prescribed plant, restoring `if err != nil { continue }` at finish.go:60 alone, COMPILES AND LEAVES THE GUARD GREEN, because siblingCollections lists every collection it appends, so once ListIDs reports its own failure that outer read fires first and the sweep still refuses. The substituted plant restores the whole pre-card behaviour on that path (the sweep's own read swallowing again, plus siblingCollections and collectionsBelow discarding ListIDs' error), builds (confirmed go build exit 0 before the run), and reddens at finish_readfailure_test.go:57 with \"the sweep reported 1 interruptions and no error while one collection was unreadable\" while the readable-collection half still passed; green after a byte-identical restore."
---
The interrupted-act sweep reports a collection it cannot read rather than sweeping past it. Test: TestTheInterruptionSweepReportsACollectionItCannotRead in internal/bench/finish_readfailure_test.go. It plants a workbench with one genuinely standing sibling in a readable collection and a plain file where a second sibling collection belongs, proves the second reads as a failure, then asserts that the sweep returns a non-nil error. The succeeding case is pinned in the same run and it is the sharper half: the same workbench with both collections readable still reports exactly one interruption, and the test asserts that count rather than asserting non-emptiness, so a build that reported an error for every collection and a build that reported no interruptions both fail. Plant that compiles and runs: restore if err != nil { continue } at finish.go:60; the test reddens with "the sweep reported 1 interruption and no error while one collection was unreadable".