---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 6
note: "TestTheVocabularyMigrationRefusesADirectoryItCannotList in internal/bench/vocabulary_readfailure_test.go, two subtests. Resolve with: go test ./internal/bench/ -run TestTheVocabularyMigrationRefusesADirectoryItCannotList -v. The fixture builds its path from the PreVocabularyDir constant (vocabulary.go:16), not from a literal, so a rename of the constant is a compile error. The assertion that catches the rename is vocabulary_readfailure_test.go:32-40, the state of the disk, and it runs before the returned error is asked about. Succeeding case in the same run: a genuine pre-vocabulary tree still migrates, asserted per member with a count of 2 checked and a failure on zero. Armed 2026-09-11 by restoring listIdentifiers' os.IsNotExist(err) branch on os.ReadDir's own error: red at vocabulary_readfailure_test.go:32 with \"a plain file named states no longer stands at ...\\states\", which is the file having been renamed to columns; green after a byte-identical restore."
---
The vocabulary migration refuses rather than renaming a path it could not list. Test: TestTheVocabularyMigrationRefusesADirectoryItCannotList in internal/bench/vocabulary_readfailure_test.go. It plants a plain file at filepath.Join(parent, PreVocabularyDir), building the path from the constant declared at internal/bench/vocabulary.go:16 rather than from a literal, whose value is "states"; the string "flow" appears nowhere in this tree and a fixture of that name would exercise nothing, which is how this criterion passed against unfixed code in round one. It proves os.ReadDir on the planted path returns a non-nil error, then calls migrateColumnDirectories and asserts both that it returns a non-nil error and that a file still stands at PreVocabularyDir with nothing created at ColumnsDir, which is the assertion that catches the rename rather than the return value. The succeeding case is pinned in the same run: a genuine pre-vocabulary tree still migrates, the directory named by PreVocabularyDir becomes the one named by ColumnsDir and each member's PreVocabularyAnchor becomes ColumnAnchor, asserted per member with a count of how many members were checked and a failure when that count is zero. Plant that compiles and runs: restore listIdentifiers' if os.IsNotExist(err) { return nil, nil } on os.ReadDir's own error; the test reddens with "a plain file named states was renamed to columns".