---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:34Z
ordinal: 3
note: "TestHeldLocksReportsARootAndAMemberItCannotRead in internal/bench/container_readfailure_test.go, four subtests. Resolve with: go test ./internal/bench/ -run TestHeldLocksReportsARootAndAMemberItCannotRead -v. The root read is exercised by a plain file at the workbench root, proven unreadable first by plantUnreadable (storage_readfailure_test.go:29). The member walk is reached through the memberWalk package variable added beside containerRename at container.go:114; the substitution counts its calls and the test fails with \"the substituted walk was never called\" at container_readfailure_test.go:99 when it is zero. Succeeding cases pinned in the same run: a healthy workbench answers (nil, nil), and one carrying a lock answers that lock with no error. Armed 2026-09-11 by restoring the callback's `if err != nil || entry.IsDir() { return nil }`: red only on the seam half at container_readfailure_test.go:102 with \"heldLocks reported no error for a member directory it could not walk\" while the root subtest still passed, which is what proves the two reads are separately covered; green after a byte-identical restore."
---
heldLocks has the signature func heldLocks(root string) ([]string, error) and both of its reads report failure. Test: TestHeldLocksReportsARootAndAMemberItCannotRead in internal/bench/container_readfailure_test.go. Two fixtures reached two different ways, because one way cannot reach both sites. The root read at container.go:693 is exercised by a workbench root replaced by a plain file, proven first with a direct os.ReadDir returning a non-nil error. The member walk at container.go:706 is exercised through the memberWalk package variable added beside containerRename at container.go:111: the test substitutes a walk that invokes the callback once with a non-nil error and asserts it was invoked, failing with "the substituted walk was never called, so this half of the test proves nothing" if the call count is zero. A fixture on disk cannot reach that site, because filepath.WalkDir over a path holding a plain file calls its callback once with a nil error, measured on 2026-09-11 as calls=1 sawErr=<nil> walkErr=<nil>. Both halves assert heldLocks returns a non-nil error. The succeeding case is pinned in the same run: with memberWalk left at its default, a healthy workbench with no lock files answers (nil, nil), and a healthy workbench carrying one lock answers that lock with a nil error, so a build that errors on everything fails both. Plant that compiles and runs: restore the callback's if err != nil { return nil }; the test reddens on the seam half with "heldLocks reported no error for a member directory it could not walk" while the root fixture still passes, which is what proves the two reads are separately covered.