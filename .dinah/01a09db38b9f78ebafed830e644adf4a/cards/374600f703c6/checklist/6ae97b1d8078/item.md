---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 15
note: "Verified and armed on head 25b5f60 in C:/dinah-scratch/dinah-439-impl2/wt. `go test ./internal/msg/ -run TestTheCardNumberKeyReachesEveryCatalogue -count=1 -v` passes and logs \"8 catalogue files enumerated, 8 entries read\", so the population is asserted rather than assumed.\n\nArming plant, performed as the criterion prescribes: the `check.card-number-duplicate` entry was deleted from internal/msg/locales/hi.json. The run went red at internal/msg/card_number_key_test.go:51 with \"hi carries no entry for check.card-number-duplicate\", and at :86 and :88 with \"8 catalogue files enumerated, 7 entries read\" and \"the sweep read 7 entries and one key across eight catalogues is eight\". The first plant attempt edited the top level of hi.json rather than its `entries` object, so it changed nothing and the run passed; that pass was read as a failed plant rather than as a green result, and the plant was redone against `entries`. hi.json was then restored from a copy taken before the plant and confirmed byte-identical with filecmp, the package re-run green, and `git status --short` is empty.\n\nCitation: internal/msg/card_number_key_test.go:51 is the assertion that reddens when a catalogue is missing the key, and :88 is the assertion that reddens when the sweep reads fewer entries than there are catalogues."
---
The one minted key reaches every catalogue. Test: TestTheCardNumberKeyReachesEveryCatalogue in internal/msg/card_number_key_test.go, following TestTheCollectionKeysReachEveryCatalogue at internal/msg/collection_keys_test.go:19 in every structural respect. It enumerates locales/*.json by glob rather than listing the languages, so a ninth catalogue arriving fails the test instead of being missed, and it asserts the file count is 8 and the subject set is exactly one key, check.card-number-duplicate. For each catalogue it asserts the key is present and that its text is non-empty. For af, cs, es, fil and id it asserts the entry is marked skeleton and carries the English text verbatim, which is what those five do for every one of the 937 keys they ship today. For de and hi it asserts the entry differs from the English and carries a source equal to msg.Fingerprint of the English text, which is the arrangement check.ordinal-duplicate already has in both and which TestATranslationIsNotEnglishUnderAnotherTag and TestATranslationTracksItsEnglishSource hold every translated entry to. It asserts the number of entries it examined is 8, so a catalogue silently skipped reports a number rather than passing. Plant that compiles and runs: drop the entry from hi.json; the test reddens naming hi and the key.