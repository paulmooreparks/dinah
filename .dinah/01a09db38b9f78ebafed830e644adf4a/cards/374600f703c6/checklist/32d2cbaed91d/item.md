---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:36Z
ordinal: 24
note: "Round one's AC-6 planted a file called \"flow\" where the code spells PreVocabularyDir = \"states\" (internal/bench/vocabulary.go:16). The migration skipped the fixture, renamed nothing, and every assertion in the criterion passed against the unfixed code, on the more destructive of the card's two writing sites. A grep over the tree confirms \"flow\" appears in no Go source here at all.\n\nThat defect is invisible by construction, because a fixture naming the wrong thing reads exactly like one naming the right thing, so round two audited the other fourteen criteria for it rather than assuming it was isolated. The names each criterion plants or asserts were checked against the tree at 65a80ad: ColumnsDir = \"columns\" and ColumnAnchor = \"column.md\" (bench.go:56, bench.go:18), PreVocabularyAnchor = \"state.md\" (vocabulary.go:17), LockName and SiblingSuffix as heldLocks reads them (container.go:691), the memberPaths member set (container.go:93), FindingOrdinalDuplicate and its three sibling keys in the constant block at check.go:26, TestNoLockIsCreatedOutsideTheOneAcquirer at guards_test.go:57 and TestGuardCatchesAHandRolledRow at guards_test.go:2656, the two Bench.Check callers at read.go:1452 and read.go:1464, and the two line-keyed ledgers AC-15 had gestured at without naming. AC-6 was the only wrong name. AC-15's unnamed \"third remaining line-keyed fixture\" was the other defect of the same family, a gesture rather than a name, and it is now named by a command over the tree.\n\nThe rule that prevents a recurrence is in the spec's section 11: the implementer builds the path from the constant, so a rename of the constant is a compile error rather than a silent pass, and the criterion cites the constant so a reviewer can check the name against a declaration rather than against their memory of one."
---
Every fixture on this card builds its path from the declared constant rather than from a literal spelling, and each criterion names the constant it plants. All fifteen original criteria were audited for the wrong-name defect in round two, and AC-6 was the only instance.