---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:36Z
ordinal: 17
note: "Patching callers is how this family survived the nine earlier sightings enumerated at the top of the spec, and each of those repairs looked sufficient at the time. Two alternatives were weighed. A second checked function beside ListIDs leaves the unchecked name in place for the next writer to reach for, which is the same shape under a new name. A wrapper type whose zero value is not an empty collection buys nothing two return values do not, since Go cannot force a method call any more than it forces an error check. The cost is 82 edits, of which 47 are the two-line mechanical test edit dinah-433 already performed 26 times without behavioural drift. The 35 production edits are not two-line edits: every one of them is an expression-context call that needs the call hoisted to a temporary first, which the spec's section 2 now states rather than describing eleven of them as the hard subset."
---
The fix is at the helper, not at the callers. ListIDs changes to ([]string, error) and heldLocks to ([]string, error), so all 82 ListIDs call sites and all three heldLocks call sites meet the question at compile time.