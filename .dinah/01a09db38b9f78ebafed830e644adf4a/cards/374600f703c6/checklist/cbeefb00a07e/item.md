---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 11
note: "`go test -json -count=1 ./...` run once at the merge base 65a80ad in the worktree C:/dinah-scratch/dinah-439-impl/base and once on the merged branch in C:/dinah-scratch/dinah-439-impl/wt, and the two verdict sets diffed programmatically rather than by eye (the comparator groups every pass/fail/skip event by package and by package+test).\n\nTests that ran before: 2519. Tests that ran after: 2557. Tests removed: 0. Common tests that changed verdict: 0. Packages: 18 before and 18 after, none failing after.\n\nTests added: 38, and every one of them is this card's own. They are the 38 subtests and parents of TestListIDsSeparatesAnAbsentCollectionFromAnUnreadableOne, TestEveryCollectionListingReadsThroughTheOneReader, TestHeldLocksReportsARootAndAMemberItCannotRead (4 subtests), TestTheContainerMigrationRefusesALockStateItCannotRead (4), TestResumableLiftReportsAContainerItCannotRead (3), TestTheVocabularyMigrationRefusesADirectoryItCannotList (2), TestTheInterruptionSweepReportsACollectionItCannotRead (2), TestCheckReportsACollectionItCouldNotRead (3), TestCheckReportsADuplicateCardNumber (5), TestTheNumberAndOrdinalMintersRefuseACollectionTheyCannotRead (4), and TestTheCardNumberKeyReachesEveryCatalogue.\n\nThe first branch run was NOT clean, and what it caught is worth the record: three cmd/dinah tests went pass to fail. TestEveryDeclaredResolverCarriesTheArmsItDeclares and TestTheQuickStartMatchesTheTool were real, and TestEveryStatementOfTheRenderingHeadIsCoveredOrNamed was a cascade of them, since its coverage run re-runs the suite. Both real failures were derived-figure ledgers this diff moved: the resolver roster in internal/addressform/addressform.go declares a return-arm count per resolver, and nine of them gained an arm, and docs/quick-start.md records the catalogue totals, which went 937 to 938 with the minted key. Both were corrected from what the guards themselves recomputed rather than by hand arithmetic, and the run above is the re-run after that correction."
---
The mechanical sweep changed no test's behaviour. go test -json ./... is run once at the merge base before any edit and once on the merged branch, the per-package and per-test verdicts are diffed programmatically rather than by eye, and the comparison is recorded in the pull request with three counts: how many tests ran before, how many ran after, and how many common tests changed verdict. Zero tests may be removed and zero common tests may change verdict. The only permitted difference is the set of tests this card's own criteria add, and each added test is named in the record. This is dinah-433's AC-9 applied to a sweep three times the size, and a sweep across this many files is where a real behavioural change hides.