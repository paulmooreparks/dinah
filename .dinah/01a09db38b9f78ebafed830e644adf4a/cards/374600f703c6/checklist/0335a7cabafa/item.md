---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:35Z
ordinal: 10
note: "Recomputed on 2026-09-11 against the merge base 65a80ad, by two independent methods that agree.\n\nMethod A, driven by the type checker rather than by a probe parameter, which is the fifth method the spec's section 2 already records as producing the same rounds: ListIDs was widened to ([]string, error) and `go test -gcflags=all=-e -run NONE ./...` run to exhaustion, patching between rounds. Round 1 reported 31 sites, all internal/bench production, and they are exactly the 31 rows of the spec's table for that package. Round 2 reported 8, four internal/bench test and four internal/verb production (read.go:1174, search.go:168, tree.go:1029, tree.go:1204). Round 3 reported the remaining 43 test sites across cmd/dinah and internal/verb. Round 4 reported none. Total 82, split 35 production and 47 test.\n\nMethod D, the syntax tree, run over the merge-base worktree at C:/dinah-scratch/dinah-439-impl/base with a go/parser program that counts each CallExpr whose callee names ListIDs and classifies the argument-position subset by the parent node rather than by a regular expression: 268 files parsed, 35 production, 47 test, 82 total, 11 production calls in an argument position. The eleven are entity.go:70, entity.go:100, entity.go:133, entity.go:835, entity.go:920, ordinal.go:285, resolve.go:651, workstream.go:171, workstream.go:427, verb/read.go:1174 and verb/tree.go:1029, which is the spec's list exactly: nine passed to SortByOrdinal, the len call, and orderedByJournal.\n\nEvery figure matches the spec and no card added or removed a site. All 35 production sites were hoisted to a temporary with the error checked; none was rewritten by dropping the error, which does not compile in any case. The two nextOrdinal composite-literal sites the spec warned about, workstream.go:348 and workstream.go:371, were both hoisted out of their literals (now NewWorkstream and AdoptWorkstream, each with the hoist explained in a comment), as was refBelowHead's result in resolve.go's EntityRef literal, a third such site the spec did not name.\n\nAfter the sweep: go build ./... clean, go vet ./... clean, gofmt -l . empty."
---
The call-site sweep is complete and its size is recomputed rather than carried forward from this spec. Before the sweep starts, the implementer re-runs method A from the spec's section 2, which is adding a probe parameter to ListIDs and running go test -gcflags=all=-e -run NONE ./... to exhaustion, and records the per-round counts and the total in the pull request. The recorded total is compared against 82, the production against 35 and the test against 47, and the count of production calls sitting in an argument position against 11; any difference is explained by naming the card that added or removed a site rather than silently adopting the new number. Every one of the 35 production sites is an expression-context call rather than a plain assignment, so none of them takes a simpler edit than the rest: the pull request records all 35 as hoisted to a temporary with the error checked, and records by name the eleven argument-position sites and the two nextOrdinal composite-literal sites at workstream.go:348 and workstream.go:371, so a site rewritten by dropping the error rather than checking it is visible in the record rather than hidden behind a green build. Nothing here rests on a site being detectable as "quietly left as a one-value call", because such a site does not compile. After the sweep, go build ./..., go vet ./... and gofmt -l . are clean repository-wide, which is what catches every call site rather than the ones the author remembered.