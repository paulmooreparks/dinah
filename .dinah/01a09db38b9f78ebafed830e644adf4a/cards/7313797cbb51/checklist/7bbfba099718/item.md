---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:25Z
ordinal: 18
note: "Answered at Spec, round 2, after re-deriving the set by call-path tracing and by running rather than by reading the resolver. Corrected in round 3 where it named one function and there are five.\n\nHow I enumerated. I did not read resolveReferenceBody. I asked go/ast which functions in internal/bench take a string and answer an entity type, which gave 46; I then read each candidate and traced who calls it, and I counted return statements per function with a second AST pass. Both passes are quoted in the round-2 evidence comment with their source. Round 3 re-ran the subject-set sweep from a fresh worktree at the same commit and read 443 declarations and the same 46, which is now three independent methods on one number, the reviewer's textual signature scanner being the third. Running confirmed the reachability: `dinah move wb-1 7601a1095bf5`, `dinah move wb-1 Done`, `dinah move wb-1 intake` and `dinah move wb-1 DOING` all exit 0, and `grep` counts 23 direct calls from internal/verb into ColumnByRef, ArchivedColumnByRef and WorkstreamByRef, which reproduces the reviewer's number exactly.\n\nWhat would make my number wrong. The eighteen rostered functions rest on the type-based sweep's subject set, so a resolver answering a type outside the nine I named, a plain string identifier for instance, is invisible to it. Five rostered functions are of that kind rather than the one this note named in round 2: `resolvePathBody`, `walkBelowCard`, `descend` and `pick` answer `(string, error)` and `ResolveLinkTarget` answers `(string, *contract.Refusal)`, so thirteen roster rows stand in the subject set and five do not, and all five are in the roster because I put them there by hand. AC-8 therefore asserts the reverse direction of the exemption roster alone, and section 6.3 is what holds the five, because it fails when a roster row names a function that does not stand in the file the row names. The arm counts rest on a return-statement definition, so an arm widened in place rather than added moves neither count. And the roster covers internal/bench alone; internal/verb/reshape.go carries a second column-naming grammar, which spec section 8 records. Each of those is written into spec section 6.6 rather than left in this note.\n\nThe verdict at the real number. The guard survives and it does not shrink. What it no longer claims is the sentence its worth rested on, that nobody can add an accepting arm without minting a form because the arm builds one of two structs; that was false by eleven arms. The replacement claim is that no address-answering function can appear in internal/bench without being rostered or exempted, and no arm can be added to a rostered one without both of its counts moving. That is stronger than the round-1 claim was true and it costs less, because it needs no production change at all: the Form field on Resolved and EntityRef and the three form-reporting siblings are all dropped."
---
A general guard on the reverse direction is built rather than declined, and it guards the resolver's accepting arms rather than the set of strings a reader might type. Its shape changes: the guard counts arms per declared function and sweeps the package for unrostered resolvers, instead of scanning composite literals of two structs.