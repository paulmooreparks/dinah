---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:26Z
ordinal: 23
note: "Answered at Spec, round 3. Agent Design Review's blocker was that seven subject-set functions fit none of the three grounds and the closed set left the implementer no sanctioned way to make AC-8 pass: `ResolveCard`, `ResolveArchivedCard`, `ResolveReference`, `ResolveReferenceIn`, `ResolveEntity`, `ResolveEntityIn` and `resolveBelow` all decide which entity a spelling names, which `ground-loads-by-identifier` excludes in its own words, and none of them creates or lists. `ground-delegates` names what they actually are, and I checked it against the seven bodies rather than against their names: five are one-line calls to a rostered resolver, and `ResolveReferenceIn` and `ResolveEntityIn` wrap one with refusing arms of their own while returning what it answered on the accepting path, so none adds an accepting arm.\n\n`AdoptWorkstream` departs from the corrected text the reviewer wrote out, which put it under `ground-loads-by-identifier`. Its own doc comment says \"AdoptWorkstream creates a workstream at an identifier a card already names\", and its body builds a `Workstream` and calls `Save` rather than reading one off disk, so the loading ground is false of it. A false ground is the defect this closed set exists to prevent, and an exemption whose stated reason does not hold is worse than a missing exemption because nobody re-checks it. The count is unaffected, since both grounds are exemptions, and the partition closes either way.\n\nThe arithmetic was re-run over the whole package rather than adjusted by twelve deltas, which is what this card has been sent back three times over. A go/ast sweep at 46abf67 reads 443 declarations in internal/bench and 46 in the subject set; comparing that set against the eighteen roster rows and the thirty-three exemptions gives 13 rostered-in-subject, 5 rostered-outside, no overlap between the rosters, no exemption naming an unswept function and no subject named by neither, so 13 + 33 = 46. AC-8 now asserts that arithmetic at run time so the implementer is not trusting a number in prose."
---
The exemption grounds become a closed set of four, and `AdoptWorkstream` is exempted as a creator rather than as a loader.