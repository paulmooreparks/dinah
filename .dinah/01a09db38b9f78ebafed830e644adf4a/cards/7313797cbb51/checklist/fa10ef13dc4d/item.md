---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:24Z
ordinal: 8
note: "TestEveryAddressAnsweringFunctionIsRosteredOrExempted logged \"443 functions declared over 31 files of internal/bench; subject set 46, of which 13 rostered and 33 exempted\", and asserts rostered-in-subject plus exempted equals the subject-set size at run time. I derived those numbers with my own go/ast sweep before writing any roster, from a fresh worktree at 46abf67, and read 443 and 46 with the same 13/5 split and the same five string-answering rows, which makes four independent methods on the number. The four grounds cover all thirty-three: loads 8, creates 8, lists 10, delegates 7. Armed with the spec's plant, `func (b *Bench) ColumnByAlias(alias string) *Column` added to bench.go: the package compiled and both halves fired, address_form_arms_test.go:295 naming ColumnByAlias as rostered by neither and :299 reporting \"13 + 33 = 46 against a subject set of 47, so the partition does not close\". Resolve with: `go test ./cmd/dinah/ -run TestEveryAddressAnsweringFunctionIsRosteredOrExempted -v`."
---
`TestEveryAddressAnsweringFunctionIsRosteredOrExempted` parses every non-test `.go` file under `internal/bench` and collects each function declaration taking at least one `string` parameter whose results include a `Column`, `Workstream`, `Card`, `Resolved`, `EntityRef`, `CollectionRef`, `Attachment`, `Item` or `Comment`, by pointer or by slice. Every member of that subject set stands in either `addressform.Resolvers()` or the exemption roster, and the exemption roster names no function the sweep did not find. The reverse direction is asserted of the exemption roster alone, because five rostered functions (`resolvePathBody`, `walkBelowCard`, `descend`, `pick`, `ResolveLinkTarget`) answer a `string` and stand outside a subject set keyed on entity result types. The exemption grounds are a closed set of four (`ground-loads-by-identifier`, `ground-creates`, `ground-lists`, `ground-delegates`); an exemption whose ground is outside it fails, and an exemption carrying an empty reason fails. The test logs the subject-set size, the number rostered and the number exempted, and fails when the subject set is empty or when the three numbers do not satisfy rostered-in-subject plus exempted equals the subject-set size. Arming plant: add `func (b *Bench) ColumnByAlias(alias string) *Column` to `internal/bench/bench.go`; the sweep finds it, neither roster names it, and the run goes red while the package compiles.