---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:24Z
ordinal: 5
note: "The seventeen Guide values are minted in internal/addressform. TestEveryDeclaredAddressFormIsTaughtByTheReferencesGuide logged \"17 declared forms held against the references guide\"; TestTheSharedGuideSentencesAreTheDeclaredOnes logged \"17 declarations over 13 distinct guide strings, 3 of them shared\" and asserts the three memberships against quoted strings rather than line numbers. Plant one (delete the `## A card` identifier paragraph) reddened the first alone while the second passed. Plant two (give card-number the same Guide string as card-reference) reddened the second alone at address_form_guide_test.go:116 and :119, which reported 12 distinct strings and a fourth group [card-reference card-number], while the first stayed green. Plant three is the stay-green case: references.md re-flowed at 29 columns with every original line break destroyed and no word changed, after which all four guide checks passed. Resolve with: `go test ./cmd/dinah/ -run 'TestEveryDeclaredAddressFormIsTaughtByTheReferencesGuide|TestTheSharedGuideSentencesAreTheDeclaredOnes' -v`."
---
`TestEveryDeclaredAddressFormIsTaughtByTheReferencesGuide` holds every entry of `addressform.Declarations()` to `guidepin.Carries("references", entry.Guide)` returning nil, and logs the number of declarations checked, failing when it is zero or when it differs from `len(addressform.Declarations())`. Each entry's `Guide` is a verbatim string from `internal/guide/guides/references.md`, and because `Carries` folds both sides with `strings.Fields` before searching, a sentence the guide wraps across two source lines is still found. `TestTheSharedGuideSentencesAreTheDeclaredOnes` groups the declarations by their `Guide` string and asserts the groups come to thirteen distinct strings of which exactly three carry more than one member: the three column forms on "You write a column as its slug, its name, or its identifier", the two prefixed workstream forms on "You write a workstream as the word workstream, a slash, and the workstream's slug or its identifier", and the two bare workstream forms on "The commands that take a workstream also accept the slug or the identifier on its own", with those memberships and no others. Arming plant one: delete the new `## A card` identifier paragraph from the guide, which reddens the first test alone. Arming plant two: give `card-number` the same `Guide` string as `card-reference`, which reddens the second alone while the build stays sound. Arming plant three: re-wrap `references.md` at a different column width without changing a word; both tests must stay green, and a red run means a pin is keyed on the file's wrapping rather than on its sentences.