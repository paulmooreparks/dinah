---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:24Z
ordinal: 10
note: "Pull request #248 (https://github.com/paulmooreparks/dinah/pull/248) is green on all six checks: test on ubuntu, windows and macos, extension on ubuntu and windows, and gofmt. The CI test job runs the whole suite, so the repo-wide sweep is covered there rather than locally, which is what this column's testing boundary requires; locally I ran go build ./..., go vet ./..., gofmt -l ., and go test on cmd/dinah, internal/guide, internal/addressform and internal/profile, all clean. The branch was merged with origin/main before the push and the merge was already up to date at 46abf67. guidepin.Pinned still holds five statements and cmd/dinah/guide_pin_test.go and internal/guide/guidepin are untouched, which the diff confirms as zero lines. The eleven existing references-guide checks pass unedited, and grep -c '^func Test' still answers 10 and 1. One departure from this criterion's last sentence, recorded rather than glossed: cmd/dinah/testdata/prose-figures.txt is modified, and it is under cmd/dinah and is not a new test file. Three of its entries key on line numbers in the references guide, and the new paragraphs moved all three, so the run was red until they were rekeyed from 104, 127 and 129 to 131, 154 and 156, recomputed by reading the lines they now stand at. No file under internal/bench or internal/verb is modified. Resolve with: `gh pr checks dinah-471-a-card-answers-to-its-raw-identifier --repo paulmooreparks/dinah` and `git diff --name-only origin/main...HEAD`."
---
The whole suite is green on the merge of current trunk into the branch, including `internal/profile`, and the pull request's checks are green on every platform. `guidepin.Pinned()` still holds five statements and `TestEveryPinnedStatementStandsInItsGuideAndNamesALiveTest` is unedited. The eleven existing references-guide checks, ten in `cmd/dinah/references_guide_test.go` and one in `cmd/dinah/references_command_resolution_test.go`, pass without edits to their expectations. No file under `internal/bench`, `internal/verb` or `cmd/dinah` outside the new test files is modified, because the card lands no production change beyond the guide.