---
kind: open_question
state: resolved
column: c9428b3bc921
owner: operator
ts: 2026-09-14T02:17:47Z
ordinal: 20
note: "**Operator ruling, 2026-09-10: a failed criterion holds the card. A new state is added for deliberately letting one through.**\n\nHe rejected both options as put and supplied a third, which is the better answer. The question offered keeping `failed` as a settling state, or narrowing the hold to `resolved` and `verified`. His ruling separates the two things the current design conflates: whether the work passed, and whether it may proceed. `failed` keeps its plain meaning, that somebody checked and it did not hold, and a new state carries the permission to proceed anyway.\n\n**What changes on this card.** The hold no longer releases on `failed`. That is a one-line change to the predicate plus a test naming `failed` specifically, and it is folded into this card rather than deferred, because the card is already built and shipping the inverted behaviour for even one revision would put a control on the trunk that opens on the one state recording that the work is wrong.\n\n**What does not change on this card.** The new state itself is filed separately. It is a format change: an item's `state` gains a value, every reader that switches on state has to handle it, and it needs a verb or a flag, since six verbs exist and none of them expresses this. It also raises a question with an uncomfortable answer, which is that Dinah records who an item is for and enforces nothing, so \"only the operator may override\" is not expressible today. And it should demand a recorded reason, the way the move-level override already does, or the state becomes a silent way past the check.\n\n**Why sequencing it this way leaves no hole.** With `failed` holding and the new state not yet built, a criterion that genuinely failed and that the operator has decided to accept anyway is still passable, using the move-level override marker that already lifts this hold along with capacity and loop limits. So the interim state is safe rather than merely tolerable.\n\n**Note on the state count, corrected here because the ruling was recorded in chat first.** An item has four states today: `pending`, `resolved`, `verified` and `failed`. The new one is a fifth rather than a fourth."
---
A `failed` acceptance criterion opens the gate, so a column gated on criteria admits a card whose criterion was tested and did not hold. Is that the intended semantics, or should `failed` hold the card as `pending` does?