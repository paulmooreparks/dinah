---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:46Z
ordinal: 6
note: "Section 6.4's fenced table carries a ninth row, \"the card carries no unresolved item that names the destination   unresolved-item\", unprefixed. checkLists[Move] carries the gate at check.move.9 and the loop row, its refusal and text unchanged, at check.move.10. pullChecks carries the gate at check.pull.11 immediately after the capacity row, and the prior 11 through 14 move to 12 through 15.\n\nCitation, the two existing cross-checks, which now pass with the gate row inside the profile-matched prefix rather than past it: cmd/dinah/main_test.go TestPerCommandHelpFollowsTheProfile (its per-row comparison against profile.Preconditions, its LayerPrefix assertion over the rows past that prefix, and its rendered row count composed as len(workbench) + len(lists[Move]) + 1), and the \"help pull prints the arguments and the thirteen checks in order\" subtest, which walks the rendered page against verb.Checks(Pull) in order. internal/verb/pull_test.go's row-by-row table was updated to the new numbering and is the second reader of it.\n\nLocale coverage was checked mechanically rather than by eye: a script over all eight files under internal/msg/locales reports check.move.9, check.move.10 and check.pull.11 through .15 present in every one, none missing. check.move.10 and check.pull.12 through .15 carry their prior text and their prior source fingerprints unchanged, which is why no translation went stale; the two new strings carry fresh German and Hindi translations with fingerprints of the new English, and the five skeleton catalogs carry the English under the skeleton flag as they do for every other check row.\n\nResolve with: go test ./cmd/dinah/ -run \"TestPerCommandHelpFollowsTheProfile\" and go test ./internal/verb/ -run \"TestPullChecksAgainstTheFullSeventeenRowTable\""
---
docs/spec/core-profile.md's section 6.4 fenced table gains a ninth row for the gate (refusal name unresolved-item, no dinah. prefix); checkLists[Move] carries the new gate check at position 9 (key check.move.9 under CheckKey's composition) and the existing loop-limit check shifts to position 10 (key check.move.10, text unchanged); pullChecks gains the equivalent row immediately after its existing capacity row, shifting the four rows after it down by one. Check: cmd/dinah/main_test.go's existing help-move cross-check (~line 1320-1345, comparing checkLists[Move] against profile.Preconditions and asserting every row past that prefix is dinah.-prefixed) passes with the gate row inside the profile-matched prefix rather than past it; the existing help-pull row-count/order cross-check (~line 7587-7614) passes against the renumbered pullChecks; every one of the eight locale files under internal/msg/locales carries check.move.9 (new gate text), check.move.10 (old loop-limit text, unchanged), check.pull.11 (new gate text) and check.pull.12 through .15 (the prior .11-.14 text, unchanged) with no locale missing a key.