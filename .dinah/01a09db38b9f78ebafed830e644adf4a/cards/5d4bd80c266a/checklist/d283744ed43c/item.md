---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:46Z
ordinal: 8
note: "Both halves ran, and both are the narrow design rather than the wider one. No column carries a list of kinds: the whole declaration is gate_items, a boolean, and nothing anywhere reads it alongside a kind.\n\nHalf one, two runs against a scratch workbench (a throwaway workbench under C:\\dinah-scratch, DINAH_HOME pointed at a scratch directory beside it, never the operator's own). One column carried the flag; one card carried one pending item of each of the three kinds naming that column by identifier. Run one, all three pending: the move was refused \"unresolved-item this card carries the item 0a3d60ff53f4\", which is the acceptance criterion. Run two, each kind held alone with the other two settled: with only the criterion pending, refused naming it; with only the decision pending, refused naming d315a1f20698; with only the question pending, refused naming 23b934901e86. Then the operator's --override carried the card through and the journal line reads \"event\":\"moved\",\"override\":true. The same three-kinds-separately shape is in the suite as cmd/dinah/gate_test.go TestAGatedColumnHoldsEveryKindOnItsOwn, which runs each kind on its own fixture so a build holding two kinds and letting the third through cannot pass by counting one refusal.\n\nHalf two, the code read. grep over the whole refusal path (internal/bench/entity.go and internal/verb/mutate.go) for acceptance_criterion, open_question and decision returns four lines and none of them is in the path: entity.go:798 is the Kind field's own comment naming what the field holds, entity.go:881 is ItemBlocksClaim, which is CORE-CLAIM-9's separate claim-time refusal and is untouched by this card, and the two mutate.go hits are the English word decision in prose about locking. GatingItems, itemsWhere, ItemIsResolved and canLand's hold block name no kind at all. A first draft of canLand's comment named the three kinds in prose to explain why none is special-cased, and it was rewritten to say the same thing without them, because a criterion checking for a kind name in the refusal path should not have to judge whether a mention is a mention.\n\nReproduce half two with: grep -n \"acceptance_criterion\\|open_question\\|decision\" internal/bench/entity.go internal/verb/mutate.go"
---
The shipped declaration is the one the operator approved on 2026-09-09 and nothing more. A column declares that it holds work with a single boolean in its own definition, and no column carries a list of which item kinds hold there. The refusal names no item kind: a build in which any kind is treated differently from another, whether by being always held or never held, has departed from the approval. Check by two runs against a scratch workbench. Give one column the flag and file, on one card, an unresolved item of each of the three kinds naming that column, then confirm the move into it is refused for each kind on its own. Then search the refusal's own code path for any mention of a specific item kind and confirm there is none. Failing case: a kind list appears on a column, or a kind name appears in the refusal path, or one kind is held while another naming the same column is not.