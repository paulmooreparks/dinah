---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:45Z
ordinal: 4
note: "The function is bench.GatingItems(cardDir, columnID) in internal/bench/entity.go. It answers every item whose Column equals the column and whose state does not lift the hold, in ListIDs order, excluding no kind, and it shares its reader with BlockingItems through the new itemsWhere. The state predicate it reads is bench.ItemLiftsColumnHold, which admits resolved and verified alone; ItemIsResolved beside it is unchanged and is what the claim refusal still reads.\n\nCitation: internal/bench/gate_test.go, TestGatingItemsExcludesNoKind and TestAFailedItemHoldsAColumnThatTheClaimRefusalLetsThrough. The first's subtests are one pending item of each kind naming the column returning all three in identifier order; a criterion filed alone returned, which is the case that proves the function did not inherit ItemBlocksClaim's exemption; each of resolved and verified dropping its item and leaving the other two; an item naming another column and one naming none both absent. The second names the failed state directly, asserting ItemLiftsColumnHold answers false for a failed item of every kind, that ItemIsResolved and ItemBlocksClaim answer as they did before, and that a failed criterion read off disk comes back from GatingItems.\n\nObserved, first pass: fail before, pass after. Adding item.Kind != \"acceptance_criterion\" to the predicate turned it red at gate_test.go:216 (\"wanted all three kinds held, got 2\"), at gate_test.go:231 (\"wanted the criterion held, got 0\") and three times at gate_test.go:247; restored byte-identical (cmp reports identical) and green again.\n\nObserved, this pass, for the failed state: pointing GatingItems back at ItemIsResolved turned TestAFailedItemHoldsAColumnThatTheClaimRefusalLetsThrough red at gate_test.go:337, \"wanted the failed criterion held, got \" with nothing after it, and separately adding ItemFailed back to ItemLiftsColumnHold's case list turned it red at gate_test.go:322 for all three kinds and at gate_test.go:192 in the state table. entity.go restored from a byte-identical backup (cmp reports identical) after each plant and green again.\n\nResolve with: go test ./internal/bench/ -run \"TestGatingItemsExcludesNoKind|TestAFailedItemHolds\""
---
A new bench function (name Implement's to choose) returns every item on a card whose Column field equals a given column ID and whose state does not lift a column hold, in ListIDs order, excluding no kind. The states that lift the hold are resolved and verified alone: a failed item goes on holding, on the operator's OQ-5 ruling of 2026-09-10, so the gate reads its own state predicate rather than ItemIsResolved, which the claim refusal keeps unchanged. Check: an entity_test.go case files one pending item of each of the three kinds naming the same column and asserts all three are returned; a case with one of them resolved or verified asserts only the remaining two are returned; a case with a failed item asserts it is still returned; a case with an acceptance_criterion pending is asserted present, proving the function does not inherit ItemBlocksClaim's exemption.