---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:46Z
ordinal: 5
note: "canLand refuses contract.UnresolvedItem naming the first holding item, in the block after the capacity block and before the loop-limit block, and req.Override lifts it and joins the returned override bool.\n\nCitation: cmd/dinah/gate_test.go. (a) TestAGatedColumnHoldsEveryKindOnItsOwn, which asserts both the refusal name and the item's own identifier, read back off disk by soleItemID. (b) TestTheOperatorCarriesACardPastAGate, which asserts the move succeeds and that the one moved event in the card's journal carries Override true. (c) TestANonOperatorIsRefusedTheOverrideAtAGate, which asserts not-operator rather than unresolved-item, proving the refusal comes from the row canRoute already carried and that no new override path was opened. (d) TestAnItemSettledAtAGatedColumnHoldsNothing, which verifies the item and moves. (e) TestAGatedColumnAnswersAheadOfTheDepartureLoopLimit, which drives a regressive move out of a column at its declared loop limit into a held column and asserts unresolved-item comes back and dinah.at-loop-limit does not. TestAColumnDeclaringNoGateHoldsNothing is the negative control the criterion does not name, and without it every assertion above would pass on a build that refused every move.\n\nObserved, first pass: fail before, pass after. Disabling the block (if false && destination.GateItems) turned all three kinds red at gate_test.go:88, turned the pull red at gate_test.go:182, and turned the override red at gate_test.go:148 printing the moved event with Override:false; restored byte-identical (cmp reports identical) and green again.\n\nObserved, this pass, for (e): moving the gate block back below the loop-limit block turned TestAGatedColumnAnswersAheadOfTheDepartureLoopLimit red at gate_test.go:273 and :276, both printing \"dinah.at-loop-limit this card has reached the loop limit of column doing\"; mutate.go restored from a byte-identical backup (cmp reports identical) and green again.\n\nResolve with: go test ./cmd/dinah/ -run \"Gate|OperatorCarries|NonOperatorIsRefused|PullIsHeld|ItemSettled\""
---
canLand (mutate.go) refuses a move into a column whose GateItems is true when the new gating-items function returns any item for that column, reporting contract.UnresolvedItem naming the first such item's ID, positioned after the capacity block and before the loop-limit block, which is where the ninth published row sits and is what makes the code check the rows in the order dinah help move promises them; req.Override from the operator lifts the refusal and the move's returned override bool is true. Check: a mutate_test.go/looplimit_test.go-style case for (a) a plain gated refusal naming the blocking item's ID, (b) an operator move with --override succeeding and the moved event carrying override:true, (c) a non-operator override attempt refused not-operator by the existing canRoute row (proving no new override path was opened), (d) a move into the same gated column with the item already resolved succeeding without needing an override, and (e) a regressive move that fails both this row and the departure's own loop limit answering unresolved-item rather than dinah.at-loop-limit.