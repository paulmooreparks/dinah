---
kind: open_question
state: resolved
column: 0789fd2dbefd
owner: operator
ts: 2026-09-14T02:17:47Z
ordinal: 16
note: "**Operator ruling, 2026-09-09: approved as specced.**\n\nThe declaration syntax stands: a column gains a boolean `gate_items: true` in its own definition, hand-edited in the same manner as the existing capacity and operator-owned flags, and entry to that column is held while any unresolved item whose own `column` field names it exists on the card. No kind of item is listed on the column and none is special-cased in the tool.\n\n**What he was told before approving, so the record shows the approval was informed.** Agent Design Review raised, in its move-note rather than burying it, that this design admits two readings of his earlier ruling. That ruling said a workbench declares which of its steps refuse a card while a named item is unsettled. Under what is approved here, the column declares only that it honours holds at all; which items hold there follows from which items are filed naming that column, which places the selectivity in each item rather than in the workbench's own declaration. The narrow reading is that this satisfies the ruling, since the workbench still decides which steps honour a hold and an item naming its own step is how this board already works. The ambitious reading is that a workbench author cannot state a standing policy, so an item filed later by somebody who forgets simply does not hold, and the rule lives in every item rather than in one place. The reviewer read it as adequate rather than short, checked it against the project's own drafted core profile which declares the same shape with no per-kind surface, and put both readings in front of him. He approved.\n\n**What this approval creates**, recorded as AC-8 on this card: the shipped declaration must match what was approved here, and a build that adds a per-kind list to the column, or that special-cases any item kind in the refusal, has departed from the ruling rather than refined it.\n\n**Two things the design review verified rather than asserted, which the implementer should not re-derive.** Building this forces `docs/spec/core-profile.md`'s own refusal table to grow a row, because an existing test requires a check added past that table's row count to carry a prefixed name while the profile fixes this refusal's string unprefixed; that coupling is real and is carried as a criterion rather than as a documentation chore. And the deadlock case, an item that holds the very step meant to settle it, cannot be detected by the tool, because nothing in Dinah's data distinguishes an item that deliberately names its own step from one that traps itself; the spec says so plainly rather than promising a refusal it cannot build."
---
Approve the declaration syntax: a column gains a boolean `gate_items: true` (hand-edited column.md frontmatter, no new CLI verb), which holds entry to that column for every unresolved item whose own `column:` field names it, of any kind, with no per-kind list on the column itself. Approve as specced, or say what to change?