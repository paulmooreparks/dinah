---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:03Z
ordinal: 7
note: "Both halves planted and observed at c25c20f. Rewriting the opening paragraph's close from \"rather than the whole of it\" to \"and nothing else\" failed twice, as this criterion predicts: `references_guide_test.go:408: the references guide does not carry \"rather than the whole of it\"` and `references_guide_test.go:419: the references guide's opening paragraph carries \"and nothing else\", and the sentence naming what DinahPath leaves out may not close the list`. Dropping the serial comma from the asserted phrase failed with `references_guide_test.go:408: the references guide does not carry \"no brackets, no wildcards, no functions, and no axes\"`. Both restored byte-identically, test ok. One correction to the spec's section 5.6 was needed and is in the diff: the closing-phrase read is scoped to the opening paragraph rather than to the whole guide, because \"and nothing else\" is ordinary English the guide already uses correctly further down, where `instructions` takes a card or a column and nothing else at all. Read against the whole text the check failed on a guide that was perfectly correct, which is observed rather than predicted; AC-9's own wording (\"standing in a paragraph that may not close the list\") is what the shipped test does."
---
Change the guide's opening paragraph so that `rather than the whole of it` reads `and nothing else`, and run `go test ./cmd/dinah/ -run TestTheReferencesGuideIntroducesDinahPathWithItsCorrection`. It fails twice: once for the missing clause that refuses to close the list, and once for the closing phrase `and nothing else` standing in a paragraph that may not close the list. The phrase the test asserts is the serial-comma spelling `no brackets, no wildcards, no functions, and no axes`, which is what section 3.1's text ships; drop that comma and the test fails on the missing phrase. Every comparison in that test folds the guide's whitespace with `strings.Join(strings.Fields(text), " ")` first, so the paragraph's hard wrap cannot report a false failure.