---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:03Z
ordinal: 11
note: "Not verified here, and deliberately so: the Implement column forbids the repo-wide sweep, which belongs to Test, and this criterion is gated on Merge. What was run at c25c20f in C:/dinah-scratch/dinah-457-impl/wt: `go build ./...`, `go vet ./...`, `gofmt -l .` (empty), and `go test` on the two packages the diff touches, `./internal/guide/` and `./cmd/dinah/`, both ok. The pull request's own three test jobs run the whole sweep on macOS, Ubuntu and Windows. The five-red claim was tested rather than assumed: the four guard edits and the new test file were reverted, leaving the two rewritten guides alone, and one `go test ./cmd/dinah/` produced exactly the five named failures and no others, with a sixth entry that is TestEveryStatementOfTheRenderingHeadIsCoveredOrNamed reporting `the coverage run failed, so nothing below can be read`, which is the coverage child re-running the suite exactly as this criterion's note says. So the list of five is complete. The serial-comma consequence landed as this criterion predicts, inside TestTheReferencesGuideSaysWhichCommandTakesWhat rather than as a sixth red: it reported `the references guide does not teach \"accepts `oq`, `ac` and `d` for those same three\"` and the comma'd literal is the repair. Everything was restored from byte-identical copies afterwards."
---
`go test ./...` passes in a worktree carrying the change. Writing the spec's guide text alone into a worktree at `b825059` and running the same command produced exactly five failing tests, in `cmd/dinah` and nowhere else: TestTheReferencesGuideSaysWhichCommandTakesWhat, TestAGuideTableSurvivesTheWindowItIsReadIn, TestEveryProseFigureIsDeclared, TestNoProseFigureEntryIsStale and TestEveryAddressTheContentsTreeDrawsResolvesThroughTheCommandsThatDeclareIt. Section 6 of the spec is the repair for each. Every other package was ok. The serial commas D-12 rules in add one more failing assertion, `the references guide does not teach "accepts \`oq\`, \`ac\` and \`d\` for those same three"`, inside the first of those five rather than a sixth failing test; it was observed against the comma'd draft and section 6 carries its repair.