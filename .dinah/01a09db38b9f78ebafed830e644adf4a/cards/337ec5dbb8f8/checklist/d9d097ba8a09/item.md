---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:03Z
ordinal: 3
note: "Both sides planted and observed at c25c20f, and both were watched compiling and running rather than reasoned about. Roster side: rewrote the fifteen `\"<command>\": {\"references\"}` entries in internal/verb/definition.go to `{}` AND replaced all fifteen `Guide: \"references\"` params entries with `Guide: \"\"`. `go build ./...` printed BUILD-OK, `go test ./cmd/dinah/ -run TestTheReferencesGuideTableNamesEveryCommandThatTakesAReference -v` printed `=== RUN` and then stopped with `references_guide_test.go:120: no command points at the references guide, so this check read nothing`. Table side: deleted all fifteen data rows, leaving header and separator; the run stopped with `references_guide_test.go:114: the references guide's table carries no row for \"show\"`, which is parseReferencesGuideTable's own fatal reported at the caller's line, exactly as this criterion says, and the test's own len(declared)==0 line was never reached. Both restored byte-identically, tests ok."
---
Empty each subject set of `TestTheReferencesGuideTableNamesEveryCommandThatTakesAReference` in turn and watch the run stop rather than pass. Roster side: delete the fifteen `"<command>": {"references"}` entries from the `guides` map in `internal/verb/definition.go` AND strip `Guide: "references"` from all fifteen `params` entries, because `verb.Guides` merges the two and clearing either alone leaves the roster at fifteen. Rebuild and the test stops with `no command points at the references guide, so this check read nothing`. Table side: delete every data row of the guide's table, leaving header and separator, and the run stops inside `parseReferencesGuideTable` with `the references guide's table carries no row for "show"`, that parser's own fatal over show, path and edit, reported at the caller's line because the parser is a `t.Helper`. That parser fatal is what closes the table side; the test's own `len(declared) == 0` line cannot be reached while the parser demands three named rows, and the comment above it says so.