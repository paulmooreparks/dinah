---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:19Z
ordinal: 3
note: "Verified. `go test ./cmd/dinah/ -run TestEveryHelpPageNamesTheReferenceKindsItDeclares -v -count=1` passes and logs \"8 catalogues enumerated from internal/msg/locales/, 144 help pages read, attach and attachments among them\" (cmd/dinah/reference_kinds_help_test.go, the t.Logf near the end; the sawAttach/sawAttachments t.Errorf above it is what names those two). All three plants armed. Making verb.ArgumentMeaning drop the last declared kind reddened at the \"does not print the label\" assertion naming command, language and label, e.g. \"internal/verb declares that attachments's reference may name collection and `dinah help attachments` under af does not print the label \\\"a whole collection\\\"\". Making it join every kind of ReferenceKindOrder() reddened on instructions at the \"declares no kind of ... carrying it\" assertion, three labels named. Replacing the template reversal with a split of the whole rendered row reddened on attach and attachments in every catalogue, reading the summary's own semicolon as a kind boundary, e.g. attach printing the label \"what the file hangs off\". Restored byte-identically after each and green. One defect found and fixed while building this: folding the template's two halves separately strips the space before {kinds}, so the rendered template is folded with the sentinel still in it and cut afterwards."
---
`go test ./cmd/dinah/ -run TestEveryHelpPageNamesTheReferenceKindsItDeclares -v` passes and logs eight catalogues enumerated from `internal/msg/locales/` and one hundred and forty-four help pages read, `attach` and `attachments` among them; making `verb.ArgumentMeaning` drop the last declared kind reddens naming a command, its language and the absent label, making it join every kind of `ReferenceKindOrder()` reddens on `instructions` naming a label present for an undeclared kind, and replacing the template reversal with a split of the whole rendered row on `verb.ReferenceKindSeparator` reddens on `attach` and on `attachments` in every catalogue.