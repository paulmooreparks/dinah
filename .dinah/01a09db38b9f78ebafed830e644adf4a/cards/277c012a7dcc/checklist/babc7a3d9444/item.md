---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:20Z
ordinal: 11
note: "Verified, and this is the criterion that arms a check which passed by luck. Baseline confirmed first: at f574ac5, before any edit, the SHIPPED TestTheReferencesGuideNamesTheCommandsThatTakeAWorkstream passed green against a sentence that omits restore. After the rewrite, `go test ./cmd/dinah/ -run TestTheReferencesGuideNamesTheCommandsThatTakeAWorkstream -v -count=1` passes and logs \"18 commands probed, 9 reached the workstream, 9 refused\" as two separate counts, with the nine refusals logged by name (attach dinah.not-attachable, rename dinah.not-renamable, and seven dinah.unknown-path). That set matches the round-3 reviewer's independent measurement command for command and refusal for refusal. All three plants armed. Deleting restore's entry from workstreamProbeSetup recreated today's bug exactly and reddened with \"refused restore exit 2 dinah.not-archived\", \"restore: binary=false declaration=true\", \"restore: binary=false sentence=true\" and both counts failing at 8 and 10, which proves the per-command setup is load-bearing rather than decorative. Dropping restore from the guide's rewritten sentence reddened \"restore: binary=true sentence=false\" and \"restore: declaration=true sentence=false\". Dropping ReferenceKindWorkstream from referenceKinds[\"restore\"] reddened \"restore: binary=true declaration=false\" and \"restore: declaration=false sentence=true\". No single edit moved all three sides into agreement, which is what the three-way shape was for. Restored byte-identically after each and green."
---
`go test ./cmd/dinah/ -run TestTheReferencesGuideNamesTheCommandsThatTakeAWorkstream -v` passes and logs eighteen commands probed, nine that reached the workstream and nine that were refused, as two separate counts; deleting `restore`'s entry from `workstreamProbeSetup` reddens naming restore, the declaration's yes and the probe's dinah.not-archived, dropping `restore` from the guide's rewritten sentence reddens naming restore and the sentence, and dropping `ReferenceKindWorkstream` from `referenceKinds["restore"]` reddens naming restore and the two sides that then disagree.