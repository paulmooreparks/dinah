---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:20Z
ordinal: 13
note: "Verified. internal/verb/checks.go now declares three rows for edit in this order: {contract.UnknownPath, \"check.edit.1\"}, {contract.IsACollection, \"check.edit.3\"}, {contract.NoEditor, \"check.edit.2\"}. check.edit.3 is present in all eight catalogues by the shape of spec 4.1 (translated in de and hi with a source fingerprint, skeleton carrying the English in the other five), and cmd/dinah/column_widths_test.go's existing sweep over verb.Checks against every msg.Tags() entry passes, which is what holds it in all eight. `dinah help edit` against the built binary prints all three rows in that order: \"1 the reference resolves to a path / dinah.unknown-path\", \"2 the reference names one thing rather than a whole collection / dinah.is-a-collection\", \"3 an editor is resolvable / dinah.no-editor\". Accepting-side evidence pinned beside the declaration: `dinah edit wb-1/comments` exits 2 with \"dinah.is-a-collection wb-1/comments names a whole collection rather than one thing in it\", so the row names a refusal the binary really raises. English text is the round-3 reviewer's corrected wording, \"the reference names one thing rather than a whole collection\", stating the precondition that must HOLD in the register every sibling row uses, rather than section 4.4's inverted phrasing which would have read as though edit requires a collection in eight languages. Position measured, with the reviewer's correction to the invocation: `dinah edit wb-1/nosuch` with every editor variable unset raises dinah.unknown-path (a bare `dinah edit nosuch` raises unknown-card, which is why the spec named the wrong invocation), so resolution runs first; and `dinah edit wb-1/comments` raises dinah.is-a-collection with a working shim, with a broken editor name, and with DINAH_EDITOR, EDITOR and VISUAL all unset, so the collection refusal sits before every editor check."
---
`internal/verb/checks.go` declares three refusal rows for `edit`, `dinah.unknown-path`, `dinah.is-a-collection` and `dinah.no-editor` in that order, `check.edit.3` is present in all eight catalogues by the shape of spec section 4.1, and `dinah help edit` prints all three rows; `dinah edit wb-1/comments` exits 2 with `dinah.is-a-collection` against the built binary, which is the refusal the new row names.