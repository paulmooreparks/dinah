---
kind: decision
state: resolved
owner: holder
ts: 2026-09-14T02:18:23Z
ordinal: 45
note: "New English \"the reference resolves to an entity this command can show\", the same register as the other three. The omission was measured rather than read: against the branch binary, `show intake` and `show wb1-1/comments` both exit 0, and the shipped row said \"a card or a path below one\". `de` and `hi` refingerprinted."
---
`check.show.2`: read against the current English and its context; retranslated in `de` and `hi` because a sweep of every check row of the eighteen roster commands found a fourth instance of the class the review named three of, and this row omitted the column and the whole collection.