---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:21Z
ordinal: 25
note: "The sibling asked for take-or-say-why on the `cmd.edit.summary` and `cmd.path.summary` enumerations and on `edit`'s missing `dinah.is-a-collection` refusal row. Both are taken. Reading all eighteen roster commands' summaries out of `internal/msg/locales/en.json` found six that enumerate address kinds rather than two: archive, delete, edit, path, restore and show. All six are wrong against the declaration once the workstream counts, so none of them is left enumerating. The refusal row is taken because it states the same fact as the `no` in `referenceKinds[\"edit\"]` against the collection kind and prints on the page this card regenerates. What is declined, and recorded in spec section 8: nothing in the tree holds any command's refusal rows against the refusals it can raise, so `edit` is one sighting of a class nobody has enumerated, and enumerating it means running every command against every refusal it declares and every one it does not, which somebody would pick up on its own. Nothing holds the six summaries stripped either, for the reason section 4.3 gives."
---
dinah-467's two smaller residue items are both taken rather than declined, and the command-summary one is taken over six keys rather than the two that card named.