---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:20Z
ordinal: 7
note: "Verified, with the plant corrected because the one written could not redden. First half: all ten summary keys of spec 4.2 carry the new English in en.json, de and hi are retranslated and refingerprinted, and `go test ./internal/msg/ -run TestATranslationTracksItsEnglishSource -count=1` passes. Plant as written (restore de's pre-card TEXT while leaving its source alone) does NOT redden, and I ran it and watched it pass: internal/msg/msg_test.go:263 compares entry.Source against Fingerprint(base.Text) and never reads the translation's own text, so a reverted German with a current source is invisible to it. The plant that does exercise the guard is the revert a real translator would make, text AND source together: setting de's param.show.card.summary back to its pre-card German and its source back to the old English's fingerprint reddened at msg_test.go:289, \"param.show.card.summary: the source is stale, so the English text changed after this entry was translated\". That is the criterion's own stated reasoning (\"the guard fingerprints the English of the day, which this card changed\") and it holds. Restored byte-identically and green.\n\nCORRECTION, round 2, after Agent Code Review checked the finding rather than accepting it. This note previously recorded that nothing holds the ten rewritten summaries' de/hi TEXT different from English. That was false. TestATranslationIsNotEnglishUnderAnotherTag, at internal/msg/msg_test.go:505, holds precisely that, for every non-skeleton entry of every non-base catalogue, keyed on the entry rather than on a roster, and none of the ten keys carries skeleton or verbatim in either language, so none escapes it. Round 2 armed it again on this round's own keys: pasting the English into de's check.delete.1 reddens at msg_test.go:528 with \"check.delete.1: carries the English text and is marked neither skeleton nor verbatim, so English is standing where a translation should be\". What the fingerprint guard genuinely cannot see is a translation reverted to an older rendering in its own language while its source is current, which is a property of the fingerprint contract rather than of these ten keys, is recorded twice by name in workbench document 51, and is already ruled on there."
---
All ten summary keys of spec section 4.2 carry the new English, and `go test ./internal/msg/ -run TestATranslationTracksItsEnglishSource` passes; restoring `de`'s pre-card text for `param.show.card.summary` while leaving its `source` alone reddens it naming `de/param.show.card.summary` and the fingerprint of the English of the day.