---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:18:43Z
ordinal: 12
note: "Corrected 2026-09-12 on head 80d20af, the follow-up to 995636e through 5b71b23: the note round 1 left on 995636e claimed the planted corpus carried every shape that defeated an earlier round, and that claim was false, because a review then planted seven shapes against the shipped guard and every one escaped while the corpus stayed green by omission. The criterion's text was corrected in the same pass rather than reshaped. Probing this round's own clauses once they were armed then found two claims that did not hold, and 80d20af corrects both: the walk's binding-form sentence said a type-switch guard was an assignment the walk reads, but the assertion it reads was not a shape that carried the taint, so a type assertion whose operand carries the taint is now one of the six carrying shapes; and the send arm, which the channel-receive residual's defense rests on, fired under probe but carried no corpus case, so it now has one and cannot be removed silently. On 80d20af the quoted triple runs green, the corpus holds 36 subtests, 11 of them new, and every new clause was armed by removal, reddening its own plant, and restored to green; the widened clauses report nothing on the real tree, which the scan test holds over cmd and internal. Round 3, on the head this round pushes, widens the enumerations a second time after a review defeated the shipped guard nine ways: the nine review shapes close in eight clauses, and the round's own sweep plants eighteen further shapes beyond them, two classes of which the review had not listed (a card or a reader stored as a container key, and a reader stored in a composite literal's element values or a spelled map literal's keys). On the pushed head the quoted triple runs green, the corpus holds 64 subtests, 28 of them new this round, every clause was armed by removal with its plant reddening alone or beside the siblings that share it, and the widened clauses report nothing on the real tree. The two read-back shapes the review ruled inside the documented limits were not widened for: the selector and index read-back out of local containers stays a limit, and round 3 closes the stores into those containers instead. The one shape the sweep could not close inside the design is named in the handoff: an elided-type composite literal whose map key holds a reader cannot be told from a struct field name without type information, which is the syntax-not-types ground the enumerations already state. The two attacks on record from this card's runs still hold: the no-op registry rewrite caught by rewriteFile's own arming, and the shrunk +1 offset that reddened the setup's own disjointness arm before anything else could pass vacuously. Round 4, on the head this round pushes, plants fifteen further shapes beyond the review's four blockers, the minor, and the residual, and its sweep found two escapes. The first sat in the 2b discovery: a literal nested inside a composite literal in a package var's initializer never reached the walk, because the discovery asserted the initializer itself, and the search now reaches anywhere under the initializer and stops at the first literal it meets, so the walk over that literal owns the bodies of the literals nested inside it and nothing reports twice. The second was the extraction class: the walk bound holders and read nothing back out of them, so an index, a slice, a type assertion, a type switch, and a field read out of a binding all laundered a reader or a card into a fresh name, a range clause over an alias container bound no alias, and a call placed through such an extraction was invisible to the callee read; namesAReader and aliasReads now read through those forms to the holder, bindRange binds aliases out of an alias subject, and isReaderCall delegates to namesAReader, so the call side reads with the walk's own reads. The round 3 sentence on read-back narrows honestly: the alias half of the selector and index read-back now closes, the card half closes for index and slice, and a card read off its holder by a field stays open because a card's own fields are read all over the tree and are not cards, which the aliasReads comment names as the half it leaves open. The elided inner literal key the residual names was probed this round and escaped, so that limit stays accurate as written. The corpus holds 88 subtests, every clause armed by removal with its plant reddening alone or beside the siblings that share it, the discovery search armed the same way with the nested-literal plants reddening together, and the widened clauses report nothing on the real tree. Both sweep classes are promoted into Convention counterexamples 3."
---
Nothing union-merges the registry, and no unstamped card escapes the three files allowed to read one by any shape these rules enumerate. `go test ./internal/bench -run 'TestTheRegistryIsNotUnionMerged|TestEveryCardNumberReadGoesThroughTheBench|TestTheLoadCardGuardGoesRed'` passes. The first asserts that the `.gitattributes` text a new workbench is written with names no pattern matching `card-numbers.txt`. The second is the guard, and its subject is all three free card readers declared in `internal/bench`, which are `LoadCard` (`card.go:100`), `loadRetiredCard` (`:112`), and `loadCard` (`:120`), rather than the exported one alone. RULE 1: it parses every non-test `.go` file under `cmd` and `internal` and records each `*ast.Ident` naming one of the three in a file whose package clause is `bench` or which dot-imports `dinah/internal/bench`, and each `*ast.SelectorExpr` whose `Sel.Name` is one of the three and whose qualifier, read through any number of parentheses, the file's own import list binds to `dinah/internal/bench`; a reference in an unlisted file fails, and a count above a listed file's budget fails. The allowlist is exactly three entries, `card.go` at 7, `check.go` at 1, and the migration's own file at 1, asserted in both directions on the `kindAllowlist` model. RULE 2, over the allowlisted files only, each of which names the functions in it permitted to answer a card: (2a) a `*ast.FuncDecl` whose result list mentions `Card` in any form, exported or not, fails unless its file's entry names it; (2b) a package-level `var` whose type mentions `*Card` fails, as does one whose type is elided but whose initializer holds a card pointer, as does one whose initializer is a bare reference to a reader, read through the address-of and dereference operators or the element values of a composite literal, and a struct field whose type mentions `*Card` fails; (2c) within a function, the guard tracks the identifiers a free reader's value is bound to, reading an assignment, a var declaration, and a range clause, which are every binding form Go has because a type-switch guard and the receive clause of a select are assignments too, so that `var c, err = LoadCard(...)` binds the card exactly as `c, err := LoadCard(...)` does and a range clause whose source carries the card binds its loop variables. Six right-hand shapes carry the taint to whatever the binding names: a call of one of the free readers, direct, through a local name bound from a bare reference to one, or through parentheses and the address-of and dereference operators, and a bound qualifier read through parentheses with it; a tracked identifier standing alone, read through any number of parentheses, address-of operators, and dereference operators, so that `d := c`, `d := (c)`, `p := &c`, and `d := *c` all bind the card; a type assertion whose operand carries the taint, so that a type-switch guard launders nothing, while an assertion through a conversion is caught at the conversion by the argument clause below; an `append` whose argument carries the taint, read through a nested append's spread; a composite literal carrying a tracked identifier anywhere inside it, whether its type is a named struct, a slice, or a map; and a call whose function expression carries a tracked identifier, which is a method called on the card. It fails on a tracked identifier appearing anywhere inside a `return`, on a free-reader call standing inside a `return`, and on a bare reader reference standing in a `return`'s results. A value carrying the taint fails when it is stored on a selector, an index, or a star target, on one of the enclosing function's own named results, or on a name the function does not itself introduce, which is the clause that catches a package-level variable, and the key of an index store is read for the card and for a reader with the same ground. A tracked identifier fails when it is sent on a channel or stands anywhere in the argument list of a call other than the builtin `append`, and a free-reader call fails when it stands inside a call's argument list, whether it names the reader directly or goes through a local name bound from a bare reference to one. A bare reader reference fails when it is handed as a call argument, the builtin `append`'s own list included, when it is sent on a channel as a value, and when it is stored on the same selector, index, star, named-result, and un-introduced targets a card is, read through the parentheses and operators a call is and inside the element values of a composite literal and the keys of a literal whose type says map. The named-result clause is required because a function with named results answers with a bare `return`, which carries no result expression for the return clause to read. Each target of a multiple assignment or declaration takes the value only from its own pair on the right, so the error a free reader answers in its second result pairs with nothing and no error return in the file is tainted. The taint is carried by those six shapes and no others: an implementation tainting on any expression that merely mentions a tracked identifier reaches `n := card.Number` and forbids the migration this allowlist exists to permit. The third test proves the scan red over planted files, on the `TestTheKindGuardGoesRed` model, and its cases include every shape that defeated an earlier round's guard, each spending exactly one free reference so that rule 1's budget is satisfied and the escape is rule 2's to catch: an exported `Bench` method wrapping the one allowed call in `check.go`, reported by 2a and 2c; that same method rewritten as `var c, err = LoadCard(...)` answering `(any, error)`, which only the `*ast.ValueSpec` binding form catches; a permitted probe passing its card to a helper declared in `bench.go` that stashes it on the bench for an exported accessor to answer, which only the call-argument clause catches; a permitted probe binding `h := CardHolder{Card: c}` and answering `h` on the next line, with `CardHolder` declared in `bench.go`, which only the composite-literal clause catches and which a caller in `internal/verb` was run against end to end; the same evasion with `cs := []*Card{c}` and with `var m = map[string]*Card{id: c}`, which declare no type at all; a permitted probe assigning the card to one of its own named results and answering with a bare `return`, which only the named-result clause catches; `p := &c`, `d := (c)`, and `d := *c` followed by `return &d`, which only the operator-reading half of the alias clause catches; a method on `*Card` declared in `bench.go` whose result is the card, bound and then answered, which only the call-receiver clause catches; the seven shapes a review planted against the shipped guard and watched escape: an index target receiving the reader call directly and a star target receiving a tracked card, both caught by the widened target arm; a package-level name receiving the reader call directly, caught because the function does not introduce it; a named result receiving the reader call directly, a spelling the corpus had pinned only in its laundered form; a range clause laundering a tracked card out of a slice; a reader call nested inside a call argument; a call through a local alias of the reader; and package-level storage whose var declaration elides its type, pinned over four spellings of its initializer; and the three shapes this round's own probes planted while arming those clauses: a type-switch guard and a plain type assertion binding the card out of a tracked identifier, which the walk's binding-form claim had to cover and did not until the assertion shape was added, and a tracked card sent on a channel, which pins the send arm the channel-receive residual's defense rests on. Round 3 plants the nine escapes a second review found in the widened guard and eighteen further shapes this round's own sweep found beyond them, each spending exactly one free reference and each pinned by a plant that reddens when its clause is removed: the nine close the reader handed to a call as a value, held by a package-level var, taken from an alias, sent on a channel as a value, called through parentheses and the address-of and dereference operators, laundered by a second assignment target or a second var name, and qualified through parentheses in a file no entry names; the eighteen close the same value class through returns, the three store targets and container keys, composite literals and spelled map literals, a nested append's spread, and rule 2's own parenthesized qualifiers, and they plant the two shapes the existing clauses already closed, an alias handed on as a value and a reader in append's own argument list.