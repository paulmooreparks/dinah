---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:18:43Z
ordinal: 13
note: "Verified 2026-09-12 on head 995636e: go test ./cmd/dinah -run TestEveryCardReferenceComesFromTheRegistry passed in 0.90s over the newBench fixture with the +1000 offset rewrite. The offset set is disjoint from the pre-offset set, asserted first so a shrunk offset cannot make the rest vacuous (that arm was attacked on 2026-09-11 and reddened as designed), and each command's integer set equals its expected set exactly: ls, search card, and tree {1001,1002,1003}, next {1002}, show fx-1002 {1002}, status {1001}, check {1003}; the six commands other than check collected no 12-hex identifier. The recorded attack held too: deleting the third card's number key took 1003 out of ls, next, search, and tree and put the bare identifier into all four, so both halves redden on a wrong build."
---
Every card reference a command prints comes from the registry, and no command prints a bare card identifier where a reference belongs. `go test ./cmd/dinah -run TestEveryCardReferenceComesFromTheRegistry` passes. SETUP: `newBench` (`cmd/dinah/main_test.go:227`), which inits with `--slug fx`, so every reference below is spelled `fx-<number>`; file three cards titled "first card", "second card", and "third card", taking 1, 2, and 3; migrate with `dinah check --migrate-numbers --yes`; rewrite `card-numbers.txt` so each number gains 1000; `carryToDoing` (`:248`) the first card and then `dinah claim fx-1001`, because a claim standing at an intake column is refused with `dinah.takes-no-work`; file one open question on the third card and rewrite its `column` on disk to `deadbeefdead`, so `check` has exactly one defect to report. COLLECTION, over each command's combined stdout and stderr: every match of `(^|[^0-9A-Za-z_-])fx-([0-9]+)` read as an integer, and separately every match of `(^|[^0-9A-Za-z_-])([0-9a-f]{12})([^0-9a-f]|$)`. ASSERTIONS: (1) the offset set and the pre-offset set are disjoint, asserted before anything else so a later hand shrinking the offset cannot make the rest vacuous; (2) the integers each command collects equal its expected set exactly rather than being contained in it, the expected sets being `ls` {1001,1002,1003}, `next` {1002}, `search card` {1001,1002,1003}, `tree` {1001,1002,1003}, `show fx-1002` {1002}, `status` {1001}, and `check` {1003}; (3) the six commands other than `check` collect no 12-hex identifier at all, `check` being excluded from that half because its detail legitimately carries the anchor path and the checklist item's own identifier. SCOPE: each assertion is over a set of references rather than over a sequence, so the criterion says nothing about the order a command prints its cards in and a build that reorders output without changing which references it prints still passes. RUN ON 2026-09-11 against a build of c1ae4a9: each command collected exactly the set above and no hex outside `check`. Deleting the `number:` key from the third card's anchor, which is the state a caller reading through a free card reader produces, took 1003 out of `ls`, `next`, `search`, and `tree`, put the bare identifier `b2503fea1941` into all four, and emptied `check`'s set, so both halves of the assertion redden on a wrong build.