---
kind: decision
state: resolved
owner: holder
ts: 2026-09-14T02:18:45Z
ordinal: 34
note: "Round 6, and it supersedes the sentences D-16's note carries about the admitted hole. The reviewer found that 2c followed a card through an assignment, an alias, and an `append` but into no composite literal, so a permitted probe could bind `h := CardHolder{Card: c}` and answer `h` on the next line while the whole scan reported only the one budgeted reference. Two further spellings of the same gap need no type declaration at all, `cs := []*Card{c}` and `var m = map[string]*Card{id: c}`. Rounds 4 and 5 described that gap as escaping an integer zero rather than a card and as needing type information to close, and both claims were false: the struct's field type is written by whoever writes the struct, so a `*Card` field declared in `bench.go` carries the live card, which was run end to end with a caller in `internal/verb` reading `h.Card.Ref(slug)` off it, and one syntactic predicate closes it. This spec's author then wrote five attacks against the composite-literal repair and all five defeated it: a named result answered by a bare `return`, `p := &c`, `d := (c)`, `d := *c` answered as `&d`, and a method on `*Card` declared in `bench.go` whose result is the card. Each spends exactly one free reference, so rule 1's budget is satisfied and only rule 2 can catch them. Four clauses close all eight shapes. On the tree at `933f0d5` the repaired rules still report thirty-two rule 1 references in nineteen files and exactly one rule 2 hit, at `check.go:288`, so the clauses cost nothing, and with that line converted the same scan reports nothing under rule 2. The taint deliberately stops at those four shapes: an implementation tainting on any expression that mentions a tracked identifier reaches `n := card.Number` and forbids the migration the allowlist exists to permit, which was also run."
---
Rule 2c carries the taint through a composite literal, through the parentheses, address-of, and dereference operators, and through a call whose receiver is the tracked card, and it treats an assignment to one of the function's own named results as an escape.