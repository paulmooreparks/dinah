---
kind: decision
state: resolved
owner: holder
ts: 2026-09-14T02:18:45Z
ordinal: 30
note: "A reference rule's subject is syntactic names, and the property the design needs is that no unstamped card reaches a caller. Round 4 defeated the round 3 guard by exploiting exactly that gap, lifting `check.go`'s one allowed call into an exported method and leaving the file's reference count unchanged. Rule 2 asserts, over the three allowlisted files alone, that the card a free reader answers does not leave the function that read it by any shape the rule enumerates: no declaration answering a `*Card` unless the file's entry names it, no package-level variable or struct field of that type, and no tracked value reaching a return, a selector assignment, or a channel send. The subject of rule 1 widened from `LoadCard` to all three readers because `retiredCardsIn` reaches `loadRetiredCard` and hands unstamped cards to `Bench.Cards` on a lenient-opened workbench today. Both rules were prototyped and run on 2026-09-11 against the tree and against eleven hostile shapes; ten failed.\n\nCORRECTED IN ROUND 6 BY D-20. The last three sentences of this note as it stood were wrong. The eleventh shape, a permitted function copying the card's fields into a local struct and answering that struct, does not escape an integer zero: the struct's field type is whatever its author writes, so a `*Card` field declared in a file rule 2 never inspects carries the live card to every caller in the module, which was run end to end in round 6 with a caller in `internal/verb` reading the card's rendered reference. Closing it needed syntax rather than type information, and it is closed, along with seven further shapes round 6 found. The corpus now stands at twenty-three shapes and the rules report all twenty-three. The claim in this card that its one remaining hole is named rather than papered over is therefore superseded: the guard now reports every shape anyone has written against it, and what the guard does not reach is stated in the spec as a property of an enumeration rather than as a named hole."
---
The guard has two rules rather than one: references to the three free card readers, and an escape rule over the allowlisted files. What the escape rule does not reach is stated as the limit of an enumeration rather than papered over, and D-20 supersedes this decision's earlier claim that one named hole remained.