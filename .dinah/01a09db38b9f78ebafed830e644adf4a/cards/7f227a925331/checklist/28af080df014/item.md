---
kind: decision
state: resolved
owner: holder
ts: 2026-09-14T02:18:44Z
ordinal: 21
note: "duplicate (two lines, one number), repeated (two lines, one card), missing (a card with no line), stranded (a line naming no card and no tombstone), malformed (a line that does not parse), and in-frontmatter (a `number:` key surviving at format 3). Two of those the card did not name, and both are reachable: the registry is hand-editable text, and leaving a `number:` key behind restores the two-stores hazard this card exists to remove. One key per condition because the repairs differ, and a sentence naming several repairs sends most of its readers to the wrong command. `check.ordinal-missing` stops covering cards for the same reason."
---
The card asked whether the three registry defects are one finding or three. They are six findings, each with its own repair.