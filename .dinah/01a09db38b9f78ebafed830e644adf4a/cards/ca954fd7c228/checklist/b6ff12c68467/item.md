---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:24Z
ordinal: 14
note: "citation: scheme=test, target=internal/msg/msg_test.go#TestATranslationKeepsThePlaceholdersAndTheSplice, observed before=fail after=pass. The loop now reads Tags() with Base skipped instead of Complete. The decisive arming was run both ways: with a placeholder dropped from cs.json (a skeleton, byte-identical English today), the trunk copy of msg_test.go passed (exit 0, the guard never looked at cs), and this branch's copy failed with `cs/answer.pull.empty.named: wanted the placeholder {upstream}, got \"Dinah found nothing ready in  to move into {destination}\"`. The pair count rose from 724 under Complete to 2,534 under Tags(), measured at dbe5103. SUPERSEDED IN PART at 3c795b3, on the operator's code-review ruling: the single counter this criterion asks for is now two, one per population, each with its own floor, because the loop walks two populations (English naming a placeholder, English opening with the splice) and en.json carries 48 splice-only keys, enough to hold a combined counter above zero while the placeholder half reads nothing. The zero-pairs arming that gave \"no key pair was compared\" is replaced by two armings, each emptying one population alone: narrowing the placeholder pattern to match nothing gives \"no pair whose English names a placeholder was compared, so the placeholder half of this guard is asserting nothing\" with the splice floor silent, and changing the splice prefix to one no entry carries gives the splice floor's own message with the placeholder floor silent. The same placeholder plant against the pre-split copy of the file passes, which is the case for the split. The floors are t.Error rather than t.Fatal so that each reports independently."
---
`TestATranslationKeepsThePlaceholdersAndTheSplice` in `internal/msg/msg_test.go` walks every shipped catalogue rather than the three on `Complete`: its loop reads `Tags()`, which returns the base plus the seven siblings. It counts the key pairs it compared and calls `t.Fatalf` when that count is zero, matching the floor `TestATranslationInventsNoPlaceholder` carries, since a walk widened without a floor is the vacuous check this whole card is about. Armed by dropping a placeholder from an entry in a skeleton catalogue (`internal/msg/locales/cs.json`, whose entries are byte-identical English today) and watching this test go red where it was green before the change, and by emptying a catalogue to prove the zero-pairs fatal fires. Against the unmodified tree it fires zero times, and the pair count it reports is larger than the count the same test reports on trunk.