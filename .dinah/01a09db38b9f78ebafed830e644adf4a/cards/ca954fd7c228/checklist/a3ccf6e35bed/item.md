---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:24Z
ordinal: 11
note: "citation: scheme=test, target=internal/msg/msg_test.go#TestATranslationInventsNoPlaceholder, observed before=fail after=pass. One subtest per tag over Tags() with Base skipped, two floors per subtest (pairs, and placeholder names read off that tag's translations), then the invented findings. It reads placeholderPattern, which is now a package-level var the sibling test compiles too, so \"the same pattern\" is literal rather than a copy. Armed three ways, each in isolation: {ziel} added to a German entry reported `de/answer.pull.empty.named: {ziel}` and failed the de subtest alone; de.json emptied failed the de subtest alone on \"de shares no key with the base catalog, so this guard compared nothing\"; and narrowing placeholderPattern to match nothing failed on \"af carries no placeholder at all, so this guard read nothing\", which is the floor a pairs count alone would have ridden past. Measured at this commit: 6,559 pairs, 2,807 placeholder names on the translation side, zero fires, both figures reproducing the spec."
---
`internal/msg/msg_test.go` carries `TestATranslationInventsNoPlaceholder`, reporting every placeholder a translation carries that its English does not, using the same placeholder pattern `TestATranslationKeepsThePlaceholdersAndTheSplice` compiles. It runs one subtest per tag over `msg.Tags()` with `Base` skipped, and each subtest counts two populations of its own and fails on either at zero: the key pairs it compared, and the placeholder names it read off that tag's translations. Two counters rather than one, because the direction this test reports reads the translation side, and a pairs count stays high while the side being read goes empty. Armed three ways, each in isolation: adding `{ziel}` to a German entry in `internal/msg/locales/de.json`; emptying `de.json`'s entries so that tag's pairs floor fires while the other six subtests stay green; and narrowing the placeholder pattern to match nothing so the placeholder floor fires rather than the run riding the pairs count. Against the unmodified tree the seven subtests compare 6,559 pairs and read 2,807 translated placeholder names, and fire zero times.