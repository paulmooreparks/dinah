---
kind: decision
state: resolved
column: c9428b3bc921
owner: holder
ts: 2026-09-14T02:16:36Z
ordinal: 17
note: "No, and the reasoning belongs on the record because the gap is real and somebody will ask again.\n\nThe defect this cycle repaired is a flag name spelled in a doc comment that the flag tables no longer hold. Nothing detects that class today. TestTheGuidesTeachOnlyDeclaredFlags covers the shipped guides and docs/quick-start.md, and it works because a guide's invocations have a grammar: it matches `dinah <command> ... --<flag>` and resolves the command before it judges the flag. Go comments have no such grammar.\n\nI read what a comment-scanning guard would have to accept in one file to see the shape of it. cmd/dinah/args.go alone carries three double-dash words that are not flags and must not be: `--name` in walkFlags's onUnknown paragraph and again in declaredFlags, where it stands for whatever spelling a positional's own row gives; `--word` in the same paragraph, standing for any unrecognized word; and `--basis`, in a comment whose whole point is that no such flag exists. A guard therefore needs an exception roster from its first day, that roster spans files this card does not touch, and it grows with every placeholder anybody writes. The corpus already warns about a hand-maintained roster that a later edit empties, and an exception roster is the weaker case of that, since a name added to it is never checked again by anything.\n\nThe cost of building it here is also wrong for this card. Running it across cmd and internal would redden comments in files this diff does not own, and repairing those is another card's work rather than a widening of this one. Implement's own instruction is that a problem bigger than the card is filed rather than folded into the diff.\n\nSo the gap is named instead. A guard worth having would judge only a comment that spells a full invocation, `dinah <command> --<flag>`, which is the shape that made a false claim here and the shape the guides guard already parses; a bare `--name` in a sentence would stay out of its reach. That is a card of its own, and I have no grant to file one from this stage, so it is written here for the operator's station to pick up.\n\nWhat this card does instead is remove the thing a guard would have caught. The doc comment now names no flag at all, and it says why, so the next editor is told rather than trusted."
---
Should this card add a guard holding flag names spelled in Go comments against the declared flag tables, the way TestTheGuidesTeachOnlyDeclaredFlags holds the guides?