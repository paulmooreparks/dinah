---
kind: decision
state: resolved
column: c9428b3bc921
owner: holder
ts: 2026-09-14T02:16:35Z
ordinal: 15
note: "Yes, it is load-bearing now, and it was not before. Before this card the askedFor branch of parseArgs's loop read `parsed.flags[asked] = \"\"` and continued, with no capture code on that path at all, so no edit to any table could have produced a domainCapture for help or version. The refactor routes both through the same visit callback every other flag reaches, and the only thing between them and parsed.domainCaptures now is sessionFlagNames carrying the two names. The guarantee changed kind, from a property of the code's shape to a property of a lookup table, which is why AC-9's original exclusion of the two names rested on reasoning the diff had already invalidated. WHAT HOLDS IT, first half: TestParseArgsRecordsNoDomainCaptureForASessionFlag (cmd/dinah/args_test.go:238) names help and version beside the other five, and the assertion is args_test.go:257. Armed by replacing the `if !session` gate at args.go:318 with `if true`, which is the future change the guard exists to catch. All seven subtests go red, help and version included, each on the capture assertion rather than on a fatal, reporting `--help was recorded as a domain capture (1 of them), which puts it within reach of resolveOpenTailFlags`. On the previous five-name roster the same break left both names unexamined. Second half: the capture-with-no-tokens shape the reviewer produced is removed at source rather than excluded by the table. walkFlags's askedFor branch called visit with nil tokens, which contradicted the walk's own documented contract that tokens are the argv words the occurrence consumed, since `-h` did consume a word. It now passes []string{word} at args.go:223. Even were help to leave sessionFlagNames, the resulting capture would carry the word the caller wrote, and resolveOpenTailFlags would splice it back into positional text the way it splices every other rejected capture, rather than deleting the flag and putting nothing back. TestWalkFlagsGivesEveryOccurrenceTheWordsItConsumed (args_test.go:292, assertion at args_test.go:305) asserts every occurrence the walk reports carries at least one token, and it observes the askedFor branch whatever sessionFlagNames says. Armed by restoring the nil, at which point it names both flags: `walkFlags reported --help with no tokens, so a caller splicing tokens back into the line would lose the word the caller wrote`. WHY NOT AN EXPLICIT REFUSAL: a runtime refusal in parseArgs against a capture with no tokens would be unreachable the moment the walk stops producing that shape, and unreachable defensive code decays unread. Making the shape impossible in the walk and asserting its impossibility in a test guards the same thing without adding a branch nothing can execute. (An earlier save of this note carried two stray XML closing tags at its end, from a tooling slip on my side; this text supersedes it.)"
---
Is sessionFlagNames load-bearing for help and version after the walkFlags refactor, and if it is, what holds it?