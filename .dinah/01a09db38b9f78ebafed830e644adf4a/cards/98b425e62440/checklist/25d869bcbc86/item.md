---
kind: decision
state: resolved
column: c9428b3bc921
owner: holder
ts: 2026-09-14T02:16:36Z
ordinal: 16
note: "Read it out of the tables. What these tests assert is that walkFlags places a --lang standing in another flag's value slot as that flag's value, and any valued flag other than --lang itself demonstrates that, so the flag's identity carries no meaning and naming one is a fact the test does not need. Swapping \"--state\" for \"--column\" would rebuild the same trap: the next rename breaks it again, and it breaks it silently, because a test naming an unrecognized word still compiles and still passes while the scenario it exists to exercise has stopped happening. An unknown word claims no value slot, so there is nothing for the following --lang to sit in, and the test then asserts that scanLangFlag reads an ordinary --lang, which every other case already covers.\n\nThe mechanism is exampleValuedFlags in cmd/dinah/args_test.go. It walks valuedFlags, the same slice declaredFlags builds from every command's parameter table and the global flags, and returns two picks: the first valued flag sessionFlagNames does not hold, which is a domain flag so parseArgs records a domainCapture for it, and the first valued session flag other than lang. Both exclude lang, since a --lang in its own value slot asks a different question. On this tree the picks are card and actor.\n\nThe two objections the operator named were weighed and neither holds here. A derived flag could be the flag under test, so lang is excluded by name and the exclusion is the first thing the loop does. Reading the table could make the test's intent unreadable, so each subtest name is composed from the pick (\"lang in card's value slot is card's value\") and every failure message carries the argv or the flag name, which leaves a failure at least as readable as the literal version and adds the fact of which flag the run chose.\n\nThe helper cannot go quiet. It fails the test outright when either pick comes up empty, at the t.Fatalf reading \"no example valued flag to read a value slot with\", naming valuedFlags and both picks, so a table that stops declaring a valued domain flag reddens rather than skipping.\n\nArmed twice, and both armings reproduced at Implement on cycle 5's tree. Reverting scanLangFlag to the pre-fix blind pattern match reddens the derived cases naming --card and --actor, so the derivation did not cost the test its teeth; it also reddens the agreement test on four lines and both rendering tests in main_test.go.\n\nCOUNT CORRECTED at Implement, cycle 5, on Agent Code Review's cycle-4 nit. This note said that forcing the domain pick back to the retired name \"state\" reddens three test functions. It reddens five, and I reproduced it rather than taking the reviewer's number: TestScanLangFlagReadsOnlyALangThatIsAFlag (three cases), TestScanLangFlagAgreesWithParseArgsOnASuccessfulParse (\"wanted no error, got dinah.usage: --state\"), TestParseArgsRecordsNoDomainCaptureForASessionFlag (the AC-9 control, \"--state is not a valued domain flag in the map handed to parseArgs\"), TestWalkFlagsGivesEveryOccurrenceTheWordsItConsumed (\"reported 5 flag occurrences, wanted 6\") and TestLangFlagIsHonouredWhateverItsPosition (\"dinah --nosuchflag --state --lang de printed the wrong refusal\"). The understatement was in the safe direction and the decision it supports is unaffected, so this corrects the record rather than reopening the question. The two line-number citations this note used to carry, args_test.go:38 and args_test.go:55, are replaced above by the helper's name and by its fatal's own message, for the reason AC-6 and AC-10 give."
---
Should the value-slot tests name a second flag, or read one out of the declared flag tables?