---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:05Z
ordinal: 3
note: "Verified at 4a8c66a. Test: internal/bench/check_test.go, TestCheckReportsAnAttachmentsDirectoryUnderAKindThatMountsNone, on the shared strayFixture helper added beside it. Command: go test ./internal/bench/ -run TestCheckReportsAnAttachmentsDirectoryUnderAKindThatMountsNone. Green. The comparison is an exact path-to-kind map against the three planted directories, not a count. Three plants, each leaving the tree compiling. (1) The kinds named instead of asked: `if kind == KindItem || kind == KindAttachment`. Red at check_test.go:2315 \"wanted 3 findings, got 2\" and :2320 \"nothing reported ...\\workstreams\\a00000000001\\attachments\". (2) Delete instead of report: os.RemoveAll before the finding. Red at check_test.go:2337 three times, \"check removed ...\\payload\\evidence.txt, and it may not remove anything\", which is dinah-456 D-14. (3) The directory tested before MountOf is asked: red at :2315 \"wanted 3 findings, got 5\" and twice at :2329 naming the card's own and the comment's own attachments directories, \"was reported, and its kind mounts attachments\". All three restored byte-identically and green again. Live confirmation outside the suite: a stray planted by hand under a checklist item in a throwaway workbench at C:/dinah-scratch/dinah-459-impl/probe made `dinah check` print \"item carries an attachments directory, and Dinah keeps no attachments on item, so nothing reaches the files inside it (<path>)\" with exit 5, and the payload file was still on disk afterwards."
---
AC-3. `internal/bench/check_test.go`, `TestCheckReportsAnAttachmentsDirectoryUnderAKindThatMountsNone`: a fixture plants an `attachments` directory holding one attachment beneath a checklist item, beneath a card's own attachment, and beneath a workstream, plus a legitimate `attachments` directory beneath the card and beneath a comment. `Bench.Check` returns exactly three findings whose `Key` is `check.attachments-without-a-mount`, whose `Detail` values are exactly the set {`item`, `attachment`, `workstream`}, and whose `Path` values are exactly the three planted directories. Every planted payload file still exists after the call, and no finding of that key names the card's or the comment's own attachments directory.