---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:06Z
ordinal: 6
note: "Verified at 4a8c66a. Test: cmd/dinah/attachments_command_test.go, TestTheNotAttachableRefusalPrintsTheAdviceForItsKind, three subtests. Command: go test ./cmd/dinah/ -run TestTheNotAttachableRefusalPrintsTheAdviceForItsKind -v. Green. Each case asserts the stderr prefix contract.NotAttachable, the base sentence rendered by msg.For(msg.Base).T(\"refusal.dinah.not-attachable\", ...), the presence of its own splice, and the absence of the other two, each named as a literal catalog key. Plant: the item fragment's When swapped from \"item\" to \"attachment\" in contract.Shapes. Red on the item and the attachment cases, and green on the workstream case, exactly as the criterion's note predicts, since only the \"and neither other\" side can see the difference. Restored byte-identically and green again. Live confirmation at the terminal in a throwaway workbench: `dinah attach probe-1/oq/1 f.txt` prints \"dinah.not-attachable probe-1/checklist/1 is item, and Dinah keeps no attachments on item; attach the file to the card instead, then cite it from probe-1/checklist/1 with `dinah cite`\", the attachment reference prints the attachment splice, and `dinah attach workstream/probe-stream f.txt` prints the unconditional splice with the bare slug in the detail."
---
AC-6. `cmd/dinah/attachments_command_test.go`, `TestTheNotAttachableRefusalPrintsTheAdviceForItsKind`: running `dinah attach` against a checklist item prints stderr beginning `dinah.not-attachable`, containing the rendered `refusal.dinah.not-attachable` sentence, containing the rendered `refusal.dinah.not-attachable.next-item`, and containing neither of the other two next steps. The attachment reference prints the attachment splice and neither other, and the workstream reference prints the unconditional splice and neither other. The three expectations are rendered through `msg.For(msg.Base).T(<key>)` naming each key as a literal, so the test follows these sentences rather than any others.