---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:06Z
ordinal: 10
note: "Verified at 4a8c66a. internal/guide/guides/references.md now carries the spec's replacement word for word: \"`attach` takes a comment below a card, and it takes an attachment only with `--replace`, which replaces that attachment's bytes rather than hanging a new file below it. It takes nothing else below a card, so `dinah attach wb-1/oq/1 notes.md` is refused.\" First search, grep -rn \"takes a comment or an attachment below a card\" . , returns nothing at all (exit 1), which is this criterion as corrected: that clause stood only in the guide, and the review's finding on the original wording is what the correction records. Second search, grep -rn \"a comment or an attachment below a card\" . , returns eight hits and no others: cmd/dinah/main_test.go:6883 and :7480 (the param.attach.ref summary and the wrapped-line assertion of TestTheArgumentsTableWrapsAndNoOtherTableMoved, neither of which reads the guide), docs/specs/dinah-172-help-ux-sketch.md:72 (a dated design document), and the param.attach.ref entries in en, af, cs, es, fil and id. Those catalog entries already qualify the attachment case with \"with --replace\", so the guard makes them more true rather than less, and de and hi do not appear because their entries are translated. All eight are unchanged in this diff."
---
AC-10. `internal/guide/guides/references.md` carries the replacement sentence in section 6 of the spec word for word, and a search of the whole repository for the superseded clause `takes a comment or an attachment below a card` returns no hits at all once the guide is edited, that clause having stood only in the guide. A search for the shorter clause `a comment or an attachment below a card` returns hits only in `cmd/dinah/main_test.go`, `docs/specs/dinah-172-help-ux-sketch.md`, and the `param.attach.ref` entries of the English and the five skeleton catalogs, which describe the parameter rather than the guide and stay unchanged. Record both search outputs on the card.