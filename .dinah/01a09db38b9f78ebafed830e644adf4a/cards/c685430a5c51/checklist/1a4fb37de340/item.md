---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:06Z
ordinal: 9
note: "Run at 4a8c66a from the repository root of the worktree C:/dinah-scratch/dinah-459-impl/wt. First search, grep -rn 'Kind[[:space:]]*[:!=]=\\?[[:space:]]*\"' --include=*.go . | grep -v _test.go, seven hits, none an entity kind: internal/bench/card.go:223 and :232 (a transition's Kind field compared to the empty string), internal/bench/check.go:147 (the FindingUnknownKind constant), internal/bench/entity.go:927 (a checklist item's kind, \"open_question\" and \"decision\"), internal/verb/checklist.go:195 (the criterionKind constant), internal/verb/mutate.go:582 (card.BlockKind cleared), internal/verb/query.go:33 (the FieldBlockKind query field name). Every entity-kind literal is gone, including from internal/bench/containment.go, whose constants are now the only place any of them is spelled. Second search, grep -rn '\"workstream\"' --include=*.go . | grep -v _test.go, twenty-four hits: the command name in cmd/dinah/commands.go and main.go, internal/mcp/mcp.go and tools.go, the table headings in cmd/dinah/render.go, the JSON tags in internal/bench/workstream.go and internal/verb/beyond.go, the shape variants in internal/contract/shape.go, the scope constant in internal/verb/changes.go, the checks and definition parameter tables in internal/verb, the query field name and the search kind, plus the doc comment at internal/verb/search.go:94, and the one entity-kind spelling that is now the constant itself at internal/bench/containment.go:25. Correction to the spec, recorded rather than absorbed: section 5 enumerated seven sites, and the first search names fourteen entity-kind literals in beyond.go and entity.go, because it also catches the \"workbench\", \"column\", \"card\" and second \"attachment\" comparisons the spec's grep for \"workstream\" could not see. All were converted, which is what this criterion actually asks for. This criterion is weaker than a test, and its own note says so: nothing stops a later file from reintroducing a literal, and widening internal/bench/kindguard_test.go to entity kinds remains a card of its own."
---
AC-9. No production Go file decides an entity kind by naming it as a string literal. Run, from the repository root, `grep -rn 'Kind[[:space:]]*[:!=]=\?[[:space:]]*"' --include=*.go . | grep -v _test.go` and `grep -rn '"workstream"' --include=*.go . | grep -v _test.go`, record both outputs on the card, and confirm that no hit outside `internal/bench/containment.go` compares or assigns an entity kind to a literal. The remaining `"workstream"` hits are the command name, the JSON tags, the query field name, the search kind, and the parameter tables.