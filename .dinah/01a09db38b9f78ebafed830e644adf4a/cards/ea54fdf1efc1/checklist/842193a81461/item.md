---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:09Z
ordinal: 9
note: "Two independent checks, and the guide half is the one that can fail because the table is typed by a person and the expectation is read from the code. Plant that reddens the guide half: delete the `notes` cell from the workstream row of the guide's table. The parse still succeeds, and the run reports that `bench.FieldsOf(\"workstream\")` carries `notes` and the guide's workstream row does not. Section 9.1 rules that the table is held rather than generated, and says why: a generated table compared against its own generator passes on every input, which would make this clause unable to fail. Plant that reddens the refusal half: fill the refusal's `fields` value from `bench.AllFields()` instead of from `FieldsOf(entity.Kind)`. It compiles, the refusal still renders, and the run reports that the card's sentence names `text`, which belongs to an item. What the cross-kind negative does and does not buy is worth stating plainly, because an earlier version of this note overstated it. It defeats a raise site listing `AllFields()` everywhere. It does not defeat a wrong `FieldsOf`, because this criterion's expectation and the raise site's value both come from `bench.FieldsOf(kind)`. What pins the content of `FieldsOf` is AC-1's two-directional `fieldSamples` hand table, and the same sentence belongs in the test's doc comment so the next reader does not read this note as covering it."
---
A reader discovers a kind's field names without guessing, at both heads and in the guide. `cmd/dinah/fields_discovery_test.go`, `TestAKindsFieldSetIsPublishedRatherThanGuessed`: `dinah set <card> frobnicate x` refuses `dinah.unknown-field` and its sentence names every member of `bench.FieldsOf("card")` and none of `bench.FieldsOf("item")`, while `dinah set <item> frobnicate x` names every member of the item's set and none of the card's; and the `set_field` tool's published schema declares its `field` argument's enum as `bench.AllFields()`. Separately, `cmd/dinah/references_guide_test.go`, `TestTheReferencesGuideNamesEveryFieldOfEveryKind`: the references guide's per-kind field table is hand-written markdown, parsed the way `parseReferencesGuideTable` at line 220 parses the command table, and it carries one row per kind in `bench.EntityKinds()`, each naming that kind's fields. It fails on a kind the table draws no row for, on a row naming a kind `EntityKinds()` does not carry, on a name in `bench.FieldsOf(kind)` the row omits, and on a name the row carries that `FieldsOf(kind)` does not, reporting kind, field and which side is missing it. It is fatal when the table draws no row.