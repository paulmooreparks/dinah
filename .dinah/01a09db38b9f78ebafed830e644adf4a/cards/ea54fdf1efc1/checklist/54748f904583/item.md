---
kind: decision
state: resolved
column: 0d86ad99cdbc
ts: 2026-09-14T02:18:09Z
ordinal: 13
note: "`bench.Workstream.Notes` is the anchor's body, it is prose a person wrote, and `dinah edit workstream/<ref>` is the only thing that reaches it today. That is the exact asymmetry this card exists to close, and the parent's own section 5.1 rules prose in while its section 5.3 table lists only the three frontmatter keys. Shipping the card that closes the prose gap with one prose field still behind the editor would be a worse outcome than adding a row. Spec section 3.4 states it."
---
A workstream's notes are settable, so the workstream field set is `title`, `slug`, `status`, `notes` rather than the parent's three.