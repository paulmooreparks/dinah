---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:09Z
ordinal: 5
note: "Asserting the identifier rather than the event name alone is what makes this fail on a wrong value rather than only on a missing one: an implementation writing `comment_updated` with the card's identifier in `note` passes a presence check and fails this. Plant that reddens it: in the comment arm, pass `entity.Card.ID` where the event's `Note` is filled instead of `entity.ID`. It compiles and the run reports the note it found beside the comment identifier it expected. The below-a-column attachment arm is a separate subtest because `journalFor` at `internal/verb/beyond.go:643` routes it to a different journal, and an implementation that always reached for the card's journal would have passed the first three arms and panicked or written nowhere on this one; its plant is to make the arm's assertion read the card journal, which then reports the workbench line missing. The column arm exists because `column_updated` already exists and only changes shape, which is the case no other criterion on this card covers. The three fixture-side checks are named beside it because a new event constant that never reaches a capture turns the build red in a different package, and `TestTheSampleFixtureCarriesEveryShapeThisBuildWrites` is what catches the changed `column_updated` member list; a criterion that stopped at the journal line would leave the implementer to discover both at Test."
---
A write below a card is journalled with the written entity's own event, naming that entity, and the format document agrees with the tree. `internal/verb/fields_journal_test.go`, `TestAWriteRecordsTheWrittenEntitysOwnEvent`: for a comment, an item and an attachment below a card, a `set` writes exactly one new line on the card's journal, that line's `event` is `comment_updated`, `item_updated` or `attachment_updated` respectively, its `note` is the written entity's own identifier, its `field` is the field written, and `from` and `to` carry the previous and new values. A fourth subtest writes a field of an attachment hanging below a column, and asserts that line lands on the workbench's journal, carries `attachment_updated` with the attachment's own identifier in `note`, and that no card's journal grew, which is the other half of section 5.1's `attachment_updated` row. A write to the card's own `severity` writes `card_updated` carrying `field` and no `note`. A write to a column's own `title` writes `column_updated` on the workbench's journal carrying `note` and `field` together. A write to an attachment's `filename` writes `attachment_renamed` rather than `attachment_updated`. Alongside it, `python scripts/derive_event_counts.py` exits 0, and `go test ./cmd/dinah/ -run 'TestEveryEventACardJournalCarriesIsOneAQueryCanName|TestReplayingThePopulationSequenceReachesEveryShapeItNames|TestTheSampleFixtureCarriesEveryShapeThisBuildWrites'` passes against the recaptured fixture.