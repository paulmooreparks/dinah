---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:09Z
ordinal: 6
note: "Every subtest pins an accepting case beside its refusal, so a build that refuses every write fails all seven rather than passing them. The filename subtest asserts the payload moved rather than that the write returned ok, which is the assertion that separates a route through `Library.Rename` from a direct frontmatter write. Plant that reddens it: replace the `filename` arm's call to `Library.Rename` with a plain frontmatter write of the new name. It compiles, the write succeeds, and the run reports that `<ref>/payload` no longer resolves. A false failure would come from asserting the refusal sentence, so each assertion reads the refusal name alone."
---
Every guarded field refuses the value its guard rejects and accepts the value its guard admits, and the guard's own side effect is observed. `internal/verb/fields_guard_test.go`, `TestEveryGuardedFieldRefusesAndAcceptsAtItsOwnGate`: one subtest per declared guard, each running a refused write and an accepted write against the same entity. `slug` refuses a malformed slug with `malformed` and refuses a well-formed one with no `--yes` under `dinah.unconfirmed`, then accepts it with `--yes` and the reference resolves under the new slug. `level` refuses an undeclared level under `dinah.unknown-level` and accepts a declared one. `tier` accepts `+1` with `--at` and records `tier_overridden`. `state` refuses `frobnicate` under `dinah.unknown-value`, refuses `verified` on an open question under `dinah.wrong-item-kind`, and accepts `verified` on a pending acceptance criterion carrying a citation, writing `item_verified`. `filename` refuses a name `bench.ValidAttachmentName` rejects and accepts a legal one, after which `dinah path <attachment>/payload` resolves and the payload's bytes are unchanged. `column-kind` refuses a kind outside `contract.Kinds` and accepts one inside it. `capacity` refuses `0` and `x` and accepts `3`. The run is fatal when the subtest count is not the length of the declared guard set.