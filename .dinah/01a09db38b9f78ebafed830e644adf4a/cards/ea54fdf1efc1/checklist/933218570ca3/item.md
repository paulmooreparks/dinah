---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:09Z
ordinal: 10
note: "The Go half is why this criterion changed in round 2. Section 9.3 names four hits that describe live behaviour, two of them doc comments at `internal/verb/raise.go:24` and `internal/verb/tier.go:12`, and the walk as first drafted reached neither, so the spec told the implementer they needed no edit and offered no check that would have caught them. Plant that reddens the Go half specifically: restore the words `card set` to `internal/verb/tier.go`'s doc comment. The file still compiles, the run names the file, the line and the spelling. Plant that reddens the document half: restore one retired line to `docs/quick-start.md`. Two file counts rather than one, because a single total is satisfied by the documents alone and would hide a Go walk that read nothing, which is the empty-subject-set shape this board keeps catching. False failures are the eight unrelated word pairs section 9.3 lists, of the shape \"the workbench setting's\" and \"the card set\", and the allowlist is what they are for: one entry and one reason each, so a ninth is argued rather than silently matched. Test files stay outside the walk deliberately, because they invoke the retired spellings as arguments in the dozens and the sweep reaches them through the counted call sites instead."
---
No retired spelling survives in anything the binary ships or the project publishes, including its Go source. `cmd/dinah/retired_spellings_test.go`, `TestNoRetiredSpellingSurvivesInTheShippedCorpus`: the check walks `internal/guide/guides/`, `internal/msg/locales/`, `docs/quick-start.md`, `docs/design/`, the embedded help text, and every non-test `.go` file under `internal/` and `cmd/`, and fails on any occurrence of `card get`, `card set`, `workbench get`, `workbench set`, `workstream get` or `workstream set` outside a declared allowlist file, each allowlist entry carrying the reason the occurrence is legitimate. It reports how many files it read and is fatal when that number is zero, and separately fatal when the number of `.go` files it read is zero, because a Go walk whose root moved reads nothing while the document walk keeps the total non-zero. Beside it, `go test ./cmd/dinah/ -run TestQuickStart` passes, which is what holds the replayed transcripts to live output.