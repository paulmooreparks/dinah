---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:18:12Z
ordinal: 42
note: "The refusal carried a variant each for `workbench` and `workstream`, selected by the command word. Both slug changes are raised by `set` now, so the selector had nothing left to select on and both sentences would have collapsed onto whichever variant was declared.\n\nWhat separated them was a fact about the kind rather than about the command: a workbench slug change renames every card in the workbench, and a column's or a workstream's does not. So the shape declares one `set` variant carrying the sentence both kinds share, and the workbench clause rides as a fragment the raise site switches on. No refusal name is minted and no meaning is lost.\n\nThe spec did not foresee this, because it names the retirement of the two commands without naming the refusal whose variants were keyed to them."
---
`dinah.unconfirmed` selects on a spliced clause rather than on two command variants, because one command now raises what two used to.