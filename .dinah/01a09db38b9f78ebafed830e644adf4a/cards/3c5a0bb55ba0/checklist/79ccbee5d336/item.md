---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
owner: holder
ts: 2026-09-14T02:17:55Z
ordinal: 10
note: "VERIFIED. internal/mcp/collection_reference_test.go TestTheMachineHeadAnswersACollectionAsTheTerminalDoes. Run: `go test ./internal/mcp/ -run TestTheMachineHeadAnswersACollectionAsTheTerminalDoes -v`. The served subset is derived at this commit by intersecting verb.ReferenceTakingCommands() with this head's own tools slice, asserted to hold thirteen, and path and edit are each asserted both to be named in toolExemptions and to be absent from the published tools. All thirteen are called over the head with a collection reference. show answers a collection member whose two members carry fx-1/comments/1 then fx-1/comments/2 in creation order, contents answers a tree whose root kind is collection, attachments answers a listing of kind collection, and the other ten answer a refusal named dinah.is-a-collection. The run logs \"thirteen tools called: 3 answered, 10 refused with dinah.is-a-collection\" and asserts both counts. ONE DEVIATION FROM THE SPEC'S PREDICTION: the criterion says the subset is the fifteen minus what toolExemptions names, and that arithmetic is right about the count but a tool's published NAME is not always its command's (cite is served as cite_item, and the checklist verbs take the reference under an argument called item rather than ref), so the sweep reads the name and the arguments off the head's own declarations rather than assuming the command name. ARMED: made readShow's collection arm answer the text member instead of the collection member; the plant compiled and ran, and the assertion at collection_reference_test.go:111 reddened with \"the show tool answered a collection reference without a collection member\" and the answered count fell to 2; restored byte-identically, green."
---
The MCP head answers a collection reference as the terminal does. The served subset of the fifteen is derived at the commit under test as the fifteen minus the commands internal/mcp/tools.go names in toolExemptions, the run asserts that subset has thirteen members and that the two excluded are path and edit, and every one of the thirteen is called over the MCP head with a collection reference. show answers a payload carrying a collection member with the members in creation order, contents answers a tree whose root kind is collection, attachments answers the holder's listing, and the remaining ten answer a refusal whose name is dinah.is-a-collection. The run reports the counts 3 and 10 and asserts each.