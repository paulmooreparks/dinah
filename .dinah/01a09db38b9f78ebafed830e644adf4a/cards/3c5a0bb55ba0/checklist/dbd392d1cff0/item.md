---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
owner: holder
ts: 2026-09-14T02:17:55Z
ordinal: 13
note: "VERIFIED. internal/verb/collection_roster_test.go TestTheDocCommentsThisCardFalsifiesWereCorrected. Run: `go test ./internal/verb/ -run TestTheDocCommentsThisCardFalsifiesWereCorrected -v`. It reads tree.go and read.go from disk, asserts its subject set holds three comments, greps each stale fragment, and reports a per-comment verdict; it logs \"three comments read, 0 stale claims survived\". Each comment is also required to carry a positive statement about a collection, so a diff that deleted the stale sentence without saying what the field now carries fails as well. (1) TreeNode.Kind now reads \"workbench, column, card, comment, item, attachment, a dotted extension kind, group, or collection. Every value but group and collection names an entity kind of the format.\" (2) AttachmentListing.Kind now says the value is verb.KindCollection where the reference named a whole collection that hangs no attachments of its own. (3) AttachmentListing.Ref now says a collection reference that hangs nothing carries itself as the reader typed it. ARMED: restored TreeNode.Kind's comment byte-for-byte to its b825059 wording; the plant compiled and ran, and the assertion at collection_roster_test.go:103 reddened with \"tree.go's comment on TreeNode.Kind still claims what this card falsifies\"; restored byte-identically, green."
---
The three shipped doc comments this card falsifies are corrected in the same diff, and all three are checked rather than any. Each quoted fragment below sits whole on one source line at b825059, so it is greppable as written; none of the three sentences it belongs to does, because all three wrap. (1) internal/verb/tree.go, TreeNode.Kind, carries the fragment `Every value but group` and the sentence it opens claims every value but group names an entity kind of the format, which verb.KindCollection falsifies; at the commit under test the comment names collection as a second such value. (2) internal/verb/read.go, AttachmentListing.Kind, carries the line `// Kind is the entity's kind, as the containment grammar spells it.`, which section 5.4 falsifies by setting the field to verb.KindCollection for a collection holding no attachments; at the commit under test the comment says what the field carries for a collection reference. (3) internal/verb/read.go, AttachmentListing.Ref, carries the fragment `Ref is what a person types to reach the entity the attachments hang`, which section 5.4 falsifies by setting the field to the collection reference as typed; at the commit under test the comment covers that case. The run names the three comments it read and asserts three, and reports for each whether the stale claim survives, so a diff that corrected none reports three survivals rather than passing quietly.