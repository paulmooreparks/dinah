---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:17:55Z
ordinal: 17
note: "Two points, both forced by machinery the parent's draft could not see. The composer fills {detail} from a refusal's own detail, so a shape declaring {ref} beside it would give one value two names; dinah-459 made the same substitution when it landed dinah.not-attachable, whose shipped English reads \"{detail} is {kind}\", and the parent recorded that shipped shape as the authority over its own draft. And checkNoPlaceholderIsStrayOrOrphaned in internal/profile/guards_test.go fails a Shape that declares a value no entry of its own carries, so declaring count would redden the build; passing it in the named-value map without declaring it is what puts it in the machine payload's context, which is what the parent asked for. Verified that an undeclared named value does reach the payload: outcomeValues in cmd/dinah/render.go copies the whole context map forward."
---
The refusal's sentences carry the reference as {detail} rather than as the {ref} the parent's draft wrote, and the member count rides as a context value that no sentence names and the Shape does not declare.