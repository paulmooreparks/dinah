---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
owner: holder
ts: 2026-09-14T02:17:54Z
ordinal: 3
note: "VERIFIED. cmd/dinah/collection_reference_test.go TestShowDrawsACollectionsMembersInCreationOrder. Run: `go test ./cmd/dinah/ -run TestShowDrawsACollectionsMembersInCreationOrder -v`. The human form prints fx-1/comments/1 before fx-1/comments/2 with each comment's text; the --json form parses as a verb.CollectionListing carrying ref \"fx-1/comments\", kind \"comment\" and two members with those refs in that order. `dinah show fx-1/checklist --json` addresses the open question as fx-1/questions/1, and every printed address is handed back to show and required to resolve. ONE DEVIATION FROM THE SPEC'S PREDICTION, and it is the head's own doing rather than the library's: the criterion says a member's text field equals what `dinah show <member>` prints, and the head writes a trailing newline of its own after the text it is handed, so the comparison is against that output with the trailing newline trimmed. The test fails first if that trimmed output is empty, so it cannot pass on two empty strings. ARMED: composed the member reference in Library.collectionListing as collection.Ref + \"/\" + position instead of the walk's own spelling; the plant compiled and ran, and the assertion at collection_reference_test.go:243 reddened with `the open question is addressed \"fx-1/checklist/1\" rather than fx-1/questions/1`; restored byte-identically, green."
---
On a card carrying two comments written in a known order, `dinah show pb-1/comments` prints both, in creation order, each preceded by its own address on its own line, and the addresses are pb-1/comments/1 then pb-1/comments/2. `dinah show pb-1/comments --json` parses as JSON and carries ref "pb-1/comments", kind "comment", and a members array of length 2 whose entries carry those two refs in that order and whose text fields equal what `dinah show pb-1/comments/1` and `dinah show pb-1/comments/2` print for the same members. `dinah show pb-1/checklist` prints the kind-narrowed spelling for a checklist item, so an open question filed first appears as pb-1/questions/1 rather than as pb-1/checklist/1, and that printed address resolves when handed back to `dinah show`.