---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:35Z
ordinal: 7
note: "test: cmd/dinah/exithold_test.go#TestAnOperatorOwnedItemIsSettledByTheOperatorAlone, three subtests (resolve on an open_question, verify and fail on an acceptance_criterion). Each asserts the non-operator call exits 2 with refusal name not-operator, reads the item's anchor bytes before and after and asserts they are identical, then runs the identical call as the operator and asserts the item lands in resolved/verified/failed. The folded AC-14 half is internal/bench/gate_test.go#TestTheOwnerFieldNamesWhatEnforcesIt, which greps item.go for the retired sentence \"recorded and never\" and returns nothing, and asserts the doc comment names closeItem and SetField. That guard sits in internal/bench rather than cmd/dinah because the product-vocabulary guard refuses the literal path segment in a cross-package ReadFile. Also run against the built binary: a non-operator `dinah resolve pf-2/questions/1` answered not-operator with exit=2 and the operator's own exited 0. observed before: fail, after: pass. Armed by deleting the closeItem check, which reddened all three subtests with `<verb> by somebody who is not the operator exited 0`; restored byte-identically."
---
**End-to-end, run against the built binary:** file an `open_question` with `--owner operator` on a card; as a non-operator actor, attempt `dinah resolve` on it and confirm it is refused `not-operator`, with the item's state unchanged on disk; as the operator, run the identical `dinah resolve` and confirm it succeeds. Repeat for an `acceptance_criterion` with `--owner operator` against `dinah verify` and against `dinah fail`, in each case confirming the non-operator attempt is refused `not-operator` and the operator's own succeeds. **Folded from the prior revision's AC-14:** after the operator's successful close in each case, confirm `bench.ItemOwnerField`'s doc comment (`internal/bench/item.go`) no longer reads "recorded and never enforced against the actor calling a terminal verb" and instead names the `closeItem` check and the `SetField` write-guard (AC-9) as what enforces it — a `grep` for the retired sentence across `internal/bench/item.go` returns nothing.