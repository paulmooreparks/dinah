---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:35Z
ordinal: 2
note: "test: cmd/dinah/exithold_test.go#TestEveryHoldDirectionIsTypedAndReadBack (each of on/out/both/off typed through `dinah set`, written twice for idempotence, the anchor's gate_items read back as true/out/both/absent, `dinah get` answering the typed word; then \"in\" refused malformed and a non-operator write refused not-operator), and internal/verb/fields_guard_test.go#guardHold for the same four values through the library with seven malformed values including \"in\", \"exit\" and \"Both\". The unchanged-on-disk claim for on and off is held by cmd/dinah/gate_test.go#TestTheHoldWritesTheDeclarationAndReadsItBack, which asserts on writes gate_items: true and touches no other key and off removes the key, and which passes unmodified by this card. bench.FieldOf(\"column\",\"hold\") keeps Key gate_items and Guard GuardHold: internal/bench/fields.go:145 is untouched in the diff (`git show 8cfa79e -- internal/bench/fields.go` carries no change to the KindColumn table). observed before: fail, after: pass."
---
`bench.FieldOf("column", "hold")` is unchanged: `Key == "gate_items"`, `Guard == bench.GuardHold`, no second row added to the field table. `dinah set <column> hold <value>` accepts exactly `on`, `off`, `out`, `both` and refuses `Malformed` for anything else (including the retired word `in`); `dinah get <column> hold` prints `on`/`off`/`out`/`both` matching what was last set, run against the built binary for all four values plus one malformed attempt. `dinah set <column> hold on` and `dinah set <column> hold off` are proven byte-identical in on-disk effect to dinah-477's own behavior: `on` writes `gate_items: true` and nothing else changes in the anchor; `off` clears the key entirely (the anchor carries no `gate_items` line afterward), matching `TestTheHoldWritesTheDeclarationAndReadsItBack`'s existing assertions, which continue to pass unmodified. All four values are proven idempotent on re-write and refused with `not-operator` to a non-operator actor.