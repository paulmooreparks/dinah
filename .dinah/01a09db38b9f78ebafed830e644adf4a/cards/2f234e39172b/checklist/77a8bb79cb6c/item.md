---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:36Z
ordinal: 14
note: "test: cmd/dinah/exithold_test.go#TestTheExitHoldAnswersAheadOfTheDestinationRows. The fixture declares hold: out on Doing and a dinah.buffer column Waiting ahead of it, the card is claimed so TakesWorkUp's refusal is reachable, and the move fails both rows at once. The assertion is on which name comes back: dinah.unresolved-item-exit naming the item, not dinah.takes-no-work. The item is then verified and the same move answers dinah.takes-no-work, which shows the later row is reached once the earlier one clears. Also run against the built binary: `dinah move od-1 waiting` answered dinah.unresolved-item-exit with exit=2, and after `dinah verify od-1/criteria/1` the same move answered `dinah.takes-no-work no owner takes work up at column waiting` with exit=2. observed before: fail, after: pass. Armed by relocating the canLand block to sit immediately before the retiring check, exactly where the prior spec revision put it, which reddened exithold_test.go:219 with `the refusal name is dinah.takes-no-work, wanted dinah.unresolved-item-exit`; restored byte-identically (cmp against the backup)."
---
**Replaces the prior revision's AC-14 (which only checked a doc comment's wording; that check is folded into AC-7 above). Pins the ordering fix Agent Design Review's blocker 2 asked for, by construction rather than by reading:** create a departure column declaring `hold: out`, with an unresolved item naming it, and a destination column of kind `buffer` (so `TakesWorkUp()` is false there) standing ahead of the departure in flow order; claim the card at the departure so it is held by the claiming actor; attempt `dinah move <card> <the buffer column>`. Confirm the refusal is `dinah.unresolved-item-exit` naming the item, **not** `dinah.takes-no-work`/`dinah.awaiting-outside` — proving the exit-hold check in `canLand` runs before the `TakesNoWork`/`AwaitingOutside` check rather than after it, which is exactly the ordering blocker 2 found broken in the prior revision. `dinah verify` the item and confirm the move now succeeds (and now reports `dinah.takes-no-work` if attempted again while still held, showing the later check is reached once the earlier one clears).