---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:36Z
ordinal: 17
note: "test: cmd/dinah/fields_guide_test.go#TestTheReferencesGuideNamesEveryHoldValueAndNoOther, which reads the paragraph out of guide.Text(\"references\") by the clause that opens it, then runs two assertions per direction: every value the declared set carries must appear backticked in the paragraph AND must appear as a `dinah set <column> hold <value>` line, and every such line the paragraph offers must name a value the declared set carries. The paragraph deliberately carries no count word, so there is no figure to go stale; the check reads the values themselves. The declared set is bench.HoldValues, and admitFieldValue now admits a typed value through bench.KnownHold reading it, so the set the guide is held against is the set the command runs, not a second copy. observed before: fail (the helper and the test did not exist), after: pass. Armed three ways, each restored byte-identically (cmp against the backup): restoring the retired two-value sentence reddened fields_guide_test.go:198 and :201 naming \"out\" and \"both\" as never named; declaring a fifth value \"in\" in bench.HoldValues and leaving the guide alone reddened the same two lines naming \"in\"; and adding a `dinah set <column> hold in` line to the guide reddened fields_guide_test.go:209 with \"the guide's hold paragraph offers `dinah set <column> hold in` and the declared hold set does not carry \\\"in\\\"\". Rendered check against the built binary: `dinah guide references` serves the corrected paragraph at lines 238 to 246, and `set`/`get` round-trip all four values with \"in\" refused malformed."
---
**Added on round 2, at Agent Code Review's blocker.** `internal/guide/guides/references.md`'s hold paragraph, which `dinah guide references` serves, no longer says the field takes one of two values and no longer says `dinah get <column> hold` answers `on` or `off`. Its corrected text names every value the tree admits and shows a `dinah set <column> hold <value>` line for each. A test pins the paragraph to the declared set in both directions: a value `bench.HoldValues` carries that the paragraph does not name fails, and a `dinah set <column> hold <value>` line the paragraph offers that `bench.HoldValues` does not carry fails too. `bench.HoldValues` is the one declaration of that set and `admitFieldValue` admits a typed value through `bench.KnownHold` reading it, so the guard the guide is held against is the guard the command actually runs.