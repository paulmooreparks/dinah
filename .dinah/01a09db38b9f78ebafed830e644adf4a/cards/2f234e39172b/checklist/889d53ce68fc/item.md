---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:36Z
ordinal: 18
note: "test: cmd/dinah/gate_test.go#TestEveryDeclaredHoldValueReachesEveryReader, a subtest per value in bench.HoldValues, each running four arms against the built CLI and the library: the set command writes it and `dinah get` answers the word that was typed (which is storedHold, readColumnIn and typedHold); the anchor carries a stored spelling, and carries none only for off; Export plus `dinah init --from` carry that stored spelling out and back unchanged (exportColumn and writeColumnFromMember), with the import driven through the command a person uses rather than through Instantiate; and HoldsOnEntry/HoldsOnExit report a direction for every value but off. The stored spelling is read off disk rather than spelled in the test, because which spelling a value stores is storedHold's to decide and this guard is about whether the readers agree. observed before: fail (no such test), after: pass. Armed three ways, each restored byte-identically (cmp against the backup): review's own plant, adding \"sideways\" to HoldValues and changing nothing else, reddens gate_test.go:737 with `the hold was written as sideways and reads back as \"off\", so a reader between the two does not carry it`; deleting the HoldBoth case from exportColumn reddens gate_test.go:785 with `the anchor stores \"both\" under the gate key and the interchange carried it back as \"\"`; and narrowing both predicates off HoldBoth reddens gate_test.go:795 with `both holds in neither direction, so the two predicates read it as no hold at all`."
---
**Added on round 3, at Agent Code Review's major finding.** A guard fails when `bench.HoldValues` and the readers that carry a value disagree, rather than the claim that they cannot. For every value the list declares it drives the whole chain: `dinah set <column> hold <value>` writes it, the anchor carries a stored spelling (and carries none only for `off`), `dinah get <column> hold` answers the word that was typed, `Export` and `dinah init --from` carry the stored spelling out of the workbench and back unchanged, and `HoldsOnEntry`/`HoldsOnExit` report a direction for every value but `off`. A value added to `HoldValues` and to nothing else must fail this, which is the exact defect review produced by doing it.