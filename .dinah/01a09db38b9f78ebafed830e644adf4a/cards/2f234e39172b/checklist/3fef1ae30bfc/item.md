---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:36Z
ordinal: 15
note: "test: cmd/dinah/exithold_test.go#TestTheOperatorCarriesACardPastTheExitHold (the operator's `move --override` out of the held column exits 0 and the last moved event on the card's journal carries Override true) and #TestANonOperatorIsRefusedTheOverrideAtTheExitHold (the identical call as \"sam\" answers not-operator, which is canRoute's existing row ahead of every hold rather than anything this card added). Also run against the built binary: `dinah move pf-1 review --override` exited 0 with the item still unresolved, `dinah --json log pf-1` shows `\"override\": true`, and the same call as DINAH_ACTOR=sam answered `not-operator this action is the operator's, and you are sam` with exit=2. observed before: fail, after: pass. The override path is held by the same `(reached || loopReached || gateHeld || exitGateHeld) && req.Override` return the capacity and entry-hold cases already ride, so the arming proof that would break it is AC-14's relocation, which leaves the return untouched; the witness assertion is what makes this criterion more than a smoke test."
---
**New, addressing the major finding that no criterion proved `--override` reaches the exit-hold refusal. End-to-end, run against the built binary:** the AC-4 card stands at the `hold: out` departure with its item still unresolved; the operator (and only the operator) runs `dinah move <card> <a later column> --override` and it succeeds, moving the card despite the unresolved item, exactly as `--override` already carries a card past the entry hold and past a full column. Confirm the resulting `moved` event's `override` field is `true`. Separately, confirm the identical `--override` attempt by a non-operator actor is refused `not-operator` before the exit-hold check is ever reached (proving the existing operator-only gate on the marker itself, not a new one, is what protects this path).