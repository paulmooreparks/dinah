---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:18:39Z
ordinal: 40
note: "No, there were three, and the two review did not name were found by changing the shape of the search rather than by widening its terms. Every one of the three states its claim across two comment lines, so a line-by-line grep matches neither half: `ItemLiftsColumnHold` breaks after \"the hold a\", `GatingItems` after \"hold it out of one\", and `ItemColumnField` after \"matches a move's\". The sweep that found all three reads whole comment blocks and whole prose paragraphs rather than lines, joins each into one string, and requires a hold term and a direction term anywhere in it. That is the same failure mode as this card's two earlier ones: the row-number sweep missed a count word inside a camelCase identifier, and the value sweep missed a claim spelled in prose rather than in digits, and this one hid in the line break. All three are corrected: `ItemLiftsColumnHold` now says it reads the same way whichever direction the column declares, `GatingItems` says which side is the caller's question and that canLand asks it twice, and `ItemColumnField` names the departure beside the destination.\n\nCORRECTED at Agent Code Review, round 3, so the count on this record is the count that holds. I ran the same whole-comment-block sweep independently over every .go file in the tree, requiring a hold term and a direction term anywhere in a folded block, and it returns thirty-three candidate blocks. Reading all thirty-three, the three named above are corrected and correct, and every other block is either direction-aware already or is about something else. But one further comment carries the same shape and is not among the three: internal/verb/checklist_test.go:137-138, \"GatingItems matches an item's column field against the destination's identifier, so an item storing the slug holds nothing\", which is half the story after this card in exactly the way ItemColumnField's was.\n\nMy ruling is to leave it, so \"there were three\" reads as three corrected rather than three in existence. Its own load-bearing claim, that the match is on the identifier and not on a slug a caller typed, is entirely true and is the only reason the sentence is there; the test it explains files an item and never moves a card, so \"destination\" is incidental to it; and it is a comment in a test file that nothing prints and nothing acts on. It is a nit for whoever next edits that file rather than work for this card. Recording it here so the fourth instance is on the record and the next sweep of this class starts from four rather than rediscovering it as a fifth disguise. Note also that this one is not hidden by a line break: \"against the destination's identifier\" sits whole on one line, so it is reachable by the line-by-line search too, which is worth knowing because it means the line-break framing above explains the three and not the class."
---
Review named one comment still describing the hold as one-directional. Was it the only one, and how was that established?