---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:36Z
ordinal: 16
note: "test: cmd/dinah/exithold_test.go#TestTheExitHoldAnswersAheadOfTheOperatorReservedClaimOnAPull, a non-operator pull out of a hold: out departure into an operator_owned destination, asserting dinah.unresolved-item-exit rather than not-operator. Against the built binary: `DINAH_ACTOR=sam dinah pull review` answered dinah.unresolved-item-exit with exit=2, and once the item was verified the same pull answered not-operator, which is the later row reached. NOTE the fixture deviation recorded as D-15: operator_owned sits on the DESTINATION, not the departure as AC-16's parenthetical says, because a departure reserved to the operator is refused by canRoute before canLand runs at all; row 13 is operatorReservesClaim(destination, ...), which is the row AC-16's own sentence names. Unit half: internal/verb/pull_test.go#TestPullChecksAgainstTheFullEighteenRowTable asserts Checks(Pull) row for row, with check.pull.12 carrying UnresolvedItemExit and 13 to 16 carrying NotOperator, Locked, UnresolvedItem, BelowTier in that order. `dinah help pull` prints eighteen rows, printed 14 being the exit hold. observed before: fail, after: pass. Armed by relocating the canLand block, which reddened exithold_test.go:285 with `the refusal name is not-operator, wanted dinah.unresolved-item-exit`; restored byte-identically.\n\nRULING, Agent Code Review 2026-09-11: I corrected this criterion's text rather than pushing back on the fixture, because the text was wrong and the code was right. The original text read \"a departure column declares `hold: out` with an unresolved item naming it and is also `operator_owned` (so `NotOperator`/`OperatorReservesClaim` would otherwise be the first failing check a non-operator hits when pulling out of it)\". I verified D-15's claim against the check list rather than taking it: internal/verb/mutate.go:343 refuses `departure.OperatorOwned && !operator` with NotOperator inside canRoute, and internal/verb/pull.go calls canRoute before claimableState, pullableDeparture and canLand, so on the literal fixture canLand is never reached and the ordering the criterion wants to observe cannot occur. The substitute proves the same thing and proves it by construction: I armed it myself by relocating the canLand block below the three destination rows, and it reddened with \"the refusal name is not-operator, wanted dinah.unresolved-item-exit, so row 13 answered ahead of row 12\", so it does not pass under both orders. The row pair observed (12 before 13, operatorReservesClaim at the destination) is the pair this criterion's own sentence already named."
---
**New, proving the pull-path ordering fix and the `pullChecks` renumbering (D-13), by construction:** a departure column declares `hold: out` with an unresolved item naming it, and the *destination* column declares `operator_owned` (so `OperatorReservesClaim` would otherwise be the first failing check a non-operator hits, at `pullChecks` row 13 inside `canLand`); a non-operator attempts `dinah pull` naming that destination. Confirm the refusal is `dinah.unresolved-item-exit`, not `not-operator`, proving the departure's exit hold (now row 12) is evaluated before the operator-owned reservation (now row 13) inside `canLand`, exactly the order `pullChecks` declares after this card's renumbering. Separately, a unit test asserts `Checks(Pull)` (or the equivalent introspection call `dinah help pull` renders from) returns `pullChecks` with `check.pull.12` carrying `contract.UnresolvedItemExit` and `check.pull.13` through `check.pull.16` carrying, in order, `contract.NotOperator`, `contract.Locked`, `contract.UnresolvedItem`, `contract.BelowTier`, pinning the renumbering itself rather than only one of its sixteen rows.