---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:36Z
ordinal: 12
note: "doc: docs/design/format.md, the gate_items passage, extended in place with no second section. The \"Absent means false, and the value is exactly true or false\" sentence now names all four spellings and what each means; the \"Entry is what is held\" paragraph is replaced by \"Which side is held follows the declared direction\", which names dinah.unresolved-item-exit and states plainly that neither refusal reads the direction of the move; the \"trap\" paragraph is replaced by the naming rule from D-10. The interchange paragraph gains the boolean-or-string value set and the lenient drop, and the moved-event row of the events table names the departure hold. `grep -n gate_items docs/design/format.md` answers four lines (451, 616, 668, 908), all of them in the passages above, and `grep -c gate_items_exit` answers 0. The Checklist items section also records what enforces owner, which the diff's own behaviour change made necessary."
---
`docs/design/format.md`'s `gate_items` passage (lines 616-655) is extended, not given a second section: the "Absent means false, and the value is exactly `true` or `false`" sentence is corrected to name all four legal values (`true`/`false`/absent/`out`/`both`) and what each means; the "Entry is what is held" paragraph is corrected to say which side is held follows the declared direction, with the exit case refused under the distinct name `dinah.unresolved-item-exit` on the identical terms as entry, symmetric on forward and backward moves; and the "The tool cannot tell a column an item deliberately names from one an item traps itself at" trap paragraph is replaced with the naming-convention rule from "The benefit that falls out" (name the column that settles an item as a hold-on-exit target on itself; name a column that depends on an already-settled item as a hold-on-entry target). A search of `format.md` for `gate_items` after the edit shows every mention is either the (corrected) entry/exit passage or the corrected naming-convention paragraph, with no second `gate_items_exit`-named section anywhere in the file.