---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:02Z
ordinal: 38
note: "AC-14 caught one cell that under-reported what exists. That defect class was searched for across every cell of section 2.5's table and section 5.4's, at origin/main b37486b and against a binary built at 22a35fc, rather than trusting that only the reported cell was wrong. What was checked and what came back:\n\nVERIFIED CORRECT, BY READING THE CODE AT TRUNK.\n- The four subcommand dispatchers accept exactly what the tables say. `runCard` takes `get` and `set`; `runWorkbench` takes the empty word, `get` and `set`; `runWorkstream` takes the empty word, `new`, `get` and `set`; `runColumn` refuses any first word but `new`, which is section 2.5's \"none at all\" for a column's Update. All four in cmd/dinah/commands.go.\n- The three Update field sets. `bench.WorkbenchFields` is title, slug, operator; `bench.CardFields` is SeverityField, PriorityField, TierField; `bench.WorkstreamFields` is title, slug, status. Each matches its cell exactly.\n- The workbench's refused Delete and Archive. `Library.Archive` and `Library.Delete` in internal/verb/beyond.go each carry `if entity.Kind == \"workbench\" { return l.refuse(..., contract.UnknownPath, req.Ref) }`, so both refuse it by name, which is what section 5.4's prose already claims.\n- Restore. No `restore` command exists in the command table, so every \"none\" in section 2.5's Restore column is right.\n\nTWO DEFECTS FOUND IN SECTION 2.5, BOTH IN THE ONE CELL AC-14 REPORTED, AND BOTH FIXED. It omitted `workbench get`, which is the reported defect. It also named `show`, which refuses every spelling of the workbench reference: `dinah show workbench`, `dinah show .` and `dinah show pb` each answer `unknown-card`, probed at 22a35fc, while `dinah path workbench` resolves. The cause is in `Library.Show`, which routes a head carrying no slash to `ResolveCard` and only a composed reference to `ResolvePath`. The transcript is in the spec beneath the table.\n\nTHREE UNDER-REPORTS FOUND IN SECTION 5.4, ALL LEFT.\n1. The surviving listers are missing from three Read cells. Section 5.2 says in its own words, \"Nothing else goes. The bare `dinah workbench` and `dinah workstream` listings stay\". Section 5.4's workbench, workstream and column Read cells name none of `workbench`, `workstream` or `columns`, though the same table's attachment row does name the lister `attachments`. So one table uses two senses of Read and 5.2 and 5.4 disagree.\n2. `rename` is missing from the attachment Update cell. Section 2.5 names it, section 5.3 keeps `Library.Rename` in the path for `filename`, and nothing retires the command, but 5.4's cell reads \"`set`, `attach --replace`\".\n3. The item Update cell says \"and the five state verbs\" where section 2.5 spells `cite`, `resolve`, `verify`, `fail`, `reopen`. Naming a set instead of its members is what makes AC-14's own method, diffing command names between the tables, read fifteen departures on a naive comparison, or ten if the five state verbs are read as one phrase, where the paragraph names six. The round-5 form of this sentence said eleven, which is the six retirements plus the five verbs and so already sets aside the listers and `rename` this same paragraph lists separately; the fifth design review caught it and the count is corrected here.\n\nWHY ALL THREE WERE LEFT. Each is a change to the after-picture, which is contract rather than a record of the trunk, and this round was sent back to repair one cell. AC-14's method survives them: the verifier's diff asks which of the six named spellings appear in 2.5 and not in 5.4, and after the correction that answer is six. Fixing the three would make the diff exact in both directions, which is a better contract, and it is a change Agent Design Review should rule on rather than one a repair round should slip in.\n\nTWO CELLS CHECKED AND DELIBERATELY NOT CHANGED, SO THE REVIEWER SEES THEY WERE CONSIDERED.\n- Section 5.4's workbench and workstream Read cells name `show`, which reaches neither entity today: `dinah show workstream/addressing` refuses `dinah.unknown-path` while `dinah path workstream/addressing` resolves. No section of this contract specifies widening `show`, and section 9 gives the matrix to no child card, so a widening asserted only in a summary table would never be built. Left, and named in the spec beneath section 2.5's table.\n- Section 2.5's card Update cell names `card set` and not `raise`, which also writes the tier. `raise` writes a per-column override as one half of an act that also releases the claim, and its own doc comment contrasts it with `card set <ref> tier <value> --at <column>` as the unrestricted route. Both tables treat the flow verbs, which are claim, move, release, block, unblock, join and leave, as outside the table, consistently. The cell is not empty and not false, so it stands. `init` and `reshape` also create and remove columns, and are outside the table for the same reason: neither addresses a column.\n\nWHAT THE AUDIT DID NOT COVER. The generic navigation commands, `path`, `contents`, `tree`, `query`, `search`, `ls`, `log` and `changes`, appear in no Read cell of either table for any kind, uniformly. That is consistent rather than an omission, and sections 2.2, 2.3 and 3.3 are where those commands are specified against a reference."
---
Every cell of both command tables was audited for the defect AC-14 caught, and the three further under-reports found in section 5.4 are recorded here rather than repaired, because repairing them is contract work this round was not sent back for.