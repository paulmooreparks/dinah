---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:58Z
ordinal: 2
note: "RE-VERIFIED at origin/main b825059 on 2026-09-09, in worktree C:/dinah-scratch/dinah-456-impl6/wt. This criterion DOES depend on the tree, so it was re-run. internal/bench/containment.go is byte-identical between 8661604 and b825059 (blob 38af9b8b68f651e064fdf8b506891356c435e845 at both, by git rev-parse). Re-run anyway: the kind block parses to workbench, column, card, comment, item, attachment, plus workstream as the criterion directs, which is 7; section 3.2's worked-examples table carries 10 data rows covering all 7 with attachment on four mounts, and no cell blank; section 5.4's matrix carries 7 rows and no blank in any of the six columns. One defect in my own first pass, recorded because a re-runner will meet it: my initial slice of the worked-examples table cut the workstream row off, and the emptiness of the resulting hit list read as a pass rather than as a miss. Widening the slice and asserting at least one row per kind fixes it, and the arming run that drops the workstream row now reports ['workstream'] rather than passing silently.\n\nVERIFIED. Evidence re-run at origin/main 8661604 after dinah-454 merged; originally run at 813e0bb, same result.\n\ninternal/bench/containment.go is byte-identical between 813e0bb and 8661604 (compared by blob hash through git rev-parse, not assumed from the diffstat), so this criterion's source did not move. I re-ran the check at 8661604 anyway rather than carrying the earlier pass forward.\n\nKind constants parsed from containment.go: workbench, column, card, comment, item, attachment, plus workstream as the criterion directs, which is 7. Section 3.2's worked-examples table gives a resolving address for all 7, with attachment carrying four rows (on a card, a comment, a column, the workbench). Section 5.4's matrix carries exactly 7 rows, one per kind, with no blank cell in any of the six columns; workbench's Delete, Archive and Restore read \"refused, deliberate\", \"refused, deliberate\" and \"not applicable\", which are statements rather than blanks.\n\nCOULD IT HAVE FAILED: yes. The check parses containment.go rather than the spec, so a kind constant added there with no worked example or no matrix row fails it; dinah-459 edited that file two commits ago, which is how close this came to mattering. A blank cell fails it, and a matrix row naming a non-kind fails it (that arm returned empty)."
---
Every kind named in internal/bench/containment.go, plus workstream, has at least one resolving address in the spec's section 3.2 worked-examples table and a non-empty cell in all six columns of the section 5.4 matrix. A kind absent from either table, or a blank cell, fails this. Verify by listing the kind constants in containment.go and checking each against both tables.