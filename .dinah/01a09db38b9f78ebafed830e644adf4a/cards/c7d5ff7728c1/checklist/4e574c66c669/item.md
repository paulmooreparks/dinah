---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:59Z
ordinal: 8
note: "RE-VERIFIED at origin/main b825059 on 2026-09-09, in worktree C:/dinah-scratch/dinah-456-impl6/wt. This criterion DOES depend on the tree, so both halves were re-run. internal/mcp/tools.go is byte-identical between 8661604 and b825059 (blob 68c9355e2b1addd2d6b51d3efe8bc27cb66d2fa1 at both, by git rev-parse). First half re-run: 49 backticked command tokens across the matrix's non-absent cells, every one either served by a tool in the tools roster or on the roster section 6 puts it on (get, set, restore), with init the single exemption relied on and shell-or-filesystem its ground. Nothing is reachable only through edit or path. Second half re-run: nine map entries against nine table rows, one to one, every row's quoted reason a prefix of or identical to its map value, every ground one of the four declared, and the split is five shell-or-filesystem against four others, which is the arithmetic the section asserts.\n\nONE CORRECTION TO THE EARLIER NOTE BELOW, found by re-running rather than by reading it. It says \"Seven are absent\" of the 42 cells. Three are: the workbench row's Delete, Archive and Restore, reading \"refused, deliberate\", \"refused, deliberate\" and \"not applicable\". Those three are also the three the section's own prose names, so the earlier count disagreed with the sentence beside it rather than with the table. Nothing in the criterion's verdict turns on the number.\n\nVERIFIED. Evidence re-run at origin/main 8661604 after dinah-454 merged; originally run at 813e0bb, same result. internal/mcp/tools.go is byte-identical between the two commits, compared by blob hash rather than inferred, and both halves were re-run at 8661604 rather than carried forward.\n\nFIRST HALF, the matrix against the tools table. Walked all 42 cells of section 5.4. Every command in the non-absent cells resolves to a tool in tools.go's roster or to one section 6 puts on the roster (get, set, restore), with one exception: init, which is exempt on the ground shell-or-filesystem, the one exemption ground this criterion permits. No capability in the matrix is reachable only through edit or path, which is the whole point of the card and the thing that would have failed before get and set.\n\nSECOND HALF, section 6's exemption table against toolExemptions entry for entry. Nine map entries, nine table rows, one-to-one with no map key lacking a row and no row lacking a key. Each row's quoted reason is either byte-identical to the map value (edit, config, mcp, guide) or a faithful prefix of it where the table drops a trailing clause (path, init, extract, reshape, help); no row contradicts its map value. Each row's stated ground is one of the four the section declares, and each is the ground its own quoted reason actually gives: path, edit, init, extract and reshape turn on a shell or the filesystem; config on the machine rather than the workbench; mcp on this head itself; guide and help on the protocol serving it another way.\n\nThat is five and four, which is the arithmetic the section asserts and the check that would have caught round 1's claim that all nine give a shell-or-filesystem reason.\n\nCOULD IT HAVE FAILED: yes, on four arms, all mechanical. A matrix cell naming a command that is neither served nor exempt is reported. A cell reachable only through edit or path is reported separately. A map entry with no table row, or a row with no map entry, is reported. A row whose ground is outside the four declared constants is reported. The one judgement in it is whether a truncated reason counts as differing, and I recorded the comparison per row rather than collapsing it to a verdict."
---
For every non-absent cell in the spec's section 5.4 matrix, the capability is reachable through a command that builds a verb.Request, and that command is either present in the tools table in internal/mcp/tools.go or is one that section 6's exemption table records as exempt on the ground `shell-or-filesystem`. A capability reachable only through `edit` or `path`, or one whose command is exempt on any other ground, fails this. Verify by walking the matrix against the tools table, and by checking section 6's nine-row exemption table against toolExemptions in tools.go entry for entry: a row whose quoted reason differs from the map's, a map entry with no row, or a row whose stated ground is not the one its own quoted reason gives, fails this too. That second half is the check that would have caught round 1's claim that all nine entries give a shell-or-filesystem reason, when five do and four do not.