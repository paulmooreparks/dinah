---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:01Z
ordinal: 30
note: "OPERATOR OVERRIDE, Paul, 2026-09-09: \"DinahPath blessed as the pathing name.\" This reverses the ruling below, which is kept in full because the reasoning it was overturned on is worth a later reader's time rather than being erased.\n\nWhat stands from the original ruling: an instance is still a reference, the act is still addressing, and the name is for the language alone, which was the one thing that lacked a word. What changes: the language is DinahPath rather than \"the reference grammar\", and a proper noun does now enter the vocabulary.\n\nOne consequence the spec must handle rather than inherit. The original ruling rejected DPath and its relatives on the ground that they borrow XPath's register and advertise a resemblance section 8 spends its length refusing: DinahPath admits no axes, no node tests, no wildcards over kinds, no string or boolean functions, no unions, no leading-slash absolute form, and no predicates in any spelling at all. A reader who meets the name and reasons from XPath will expect several of those. The operator has ruled and the name stands; what the spec owes is that section 8's refusals are reachable from wherever the name is introduced, so the name sets an expectation the very next paragraph corrects.\n\nThe surfaces question from the original ruling still needs answering for the new name, and the answer may differ. \"Reference grammar\" was to appear only in design documents and the references guide's opening paragraph, and nowhere a reader meets by accident. A proper noun is a stronger claim: say whether DinahPath appears in help text, in refusal text, in the guide's title, or on any machine-surface field, and rule each deliberately rather than letting the name spread by use.\n\nTHE ORIGINAL RULING AND ITS REASONING, superseded:\n\nAn instance stays a reference, the language is the reference grammar, the act is addressing, and no new noun enters the product. Spec section 13. The language is separately learnable, so a name is earned, but the instance already has one and only the language lacked a name. The words appear in design documents and in the references guide's opening paragraph, and nowhere else: not in refusal text, not in help, and on no machine-surface field, so a reader who never opens a design document carries one word rather than two. `path` was rejected because `dinah path` already means the filesystem answer rather than the question. DPath and its relatives borrow XPath's register and advertise a resemblance section 8 spends its length refusing. `selector` is taken by one segment of a reference. `locator` was rejected in round 2 with a reason of its own: in the sense the letters of URL made familiar, a locator says where a thing is fetched from, which is what `dinah path` answers rather than what a reference asks, so it fails the same way `path` does and one step further from Dinah's own words. `expression` was rejected in round 2 because it is already taken on the machine surface: a tier expression is the form an `--at` value is written in, internal/bench/tier.go calls it that throughout, and the tier_overridden event publishes what was typed under the key `expr`; section 3.3 also uses the word for one form inside the reference grammar, so promoting it would make one word mean both a part and the whole. Round 1 rejected those two with \"each defensible and neither beats reference grammar\", which is true of any losing candidate and is what AC-10 refuses. If a single proper noun is wanted anyway, `addressing` fits the register and the workstream already carries it."
---
The pathing language is named DinahPath. An instance of one stays a reference and the act stays addressing.