---
kind: decision
state: resolved
column: 0d86ad99cdbc
owner: holder
ts: 2026-09-14T02:18:00Z
ordinal: 21
note: "The gap is real: Library.Attachments reports one entity's own attachments and never those of anything it contains, and says so in its own comment, so \"every attachment under this card\" has no spelling today. A flag appears in `dinah help attachments`, which is a screen the reader can reach; `pb-1//attachments` appears on no screen and adds a second separator to a published address format that every client would then have to parse. DIRT counts what escapes into an interface, and a flag on one command escapes less far than a separator in the address grammar."
---
The descendant step is borrowed as a flag, `dinah attachments <ref> --deep`, and not as reference syntax.