---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:59Z
ordinal: 6
note: "VERIFIED. Spec-only criterion: it reads section 8 and a card comment, neither of which any commit touches, so the trunk move from 813e0bb to 8661604 leaves it untouched. I re-read it after the merge to confirm that rather than assume it, and the text checked is the text now stored on the card.\n\nRead against the operator's XPath comment, card comment id 7245, 2026-09-08, \"I'd like to draw inspiration here from XPath\".\n\nNO CONSTRUCT IN BOTH LISTS. Borrowed: the collection reference; selection by a value the entity carries; the descendant step. Refused: predicates; axes; node tests and wildcards over kinds; string and boolean functions; the absolute/relative distinction; unions. The one pair that could plausibly overlap is handled explicitly rather than by luck: the borrowed \"selection by a value\" is the existing NameField arm of pick, and its entry says outright that it \"passes the test with no bracket in it\", while the refused predicate is the bracket syntax. Distinct, and the spec says why they are distinct.\n\nEVERY CONSTRUCT IN THE OPERATOR'S COMMENT CARRIES A VERDICT. He named four: a collection reference yielding many entities, borrowed; a predicate as the place for a non-position selector, whose field-selection half is borrowed in the form it already has and whose bracket syntax is refused; a descendant step, borrowed as the --deep flag; an upward axis, refused. Section 8 opens by saying four ideas were named and each was put to the screen test, and all four are answered.\n\nTHE SCREEN TEST IS APPLIED RATHER THAN CITED. Each borrowed entry names something on a screen: pb-1/comments is \"spelled out of a heading the reader is already looking at\"; the filename \"is in the File column of the table on screen\"; the flag \"appears in dinah help attachments, which is a screen\". Each refused entry names why no screen shows it or which surface already answers it: predicates fail the screen test and route to query; axes are unnecessary because the head is already the first segment every surface prints; wildcards route to dinah contents; functions route to query; the leading slash \"would carry no information\"; unions route to two commands and to the collection reference. None rests on elegance.\n\nCOUNT NOTE, not a failure: section 8's preamble enumerates SEVEN refusals by splitting \"node tests\" from \"wildcards over kinds\", while the bold entries group those two under one heading, so six paragraphs carry seven named constructs. Sections 1 and 13.1 both list the seven, so the stores agree.\n\nCOULD IT HAVE FAILED: yes. A construct in both lists fails, and the borrowed-field-selection against refused-predicate pair is precisely where a careless spec would put one. A verdict resting on elegance fails, and the axes entry is the one at risk, since \"an axis is inelegant\" was the easy sentence to write; it instead gives the concrete reason that an agent holding pb-1/comments/1 already holds pb-1."
---
No construct appears in both the borrowed and the refused lists of spec section 8, every construct named in the operator's XPath comment on this card carries a verdict there, and every borrowed construct's entry names something a reader sees on a screen while every refused construct's entry names why no screen shows it or which existing surface already answers it. A construct with no verdict, a construct in both lists, or a verdict resting on elegance rather than on the screen test fails this.