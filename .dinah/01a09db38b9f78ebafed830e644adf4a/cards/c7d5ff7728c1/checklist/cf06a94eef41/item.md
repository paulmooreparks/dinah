---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:59Z
ordinal: 7
note: "VERIFIED. Spec-only criterion, unaffected by the trunk move to 8661604: it reads the section 3.1 grammar block and the spec's own sentences, and no commit touches the spec field. Re-read after the merge to confirm that rather than assume it; the text checked is the text now stored on the card.\n\nRun by the recipe the criterion prescribes, which is reading the grammar block alone and then reading, rather than grepping, the surrounding sentences.\n\nGRAMMAR BLOCK, 16 lines inside the fence: zero bracket characters other than the angle brackets marking a nonterminal, zero operators in any right-hand side, zero field names in any production. The only selection-by-value production is selector = <12-hex identifier> | <positive integer> | <name>, and <name> is the collection's declared NameField, which is the one language this criterion permits.\n\nSENTENCES OUTSIDE SECTIONS 8 AND 12.2, read individually rather than counted. Section 1 and section 12.1 attribute selection by field to query rather than to a reference. Section 1 says outright that \"references gain no field predicate\". Sections 2.1, 3.1 and 3.2 describe the NameField resolution order, identifier then position then the collection's declared name field, which is the permitted mechanism. Section 8's borrowed-2 entry says \"this card adds no name field\". Every remaining candidate is a denial. No sentence anywhere promises selection by a field value through a reference.\n\nI did NOT grep the whole spec for \"[\", per the criterion's explicit instruction, and I confirmed why that instruction exists: bracketed forms appear in section 8 (comments[1], comments[author=alka]) and section 12.2 (workbench/cards[column=doing][state=ready]) as the counter-examples being refused, and markdown link syntax adds more, so a whole-spec grep reports a failure on a passing spec. That is the round-1 finding this criterion was rewritten to avoid repeating.\n\nCOULD IT HAVE FAILED: yes. A bracket, an operator or a field name added to any production is reported by the block scan, which parses only the fenced lines and so cannot be fooled by the counter-examples elsewhere in the document. A prose sentence promising field selection through a reference fails on the read, and the candidate list is generated mechanically, so no such sentence escapes being read even though the verdict on each one is a judgement."
---
DinahPath, as spec section 3.1 writes it out, admits no production that selects an entity by the value of a field other than a collection's declared NameField, so the spec leaves exactly one language that selects by field. A production admitting a bracket, an operator, or a field name, or a sentence elsewhere in the spec promising field selection through a reference, fails this. Verify by reading the section 3.1 grammar block alone for any bracket, operator or field name in a production, and then by reading, rather than grepping, every sentence outside sections 8 and 12.2 that could promise selection by a field value. Do not grep the whole spec for `[`: sections 8 and 12.2 quote bracketed forms as the counter-examples they are refusing, and markdown links add more, so a whole-spec grep reports a failure on a passing spec.