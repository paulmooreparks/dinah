---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:58Z
ordinal: 3
note: "RE-VERIFIED at origin/main b825059 on 2026-09-09, in worktree C:/dinah-scratch/dinah-456-impl6/wt, and re-run against the spec text as it now stands after this column's two prose edits. This criterion DOES depend on the tree, so it was re-run. internal/contract/contract.go is byte-identical between 8661604 and b825059 (blob 0729c3bf69f7d4f36764e5bee3b9ab3a9031fb42 at both, by git rev-parse), and the roster re-derived at b825059 is again 17 profile names plus 71 LayerPrefix-built plus the two mints. Scanning every code context in the current spec, meaning inline backticks, fenced-block lines and refusal. keys, gives 51 hits over 13 distinct names, which is the earlier pass's 10 names plus the three refusal.dinah.* catalogue keys counted separately. Exactly one spelling failure is reported, and it is the one the criterion declares expected: `dinah.unknown-card` in section 3.2's closing paragraph, quoting the round-1 misspelling in the sentence that names it as wrong. Every other Dinah-layer name carries the prefix at every hit and every profile name is bare at every hit. That single reported hit is also this run's live proof that the check fires, since it is precisely the shape the criterion exists to catch.\n\nVERIFIED. Evidence re-run at origin/main 8661604 after dinah-454 merged; originally run at 813e0bb, same result. internal/contract/contract.go is byte-identical between the two commits, compared by blob hash rather than inferred from the diffstat, and the roster re-derived at 8661604 is the same size (17 profile names, 71 LayerPrefix-built).\n\nRun by the whole-roster recipe rather than by extracting dinah.-prefixed tokens. Roster: the 17 unprefixed profile names in contract.go's \"seventeen refusal names\" block, plus 71 LayerPrefix-built constants, plus section 3.4's two mints, which is 89 names after overlap. Grepped the spec for each and classified every hit sitting in a code context (inline backticks, a fenced block, or a refusal. key prefix). 44 hits over 10 distinct names: ambiguous-name, exists, is-a-collection, not-attachable, not-renamable, unknown-card, unknown-command, unknown-field, unknown-key, unknown-path. Every Dinah-layer name carries the prefix at every hit, and unknown-card is bare at every hit but one: line 417, section 3.2's closing paragraph, quoting the round-1 misspelling in the sentence that names it as wrong. That is the single hit this criterion declares expected.\n\nSecond half also run. Every token the spec quotes beside refused, refuses, refusal or a refusal. key was listed and checked against the roster. The refusal-shaped ones are the six dinah.* names plus unknown-card, all in roster. The seven refusal.dinah.* catalogue keys each resolve to a roster name (is-a-collection, not-attachable, unknown-path) plus a splice suffix. Every remaining quoted token is a command name, a field name, or the \"addressed\" detail key, none of which is a refusal name and each of which the surrounding sentence identifies as what it is.\n\nROSTER-WIDTH NOTE: read literally, \"every LayerPrefix-built constant\" also admits contract.KindBuffer (dinah.buffer), a column kind rather than a refusal. I kept it in the roster, since a superset only makes the check stricter, and it has no hit in the spec.\n\nCOULD IT HAVE FAILED: yes. A Dinah-layer name written bare, or a profile name written with the prefix, is reported with its line and its text. That is exactly the class that let a spelled-out dinah.unknown-card survive round 1, and the recipe this criterion forbids, extracting only prefixed tokens, still cannot see it."
---
Every refusal name the spec quotes is either an existing constant in internal/contract/contract.go or is minted in spec section 3.4 with its English text and the names of the details it carries, and each one is spelled with the `dinah.` prefix if and only if its constant is built from contract.LayerPrefix. A name with neither backing fails this, and so does a name whose spelling adds the prefix to a profile name or drops it from a Dinah-layer name. Verify over the whole set rather than over the prefixed ones alone: build the roster from contract.go, which is every unprefixed profile-refusal constant plus every LayerPrefix-built constant, add section 3.4's two mints, grep the spec for each name in that roster and check its spelling at every hit, then read every remaining token the spec quotes next to "refused", "refuses" or "refusal" and check it against the roster too. Extracting only `dinah.`-prefixed tokens is what let a spelled-out `dinah.unknown-card` survive round 1, and that recipe cannot catch this class of error. One hit is expected and does not fail this: section 3.2's closing paragraph quotes the round-1 misspelling in the sentence that names it as wrong, which is the only place in the spec where a refusal name appears as a counter-example rather than as a claim.