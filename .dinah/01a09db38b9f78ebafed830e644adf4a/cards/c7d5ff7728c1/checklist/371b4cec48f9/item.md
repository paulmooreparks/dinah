---
kind: decision
state: resolved
column: 5729d4578008
owner: holder
ts: 2026-09-14T02:18:02Z
ordinal: 39
note: "Round 5's review took this call, which section 2.5's paragraph and D-25 both hand to it.\n\nTHE THREE UNDER-REPORTS STAY. Each is a summary-table imprecision that reaches no implementer. Section 9 gives the section 5.4 matrix to no child card, and each child card's description names the sections it builds from, so the operative text is elsewhere in every case: section 5.2 says in its own words that the bare `dinah workbench` and `dinah workstream` listings stay and that nothing else goes, which covers the missing listers; section 5.3 keeps `Library.Rename` in the write path for `filename`, and nothing retires the `rename` command, which covers the missing `rename`; and section 2.5 spells the five state verbs the item Update cell names as a set. AC-2 reads that matrix for kind coverage and non-empty cells, not for command-name exactness, so nothing downstream verifies against the cells either. Repairing them would improve the contract and would have been contract work in a round sent back for one cell, so leaving them was the right call rather than an omission.\n\nTHE `show` CELLS STAY, AND THE READING IS RECORDED SO IT IS NOT INFERRED TWICE. `show` reaches no spelling of the workbench or workstream reference today, reproduced at 22a35fc in this review: `dinah show workbench`, `dinah show .` and `dinah show pb` each answer `unknown-card`, while `dinah path workbench` resolves and `dinah workbench get <field>` prints the value. `Library.Show` is unchanged between 22a35fc and trunk `b825059`; it checks a slashless head against `ColumnByRef` and otherwise sends it to `ResolveCard`, so a workbench or workstream head can never resolve there.\n\nThe cells' capability claim is nonetheless true after the contract lands, because section 5.2 gives `get` any reference the grammar resolves and `workbench` is such a reference. The wrong part is one token in two cells.\n\nWHAT THE CONTRACT DOES NOT SAY, NAMED RATHER THAN RESOLVED. Section 3.3's table gives `show` a \"yes\" in its One entity column, and section 3.2 lists `workbench` and `workstream/<ref>` among the forms that resolve, so a reader can construct an argument that dinah-455, which builds section 3.3, must widen `show` to every resolving head. Section 2.5's own paragraph takes the other reading, that no section specifies the widening. Section 3.3's prose is about collections throughout, and its One entity column is a baseline rather than an instruction, so the practical reading is that dinah-455 builds collection acceptance and leaves head resolution alone. That is the reading recorded here. It costs nothing downstream: nobody builds from section 5.4, and dinah-455's implementer following section 3.3's prose reaches the same place. If the widening is wanted later it is one sentence in dinah-455's description, and it is not a precondition of this contract.\n\nEvidence for every claim above was produced by running a binary built at 22a35fc in a throwaway workbench under C:/dinah-scratch with DINAH_HOME set beside it, and by reading `cmd/dinah/commands.go`, `internal/verb/read.go` and `internal/verb/beyond.go` at that commit and at trunk."
---
Section 5.4's three under-reports stand, and its `show` entries in the workbench and workstream Read cells stand as a summary of a capability rather than as a specification that `show` widens. Round 5's restraint was right, and section 2.5's referral of the question to this review is answered here.