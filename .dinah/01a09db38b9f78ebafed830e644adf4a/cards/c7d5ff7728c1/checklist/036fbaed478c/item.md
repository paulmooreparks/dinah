---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:59Z
ordinal: 12
note: "VERIFIED. Spec-only criterion, unaffected by the trunk move to 8661604; read after the merge against the text now stored on the card. It specifies a guard rather than running one, so what is checked is that section 13.3 says both things.\n\nBOTH ARMS PRESENT. Arm one: \"A hit in any file not on the allowlist fails.\" Arm two: \"Zero hits in internal/guide/guides/references.md fails.\" The section states the two as a pair under \"The test fails in both directions\", and then argues for the second on the exact ground the criterion gives: \"a one-directional guard on a name is satisfied by deleting the name, and a guard anybody can satisfy by deletion is not a guard.\"\n\nMATCHING IS CASE-INSENSITIVE, stated as such and with the reason: \"case-insensitively so that dinahpath and DINAHPATH are caught too\".\n\nTHE ALLOWLIST IS A FIXTURE THE TEST READS, stated explicitly and with its motive: \"a fixture the test reads rather than a list written inside the test, so adding a design document is an edit to data.\"\n\nTHE REFUSED SET COVERS ALL FIVE PLACES THE CRITERION NAMES, checked one by one against the section's own sentence: internal/msg/locales/, Go string literals, params and guides in internal/verb/definition.go (both named, in one clause), internal/mcp/, and any guide other than references.md.\n\nMETHOD NOTE, because it nearly produced a false failure of my own making: my first pass tested for those five as literal substrings of the section joined by newlines, and reported the last one absent because the phrase \"in any guide other than references.md\" wraps across a line break between \"any\" and \"guide\". Normalising whitespace before matching returns true. That was a defect in my check rather than in the spec, and I am recording it because a reviewer re-running a naive substring test will hit the same thing.\n\nCOULD IT HAVE FAILED: yes, and its sibling arm is the reason it exists. A section specifying only the outside-the-allowlist arm passes a naive read and fails this criterion, because the criterion names that exact shape as the failure. A section that put the allowlist inside the test rather than in a fixture fails. A refused set omitting any of the five named places fails; each was checked separately rather than as a group."
---
Spec section 13.3 specifies a guard on the name that fails in both directions: it fails on an occurrence of the token `DinahPath`, matched case-insensitively, in any file outside a declared allowlist, and it fails on zero occurrences in `internal/guide/guides/references.md`. The allowlist is a fixture the test reads rather than a list written inside the test. A guard specified in one direction only fails this criterion, because a one-directional guard on a name is satisfied by deleting the name. Verify by reading 13.3 for both arms and for the fixture, and by checking that the refused set it names covers `internal/msg/locales/`, Go string literals, `params` and `guides` in `internal/verb/definition.go`, `internal/mcp/`, and every guide other than `references.md`.