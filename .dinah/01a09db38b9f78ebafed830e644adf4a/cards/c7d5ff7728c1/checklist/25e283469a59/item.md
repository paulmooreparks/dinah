---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:58Z
ordinal: 5
note: "VERIFIED at 22a35fca90c254625071b9b7eca237131cdce3b0, which is the commit this criterion names, and the trunk move to 8661604 does NOT stale it. The criterion pins its own commit, so its evidence is anchored rather than current, and re-running at the trunk would be running the wrong check. I re-read the criterion after the merge to confirm that, rather than assuming it.\n\nSetup: worktree C:/dinah-scratch/dinah-456-impl/wt22 detached at 22a35fc, binary built into the card's own scratch bin, probe workbench under the same card directory with DINAH_HOME pointed inside it, carrying one comment, one open question and one attachment. Nothing outside the card directory was touched.\n\n2.3, ten invocations, all ten reproduce. path prints the collection directory, exit 0. show, archive, delete --yes, rename, attach, contents and attachments each return the identical sentence \"dinah.unknown-path nothing in this workbench answers to comments; run `dinah ls` to see what this workbench carries\", exit 2. instructions returns the same sentence quoting pb-1/comments, exactly as its row says. That is the eight the closing sentence counts. edit resolves and hands the collection DIRECTORY to the editor: with the reader's editor replaced by a scratch shim it printed the resolved comments directory and exited 0, and a single-entity control printed card.md. I did not launch the operator's own editor, so the row's quoted \"Error 0x80070006: The handle is invalid.\" is the one string here I did not reproduce; that text belongs to the editor rather than to Dinah, and the row's claim about Dinah is what I checked.\n\n2.4, both transcripts reproduce apart from the capture timestamp, which is a wall-clock value rather than a claim: show prints a bare \"#\" position for the attachment, When and Who with no address for the comment, and \"pb-1/oq/1  pending  a question\" for the checklist; contents prints references for all three and prints the item as pb-1/checklist/1, the differing spelling the section is about.\n\n2.5, both probes reproduce: card set pb-1 title returns dinah.unknown-field naming severity, priority, tier; workbench set instructions returns dinah.unknown-key listing lang, actor, editor, workbench.\n\n2.6, all four lines reproduce: attach pb-1/oq/1 exits 0, path pb-1/oq/1/attachments/1 refuses dinah.unknown-path, attachments pb-1/oq/1 reports \"pb-1/checklist/1 carries 1 attachments.\", contents pb-1 reports 3 entities and so is short by one.\n\nCOULD IT HAVE FAILED: yes, and it now fails at BOTH later commits, which is the whole reason round 4 named this one. At 813e0bb, 2.6's first line refuses \"dinah.not-attachable pb-1/checklist/1 is item, and Dinah keeps no attachments on item\" instead of exiting 0. At 8661604 the damage is wider: I built that binary too and dinah show now prints a Ref column for comments and attachments and spells the checklist item pb-1/questions/1, so 2.4's quoted screen no longer matches at all. A tester who read \"the commit the spec names\" as the trunk would today report four true records as failures rather than one.\n\nTHE OLD SPELLINGS IN THESE TRANSCRIPTS ARE CORRECT AND MUST NOT BE FIXED. Section 14.1 states outright that sections 2.3 to 2.6 keep the old spellings because they record what a binary built at 22a35fc printed, and AC-5 checks them against that commit. I confirmed the disagreement is real rather than theoretical by probing the 8661604 build: it prints pb-1/questions/1 while still resolving pb-1/oq/1 on input, exit 0. The contract sections (3.1, 3.2, 4.3) already carry the new words, so the spec is internally consistent and deliberately dated in the two places it needs to be."
---
Every invocation quoted in spec sections 2.3, 2.4, 2.5 and 2.6 reproduces the output quoted beside it, when run against a workbench initialised from a binary built at 22a35fca90c254625071b9b7eca237131cdce3b0 and carrying the same one comment, one open question and one attachment. That commit and not the trunk: this spec names two commits, 22a35fc where these transcripts were captured and 813e0bb where rounds 3 and 4 were written, and dinah-459 landed between them and changed `attach`, so rebuilding at the trunk would report true records as failures. Sections 2.3 and 2.6 each name 22a35fc in their own text. An invocation whose output has changed against that commit, or which the spec quotes without giving the invocation, fails this. Verify by rebuilding at 22a35fc and re-running each line.