---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:58Z
ordinal: 1
note: "RE-VERIFIED at origin/main b825059101980d52eb33dbd5bc62491f4c3c1e7a on 2026-09-09, in worktree C:/dinah-scratch/dinah-456-impl6/wt. This criterion DOES depend on the tree, so it was re-run rather than carried forward. internal/verb/definition.go is byte-identical between 8661604 and b825059 (blob 05fa08b8b6b6520b036a0d5164ba153e13adad12 at both, compared with git rev-parse rather than read off a diffstat), and the two commits since 8661604 are b37486b (dinah-448, documentation guards) and b825059 (dinah-438, internal/bench/blockjson.go). The derivation was re-run anyway: both rosters print 15 and agree, archive attach attachments cite contents delete edit fail instructions path rename reopen resolve show verify, and section 3.3's table parses to the same 15 rows with no duplicate and an empty diff both ways. The three arming arms were re-run at b825059 and each still reports: an added map command with no row printed ['ls'], a deleted row printed ['reopen'], a duplicated row printed ['show'].\n\nVERIFIED. Evidence re-run at origin/main 866160491f590204f752a24b0434dc4b935eb417 after dinah-454 merged; originally run at 813e0bb and the result is the same at both.\n\nRE-RUN WAS REQUIRED rather than optional: 8661604 touches internal/verb/definition.go, which is this criterion's derivation source. The change is confined to one comment line inside the params map (\"short aliases included\" becomes \"the checklist segments' short forms included\"), so it moves neither roster, but that is a fact I established by re-running rather than by reading the diff and inferring.\n\nAt 8661604, section 2.2's derivation script run verbatim in C:/dinah-scratch/dinah-456-impl/wt8661 prints 15 for both rosters and they agree: archive attach attachments cite contents delete edit fail instructions path rename reopen resolve show verify. Section 3.3's table parses to 15 rows, no duplicate, set-equal to the derived set, diff empty both ways.\n\nCOULD IT HAVE FAILED: yes, on three arms demonstrated by perturbing the parsed data rather than by argument. A reference-taking command added to the guides map with no table row is reported (arm A printed ['ls']); a deleted table row is reported (arm B printed ['reopen']); a duplicated row is reported (arm C printed ['show']). The risk was live on this very re-run, since the merge that just landed edits that file."
---
Running the derivation in spec section 2.2 against the commit under review yields a set of command names, and every name in that set has exactly one row in the spec's section 3.3 table. A command in the derived set with no row, or a row naming a command not in the derived set, fails this. Verify by running the script and diffing its two printed lists against the table's first column.