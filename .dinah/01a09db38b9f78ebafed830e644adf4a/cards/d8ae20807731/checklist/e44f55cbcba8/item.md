---
kind: decision
state: resolved
ts: 2026-09-14T02:18:41Z
ordinal: 21
note: "Round 2's guard disagreed with its own rule and shipped red, round 3's guard agreed with its rule but the rule walked past real code, and round 4's guard agreed with its rule while its written limits claimed coverage the rules did not give. All three faults survived because the guard was read rather than attacked. Round 3 keyed family A on a call whose first argument was written as ArchivedCardsRoot() or cardsRootIn(...), which any caller defeats by hoisting the root into a local, and keyed family B on the identifier ArchivedHalf, which any caller defeats by taking the half from halfFor(req); Contents at internal/verb/tree.go:889 already does the second one. The resolution is that a rule keyed on the expression around a name is defeated by rewriting the expression, and a rule keyed on the name is not, so every family collects identifier-node mentions wherever they appear: as a callee, an argument, an assignment, a method value, or a declaration. Family B additionally derives its three names from internal/bench's own declarations, taking every exported method of *Bench whose first parameter has type ResolutionHalf, which is what lets it collect a half-taking resolver nobody has written yet. Family C widened from a comparison of a number to a read of the Number field, because checkCardNumbers already scans by number with a map key and no comparison. Round 5 widened the rule set again, on the finding that three families were three names too few. Family B now reads inside internal/bench as well as outside it, since a fall-through written beside the resolver this card is fixing was otherwise invisible. Family D collects ArchivedCardsRoot, cardsRootIn, and ArchiveDir, which is the reaching axis stated by every name the tree actually uses rather than by the accessor alone. Family E collects a one-argument Ref call and family F the literal \"number\", which is the reading axis stated the same way. Each row asserts its own mention count, so a fall-through added inside an already-recognised function is caught. Thirteen attacks are written out in section 7 with verdicts; twelve fail and the thirteenth is recorded as half an attack in the guard's limits."
---
The standing guard collects mentions of a name rather than calls of a shape, and family B's callee set is derived from the ResolutionHalf parameter rather than written down.