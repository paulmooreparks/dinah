---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:40Z
ordinal: 8
note: "All five plants made in turn, each watched red, restored with git checkout (byte-identical, git reported the file clean afterwards), and watched green. Plant 1, the first-match return in resolveCardIn: internal/bench reddened TestAnAmbiguousNumberNamesEveryCandidateInOrder (\"wanted a refusal and the call answered no error at all\"), TestALinkTargetRefusesAnAmbiguousNumber (\"fx-1 answered the identifier c00000000001\") and TestTheEditTargetRefusesAnAmbiguousNumber, 3 of 4 tests run; cmd/dinah reddened all three of TestAnAmbiguousCardNumberIsRefusedOnTheHead, TestTheAmbiguousCardRefusalNamesCandidatesThatResolve and TestTheAmbiguousCardRefusalCarriesItsCandidatesAsData. Plant 2, the live unwrap of ResolveLinkTarget: \"fx-1 answered the identifier c00000000004\", which is the archived card, plus cmd/dinah's \"two cards answer to fx-1 and the link was recorded\". Plant 3, the archived unwrap of ResolveLinkTarget: \"fx-3 refused unknown-card: fx-3\". Plant 4, the live unwrap of watchedCard: \"fx-1 answered the identifier 1977f3af3be3\", the archived card, plus cmd/dinah's \"changes --card fx-1 watched something\". Plant 5, the archived unwrap of watchedCard: \"fx-3 refused unknown-card: fx-3\" and the head's \"wanted dinah.ambiguous-card\". Every plant compiled and every red run executed its tests."
---
Every test this card adds is armed by breaking the behaviour it guards rather than by deleting an assertion. Five plants are made in turn, one per changed arm: the first-match return is restored in resolveCardIn, the live `err == nil` fall-through is restored in ResolveLinkTarget, the archived `err == nil` fall-through is restored in ResolveLinkTarget, the live fall-through is restored in watchedCard, and the archived fall-through is restored in watchedCard. Each plant is watched going red, the file is restored from a byte-identical copy, and the tests are watched going green. The handoff quotes what each red run said and the test count it ran, so a plant that failed to compile cannot be reported as a pass.