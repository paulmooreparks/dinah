---
kind: decision
state: resolved
ts: 2026-09-14T02:18:41Z
ordinal: 17
note: "resolveCardIn scans one root, so a live card and an archived card sharing a number is not this refusal, and ResolveCard goes on answering from the live half first, which ResolveArchivedCard's own comment already fixes as the order. Three sites try the live half and fall through to the archive on any error. ResolveLinkTarget (resolve.go:913) and watchedCard (internal/verb/changes.go:400) both change, because each answers a card identifier to a caller and would otherwise answer the archived card's. probe (resolve.go:1144) does not, because it answers no card to anybody: it is reached from one caller, notArchivedFor (resolve.go:1093), every return of which is an error, and its whole output is which of two refusal names an already-refused --archived reference prints. Its behaviour under an ambiguous number is not \"the walk finds nothing\", which is true only while the archive holds no card on the number. Where the archive holds one, the live attempt fails on the ambiguity, the archive attempt succeeds, and the walk answers holder = head, so the reader gets the mirror-holder sentence instead of the live one. Both sentences are true of an already-refused reference and neither names a card, so probe is safe either way. The tripwire is what would end that: probe is safe exactly while its result stays a choice between two refusal names, so the moment probe gains a caller that is not notArchivedFor, or notArchivedFor gains a non-error return, this fall-through becomes a fourth instance of the defect and needs the same unwrap ResolveLinkTarget gets. AC-10's family A keeps probe on the recognised list so that reasoning is met rather than reconstructed."
---
Ambiguity is scoped to the half being read, two callers that read both halves are changed, and probe keeps its existing fall-through.