---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:40Z
ordinal: 10
note: "TestEveryArchivedCardResolutionIsDeclared in cmd/dinah/archived_card_resolution_test.go. All six families reproduce the declared tables exactly (A 10/2/8, B 17/6/11, C 15/5/9, D 29/11/24, E 16/8/14, F 3/2/3); family B's derived set comes back as ResolveEntityIn, ResolvePathIn, ResolveReferenceIn. Armed against round 4's attack 8 (reddens D and E) and attack 7 (reddens B). The doc comment carries the six limits with the floor stated honestly: the round 5 review's composed attack and its GoneEntity.Ref route both evade all six, and the comment says so rather than claiming otherwise."
---
A source guard names every route by which a card can be resolved by number against the archived half, in six families on two axes, each keyed on a mention of a name rather than on the shape of a call, each asserted against its own recognised set, and each reporting its own file count, mention count, and enclosing-function count. A test in cmd/dinah parses every non-test .go file under the repository root. Families A, B, and D are the reaching axis and families C, E, and F are the reading axis, so a route has to evade one family on each axis rather than one family in total. Families A through E collect identifier nodes only, never comments and never string literals; family F collects a string literal, because a frontmatter key is one. Family A collects every mention of `resolveCardIn` or `ResolveArchivedCard`, whatever expression surrounds it, and asserts the eight functions and ten mentions in section 7's first table. Family B derives its callee set from internal/bench's own declarations, taking every exported method of *Bench whose first parameter has type ResolutionHalf, asserting that name set (ResolveEntityIn, ResolvePathIn, ResolveReferenceIn today), then collects every mention of one of those three names in any non-test file, inside internal/bench as well as outside it, plus every mention of ArchivedHalf in a non-test file outside internal/bench, and asserts the eleven functions and seventeen mentions in section 7's second table. Family C collects every mention of the field name Number in a selector expression and asserts the nine functions and fifteen mentions in section 7's third table. Family D collects every mention of `ArchivedCardsRoot`, `cardsRootIn`, or `ArchiveDir` and asserts the twenty-two functions, two package-level sites, and twenty-nine mentions in section 7's fourth table. Family E collects every call whose selector is named `Ref` and which carries exactly one argument, asserts from internal/bench's declarations that Card.Ref is the only Ref method taking a parameter, and asserts the fourteen functions and sixteen mentions in section 7's fifth table. Family F collects every basic string literal `"number"` and asserts the three functions and three mentions in section 7's sixth table. Each row's own mention count is asserted rather than only the family's set, so a second resolution added inside an already-recognised function fails the test. Any mention outside a family's recognised set fails with the file, the line, the enclosing function, and the family named. The test runs green on the tree it ships on, which is the verdict `go test ./cmd/dinah/ -run TestEveryArchivedCardResolution` produces, and the six limits in section 7 are restated in its own doc comment.