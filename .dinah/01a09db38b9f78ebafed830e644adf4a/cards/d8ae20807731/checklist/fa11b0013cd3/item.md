---
kind: decision
state: resolved
ts: 2026-09-14T02:18:41Z
ordinal: 15
note: Carried rather than interpolated because the two refusals that name candidate addresses both draw rows, and only ambiguous-name interpolates, which suits it because an attachment ordinal is a one-character token. Identifiers because dinah-471 fixed the raw identifier as the address a card always answers to, so the reader can act on a row without a second lookup. No title because the carried renderer at cmd/dinah/render.go:1016 builds a one-column table and a second column would be a renderer change outside this card. No cap because the duplicate arises from two clones filing from one base, so the realistic count is two and a truncation rule could hide the candidate the caller wanted.
---
The candidates ride as a Carried set of 12-hex identifiers, one per row, every match, with no cap and no title beside them.