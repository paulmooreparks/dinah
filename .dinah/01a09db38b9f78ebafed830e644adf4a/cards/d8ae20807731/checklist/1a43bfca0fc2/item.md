---
kind: decision
state: resolved
ts: 2026-09-14T02:18:41Z
ordinal: 19
note: "Two of the three were found by reading, one per review round, which is why the set was settled by searching for the shape instead of for the names already known. Six searches are written out in the spec's section 6 so a reviewer can judge the sweep: every ResolveArchivedCard call site; every route into the archived half spelled as ArchivedHalf, ArchivedCardsRoot, or cardsRootIn; every occurrence of the `err := ...; err == nil` fall-through text; every ResolveCard call site read one at a time; every read of a card's Number field in non-test code, which returns nine functions of which resolveCardIn alone resolves a reference; and every call from outside internal/bench to a method whose first parameter is a bench.ResolutionHalf, whatever expression the caller writes in that argument. Searches 5 and 6 are what make this a finding, and search 2 is not, which is round 3's correction: search 2 misses tree.go:889, read.go:876, and read.go:887, all of which hand the half over through halfFor(req) and spell none of the three names. Search 5 is exhaustive over the second mechanism because a number cannot be read without the field being named, search 6 is exhaustive over reaching the archived half from outside the package, and searches 1 and 2 together are exhaustive over reaching it from inside, where the half is either the archived root named directly or the parameter cardsRootIn turns into it. Search 5 is also wider than round 3's, which looked for a comparison: checkCardNumbers groups with byNumber[card.Number] = append(...) and compares nothing, so a comparison-keyed search walks past a number scan this tree already contains. Two sites are named rather than fixed because no search can reach them by shape. ResolveEditTarget (resolve.go:262) discards any resolver error and retries through ResolvePath, and the collection probe in Show (read.go:876) does the same and reaches the archived half; both are benign because the retry descends to resolveBelowLanding at resolve.go:599 and re-raises the same refusal, and AC-12 drives that class rather than leaving it asserted. AC-10 turns searches 1, 5, and 6 into a standing source guard in three families."
---
Exactly three sites resolve a card by number and then fall through to the archived half on any error, and that is a finding rather than an absence of evidence.