---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:13Z
ordinal: 2
note: "VERIFIED. TestEveryArchivedHalfCommandRefusesALiveReference in cmd/dinah/restore_test.go runs three references (a live card, a live comment, the workbench) against all four commands, twelve runs generated from the cross product of two declared tables, and logs \"12 invocations ran, 3 references against 4 commands\". Every run asserts exit 2 and a first stderr token of dinah.not-archived; each asserts its branch of the alternation; the workbench arm additionally asserts stderr carries neither `dinah archive workbench` nor `dinah archive .`; and a separate assertion holds the base-sentence split in both directions. TestRestoreAcceptsAnArchivedReference is the accepting half. ARMED with the round-3 blocker itself: with the diagnostic walk removed from Bench.notArchivedFor (holder, collection, found := \"\", \"\", false; compiles and runs) eight of the twelve runs fail reporting unknown-card for the live card and dinah.unknown-path for the live comment, exactly the two tokens the criterion names, while the four workbench runs still pass off the pre-walk call. Restored byte-identically, green. Command: go test ./cmd/dinah/ -run TestEveryArchivedHalfCommandRefusesALiveReference -count=1\n\nROUND 2 OF CODE REVIEW, subject-set pin. The guard over the two tables tested them against being empty, where AC-1, AC-3 and AC-6 all pin their sizes, so dropping a command from archivedHalfCommands left nine passing runs and a log line whose second figure was silently false. It is now `if len(cases) < 3 || len(archivedHalfCommands) < 4` at cmd/dinah/restore_test.go:349, reporting both counts. ARMED by deleting the `path` row from archivedHalfCommands, which is the edit the old guard could not see and the command round 3's blocker was about: the run then fails with \"this sweep drives 3 references against 3 commands and the subject set is the three references against the four commands that take the archived half\" at restore_test.go:350. Restored from a copy taken before the plant, `cmp` byte-identical, and the same command exits 0."
---
All four commands that take --archived refuse dinah.not-archived against a reference the archive does not hold, none of them answers unknown-card or dinah.unknown-path instead, every raise site renders advice the reader can actually carry out, and the same reference restores successfully once it is archived. TestEveryArchivedHalfCommandRefusesALiveReference in cmd/dinah/restore_test.go drives a table of three references, a live card, a live comment and the workbench, against all four commands, `dinah restore <ref>`, `dinah show --archived <ref>`, `dinah path --archived <ref>` and `dinah contents --archived <ref>`, which is twelve runs, and asserts for every one of them exit code 2 and stderr whose first token is `dinah.not-archived`. Driving all four is what makes this criterion see the round-3 blocker: a command whose call chain never reaches Bench.notArchivedFor answers unknown-card or exits 0, and a criterion exercising restore alone cannot tell. It then asserts which branch of section 4.1's five-member alternation each run got. The live card gets a clause naming `dinah archive <ref>` under restore and the clause `drop` plus the flag under the three reads. The live comment gets a clause naming `dinah show --archived <card>/comments` under all four. The workbench gets a clause containing `is never archived` under all four, and its stderr contains neither the string `dinah archive workbench` nor the string `dinah archive .`, which is the advice the first draft of this spec would have shipped for a command the tool refuses by design at internal/verb/beyond.go:238. A separate assertion holds the base-sentence split: every restore run carries `is not archived, so there is nothing to restore` and no read run does, and every read run carries `nothing in the archive answers to` and no restore run does. TestRestoreAcceptsAnArchivedReference then archives the card and the comment and asserts `dinah restore` on each exits 0. Stderr is folded to single spaces before matching, and every match is on the refusal token plus one backticked command spelling or one short fragment rather than on a whole sentence, so a re-wrap at a different terminal width reports nothing.