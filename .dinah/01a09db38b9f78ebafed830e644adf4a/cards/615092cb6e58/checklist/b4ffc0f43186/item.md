---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 7
note: "VERIFIED. TestRestoringAColumnReturnsItToTheOrderAndRepairsAStrandedCard in cmd/dinah/restore_test.go archives an empty column, asserts `dinah columns` no longer lists it and the anchor's columns sequence no longer holds its identifier, restores it, and asserts the listing's LAST row names it and the sequence ends with it. The second arm proves the occupancy scan first (archiving a column a live card stands in is refused), then carries the card out, archives the now-empty column, writes the card's anchor to name the archived identifier by hand, asserts `dinah check --json` reports check.unknown-column, runs the restore, asserts exit 0 rather than dinah.occupied, and asserts the same finding is gone. The finding is read off the machine answer's key rather than off the rendered sentence. ARMED twice: (1) omitting AddColumnID from Bench.Run's restore branch (compiles, runs) fails with the listing ending on done and the sequence [three ids] not ending with the spare id; (2) leaving the occupancy guard as `if act.ColumnID != \"\"` fails with \"restoring the column a stranded card names exited 2 ... dinah.occupied cards still occupy column spare\" and \"the restore left the card stranded\". Restored byte-identically after each, green. Command: go test ./cmd/dinah/ -run TestRestoringAColumnReturnsItToTheOrderAndRepairsAStrandedCard -count=1"
---
Restoring an archived column puts its identifier back into the workbench anchor's columns sequence, at the end, and does so even when a live card still names that column. TestRestoringAColumnReturnsItToTheOrderAndRepairsAStrandedCard in cmd/dinah/restore_test.go archives an empty column, asserts `dinah columns` no longer lists it and the anchor's columns sequence no longer holds its identifier, restores it, and asserts `dinah columns` lists it as the LAST row and the anchor's sequence ends with its identifier. A second arm writes a live card's anchor to name the archived column's identifier by hand, asserts `dinah check` reports check.unknown-column for that card, runs `dinah restore <column>`, asserts exit 0 rather than a dinah.occupied refusal, and asserts the same `dinah check` no longer reports the finding.