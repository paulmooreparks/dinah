---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 8
note: "VERIFIED. TestRestoreRefusedByAnOccupiedSlotRendersItsOwnSentence in cmd/dinah/restore_test.go archives a comment, rebuilds a live directory at the comment's restore target by hand under the archived comment's own identifier, runs the restore, and asserts the first stderr token is dinah.exists, that the folded stderr carries `has nowhere to go back to`, and that it carries neither `workbench.md` nor `choose a different directory`. The second arm runs `dinah init` against a directory already carrying a workbench and asserts it still refuses dinah.exists naming workbench.md. ARMED: changing the Exists shape's Variants from {\"restore\"} to {\"init\"} (compiles, runs) makes the restore render the init sentence, and the run exits 1 reporting the missing restore-sense fragment, the present workbench.md, AND the second arm reporting `{refusal.dinah.exists.init}` unrendered, so both arms redden on the same plant and the cheapest wrong way to pass the first is closed. Restored byte-identically, green. Command: go test ./cmd/dinah/ -run TestRestoreRefusedByAnOccupiedSlotRendersItsOwnSentence -count=1"
---
A restore refused because the slot is taken renders the restore variant of dinah.exists and never the init sentence about workbench.md. TestRestoreRefusedByAnOccupiedSlotRendersItsOwnSentence in cmd/dinah/restore_test.go archives a comment, recreates a live directory at the comment's restore target by hand under the archived comment's own identifier, runs `dinah restore <ref>`, and asserts the first stderr token is `dinah.exists`, that the folded stderr contains the fragment `has nowhere to go back to`, and that it contains neither `workbench.md` nor `choose a different directory`. The same test asserts that `dinah init` in a directory already carrying a workbench still renders the original sentence, so the variant cannot be landed by rewriting the shared entry.