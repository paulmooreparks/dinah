---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 4
note: "VERIFIED, and the four figures have moved because trunk did. `python scripts/derive_event_counts.py` exits 0 against the merged branch. Its derivation reports UNWRITTEN 0, WRITTEN 35, OVERLAP 32 and CARD 31, not the 0/32/29/28 this criterion states: dinah-460 landed three new journal events between the spec's trunk (9260a2a, DECLARED 32) and the commit this card was told to build on (8dfef06, DECLARED 35), and every one of the four figures moved with it. The shape of the criterion holds exactly: the exemption map empties, the script's unwrittenEvents pattern is widened to match the one-line spelling gofmt writes an empty map as, the compat fixture is recaptured so a card journal carries a restored line, and format.md is rewritten to what the script derives rather than to a number typed into prose. The stray bare card identifier at format.md line 20 was already gone at 8dfef06, fixed by a sibling, so the script already exited 0 at trunk. ARMED three ways: (1) leaving contract.EventRestored in unwrittenEvents makes the script exit 1 reporting UNWRITTEN 1, WRITTEN 34 and failing the written-count and card-journal claims; (2) landing the code and leaving format.md's written-count sentence alone made the script print FAIL written count and exit 1, observed during the work; (3) emptying the map against the old regex raised SystemExit, which is why the pattern moved. Restored byte-identically after each, exit 0."
---
`python scripts/derive_event_counts.py` exits 0 against the merged branch, and its derivation reports UNWRITTEN 0, WRITTEN 32, OVERLAP 29 and CARD 28. Test runs the script from the repository root, asserts exit status 0, and asserts those four lines appear in its output. The script itself holds docs/design/format.md to those numbers, so a document left saying thirty-one, twenty-eight or twenty-seven fails through the script rather than through a second count typed into this criterion. It exits 1 at trunk 9260a2a for one reason only, a bare card identifier surviving at format.md line 20, and section 6 of the spec puts that edit in scope precisely so this criterion's exit-status assertion means what it says.