---
kind: decision
state: resolved
column: 0d86ad99cdbc
ts: 2026-09-14T02:18:15Z
ordinal: 16
note: "refusal.dinah.exists reads \"{detail} already carries a workbench.md\" at 9260a2a, so a restore refused through the existing path would tell a reader their card directory carries a workbench anchor. contract.Shapes already carries the Variants mechanism for one name answering two acts, used by Unconfirmed and NoReason, so the fix is two catalogue entries and one fragment rather than a fourth minted name. dinah-456 section 5.5 names dinah.exists for this case and that ruling is honoured. Stated plainly in the spec: the slot is keyed by a twelve-hex identifier that does not collide in practice, so this refusal guards a hand-edited tree or a race rather than an ordinary case, and it is kept because a refusal rendering a false sentence is worse than one nobody meets."
---
dinah.exists gains a restore variant rather than a new refusal name, because Bench.Run already raises dinah.exists on a taken slot and its current sentence is written for init.