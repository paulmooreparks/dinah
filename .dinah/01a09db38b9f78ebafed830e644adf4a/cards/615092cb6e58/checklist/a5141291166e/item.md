---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 10
note: "VERIFIED, and one arming recipe had to change because the note's plant could not redden. TestAnArchivedReadShowsOneHalfAndWritesNothing in cmd/dinah/restore_test.go asserts all three arms: the flagged listing carries exactly one member and the unflagged two, with no member shared (compared on the member anchors rather than on the references, since a reference names a position and a position is counted per half); an archived card's journal is byte-identical before and after `dinah show --archived`; and `dinah contents --archived --json` carries archived true on the tree while the human rendering carries the contents.archived line naming the root. ARMED, first arm: resolving the composed branch under LiveHalf makes the flagged listing return two members and both listings share both members. ARMED, third arm: Tree.Archived set to false fails both the JSON assertion and the human line. THE SECOND ARM'S STATED PLANT DOES NOT REDDEN, and the reason is worth the next reader's time: calling l.lapseRead from Show's archived branch changes nothing, because lapseRead reloads the card through bench.LoadCard(l.Bench.CardsRoot(), ...) and that load fails for a card that is not in the live half, so it returns without writing. The write the assertion guards is therefore structurally out of reach twice over. To arm it honestly I planted the write itself: the lapseRead call in the archived branch AND lapseRead loading from the card's own collection rather than from the live cards root, which is the shape a later card could plausibly give it. Both edits compile and run, and the run then exits 1 with \"reading an archived card wrote to its journal: 465 bytes before, 561 after\". Both files restored byte-identically (git diff empty), green.\n\nWHAT THIS ARM CAN AND CANNOT SEE, recorded at round 2 of code review. The reviewer ran the two halves of the substitute plant separately and neither reddens alone: the load change by itself leaves the suite green because the archived branch does not call lapseRead, and the call by itself leaves it green because lapseRead cannot write. So the check needs both edits at once, and the single most plausible future regression, somebody unifying Show's two bare-head branches and restoring the lapseRead call, would pass it. Say plainly what follows. Omitting lapseRead from the archived branch is belt-and-braces today; what carries that omission is D-10's argument rather than any check in the tree, and this criterion guards the outcome (an archived read leaves the journal byte-identical) rather than the code decision it is credited with. Making a test see the omission on its own would mean weakening lapseRead so it could write to a card outside the live half, which is a worse tree than the gap. No code change was made for this, deliberately."
---
A read under --archived shows the archived half alone, does not write to an archived card's journal, and says so where the addresses it prints do not resolve. TestAnArchivedReadShowsOneHalfAndWritesNothing in cmd/dinah/restore_test.go asserts three things against one workbench. First, with two comments live and one archived, `dinah show --archived pb-1/comments --json` lists exactly one member and `dinah show pb-1/comments --json` lists exactly two, and neither listing carries a member the other does. Second, a card carrying a claim whose lease has lapsed is archived, `dinah show --archived <card>` is run, and the archived card's journal is byte-identical before and after, so no expired event was written. Third, `dinah contents --archived <card> --json` carries archived true on the tree and the human rendering carries the contents.archived line naming the root's reference.