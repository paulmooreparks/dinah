---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 3
note: "VERIFIED, with one deviation stated. TestTheArchivedHalfIsReadAtTheDeepestCollectionStep in internal/bench/resolve_archived_half_test.go drives ten rows through ResolvePathIn(ArchivedHalf, ref) and fatals on a table of fewer than ten. Every expected path is the path the fixture's own archiving produced, taken from ArchiveEntity's answer rather than composed from a layout constant. DEVIATION: section 3's table writes every row against pb-1, and two of its rows need that card archived while four need it live, which one workbench cannot hold; the archived-head rows are driven against a second card of the same fixture and the doc comment says so. The positional claim is its own test, TestAPositionUnderTheFlagCountsTheMirrorsOwnMembers, which asserts the flagged answer, the unflagged answer AND that the two differ. ARMED: with descend's deepest-step test changed from len(segments) == 2 to false for the member case (compiles, runs) the run exits 1 and reports the live comment path against the expected mirror path, plus three attachment rows refusing. Restored byte-identically, green. Command: go test ./internal/bench/ -run 'TestTheArchivedHalfIsReadAtTheDeepestCollectionStep|TestAPositionUnderTheFlagCountsTheMirrorsOwnMembers' -count=1"
---
`--archived` reads the mirror at the reference's deepest collection step and the live half above it, and positions under the flag count the mirror's own members. TestTheArchivedHalfIsReadAtTheDeepestCollectionStep in internal/bench/resolve_archived_half_test.go builds one workbench and drives every resolving row of the spec's section 3 worked table, ten rows, through ResolvePathIn(ArchivedHalf, ref) and compares the answer with the path the fixture wrote the entity to. The table's eleventh row is the workbench, which resolves nothing and is pinned by AC-2 and AC-9 instead, so the ten here is the whole of what this test can drive rather than a row silently dropped. The table is a declared slice in the test and the run fails when it holds fewer than ten rows, so a row deleted to make a failure go away stops the run. Two rows carry the positional claim on their own: with two comments archived in a known order and one live comment standing beside them, `pb-1/comments/1` under the flag resolves to the FIRST ARCHIVED comment and `pb-1/comments/1` without the flag resolves to the live one, and the test asserts the two answers differ as well as asserting each is right.