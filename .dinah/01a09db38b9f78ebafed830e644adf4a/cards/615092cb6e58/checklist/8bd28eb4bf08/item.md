---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 6
note: "VERIFIED. TestTheRestoreKeysAreCarriedInEveryCatalogue in internal/msg/restore_keys_test.go declares the fifteen keys as a slice, fatals on fewer than fifteen, enumerates the catalogue directory rather than listing the languages and fatals on fewer than eight files, and logs what it read: \"8 catalogue files enumerated, 120 entries read\". Per key it asserts presence in every catalogue; a non-empty en text and context; de and hi carrying a text differing from en's, no skeleton mark, and a source equal to msg.Fingerprint(en text); and the five skeletons carrying en's text byte for byte with skeleton true and no source. Every comparison is on JSON values. The fingerprint routine was checked against two shipped entries before any key was written. ARMED: renaming contents.archived in hi.json alone (one key, valid JSON) makes the run exit 1 reporting \"hi carries no entry for contents.archived\" and \"the sweep read 119 entries and 15 keys across 8 catalogues is 120\". Restored byte-identically, green. Command: go test ./internal/msg/ -run TestTheRestoreKeysAreCarriedInEveryCatalogue -count=1"
---
All fifteen new message keys stand in all eight catalogues under internal/msg/locales, with de and hi carrying non-English text stamped with msg.Fingerprint of the English, and cs, id, es, fil and af carrying the English byte for byte with skeleton true and no source. TestTheRestoreKeysAreCarriedInEveryCatalogue in internal/msg/restore_keys_test.go declares the fifteen keys as a slice, fails when the slice holds fewer than fifteen, and for each key asserts: the key is present in every one of the eight catalogues, en carries a non-empty text and a non-empty context, de and hi carry a text differing from en's and a source equal to msg.Fingerprint(en text), and the five skeleton catalogues carry a text byte-identical to en's with skeleton true and no source field. The eight catalogues are enumerated by reading the locales directory rather than listed, and the run fails when it finds fewer than eight files. The count is fifteen rather than the eleven the first draft of section 7 stated, because section 4.1's alternation carries a restore base entry and four next-step branches where the first draft carried one base entry and two.