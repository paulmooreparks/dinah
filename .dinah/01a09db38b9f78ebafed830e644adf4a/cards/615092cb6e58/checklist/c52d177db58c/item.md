---
kind: decision
state: resolved
column: 0d86ad99cdbc
ts: 2026-09-14T02:18:15Z
ordinal: 15
note: "dinah-456 section 3.4 minted two names on the ground that widening unknown-path a third time would tell a reader something they can see does not exist, and both were read against this case first. is-a-collection is about a reference naming a set, not-attachable is about a kind that mounts no collection, and neither describes an entity that exists in the other half. unknown-card and unknown-path are both false here.\n\nWHERE IT IS RAISED, corrected in round 3. The decision previously said the single raise site was Bench.ResolveReferenceIn under ArchivedHalf and that all four commands therefore answered alike. That was false for two of the four, traced in round 3: Library.Show discards the resolver's error at internal/verb/read.go:816 by design (its own comment at :809 says why) and answers ResolvePath's instead, Library.Show's bare-head branch reaches neither resolver, and ResolvePath never calls ResolveReference at all, so show --archived on a live card and path --archived workbench both answered as though the flag had not been passed. The rule now lives in Bench.notArchivedFor, whose only callers are Bench.ResolveReferenceIn and Bench.ResolvePathIn, each calling it twice under ArchivedHalf: once before the walk and once on the walk's failure. Two entry points rather than one because neither can be written in terms of the other, which their own doc comments state at 9260a2a: ResolvePathIn answers an attachment's payload that ResolveReferenceIn refuses, and ResolveReferenceIn answers a collection that ResolvePathIn answers as a directory. Library.Show gains a bare-head branch under the flag that goes through ResolveReferenceIn, so all four commands reach the refusal on their real call chains; spec section 4.1 carries the per-command trace table.\n\nWHY THE PRE-WALK CALL EXISTS. Neither walk fails on the workbench. ResolveReferenceIn answers it at its own top and resolveBelowLanding answers a bare workbench head at internal/bench/resolve.go:399 with the live anchor and a nil error, so a refusal left to the failure path alone would never fire for the one reference the archive can never hold.\n\nWHAT THIS NOTE SUPERSEDES. Round 2 replaced the live-retry mechanism this note used to describe with the diagnostic walk of spec section 4.1, which reads live first and the mirror second at every step; the earlier wording survived that edit because the sweep searched phrases rather than the claim."
---
dinah.not-archived is minted as a third refusal name rather than widening dinah.unknown-path, dinah.is-a-collection or dinah.not-attachable, and it is decided in one declared function, Bench.notArchivedFor, which the two archived-half entry points call.