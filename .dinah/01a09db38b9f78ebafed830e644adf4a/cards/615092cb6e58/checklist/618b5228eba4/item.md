---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:13Z
ordinal: 1
note: "VERIFIED. TestRestoreIsTheInverseOfArchiveForEveryArchivableKind in cmd/dinah/restore_test.go drives all six archivable kinds as named subtests (column, card, comment, item, attachment, workstream) and fatals on a table of fewer than six. Each kind: the entity directory is recorded from `dinah path <ref>`, archived, asserted gone, restored, and every file compared byte for byte against the snapshot taken before. The one file excluded from the byte comparison is the journal the two events land on, which sits inside the directory for a card and for a workstream and legitimately grows by two lines because the format appends the event before the move; that file is asserted by its events instead. ARMED: with Library.Restore's event changed to contract.EventArchived (internal/verb/beyond.go, one identifier, compiles and runs) the run exits 1 and five subtests report \"the journal for <kind> carries no archived line followed by a restored line noting <id>\". Restored byte-identically from a copy taken before the plant; the same command then exits 0. Command: go test ./cmd/dinah/ -run TestRestoreIsTheInverseOfArchiveForEveryArchivableKind -count=1"
---
A round trip through archive and restore returns every one of the six archivable kinds to the exact directory it came from, with its journal carrying archived then restored, and the accepting case is pinned beside the refusing one so a build that refuses everything fails. TestRestoreIsTheInverseOfArchiveForEveryArchivableKind in cmd/dinah/restore_test.go builds a workbench carrying a column, a card, a comment on that card, a checklist item, an attachment on the card and a workstream, and for each: records the entity directory from `dinah path <ref>`, runs `dinah archive <ref>`, asserts the live directory is gone, runs `dinah restore <ref>`, asserts the directory is back at the recorded path byte for byte, and asserts `dinah log`/the entity's own journal carries an `archived` line and a `restored` line in that order, each with `note` equal to the entity's twelve-hex identifier. The subject set is named rather than counted, and the test fails when it covers fewer than six kinds, so a fixture that quietly stops creating one of them stops the run instead of passing for free.