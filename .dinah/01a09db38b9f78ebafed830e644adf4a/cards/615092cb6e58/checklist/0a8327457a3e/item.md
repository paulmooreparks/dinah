---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:18:14Z
ordinal: 9
note: "VERIFIED. internal/mcp/roster_test.go's TestEveryLibraryCommandIsServedOrExempted passes with toolExemptions untouched at its nine entries. TestTheRestoreToolIsServedWithItsReference in internal/mcp/restore_tool_test.go reads the schema off what tools/list actually serves, asserts a ref property and ref in required, calls the tool against an archived card and asserts it answers ok with the same identifier the library's own archive reported, then calls it again and asserts dinah.not-archived. TestTheArchivedFlagIsPublishedAndHonouredOnEveryCommandThatTakesIt asserts show and contents each publish an archived property of type boolean, and that ResolvePathIn(ArchivedHalf, \"workbench\") refuses dinah.not-archived, which is what runPath reaches once its pre-bench shortcut is guarded off. The shortcut itself is asserted at the terminal by AC-2's path-against-workbench run. ARMED: deleting the restore entry from internal/mcp/tools.go (compiles, runs) makes both the roster check and the new test exit 1, reporting \"the library defines restore and this head neither serves it nor names a reason it is absent\" and \"this head serves no tool called restore\". Restored byte-identically, green. Command: go test ./internal/mcp/ -run 'TestEveryLibraryCommandIsServedOrExempted|TestTheRestoreToolIsServedWithItsReference|TestTheArchivedFlagIsPublishedAndHonouredOnEveryCommandThatTakesIt' -count=1"
---
restore is served as an MCP tool and its schema publishes the reference, and the four commands that take --archived publish or accept it. The existing roster check in internal/mcp/roster_test.go, which requires every library command to be served or named in toolExemptions with a reason, is run and passes with toolExemptions holding its nine existing entries and no tenth. TestTheRestoreToolIsServedWithItsReference in internal/mcp/restore_tool_test.go asserts tools/list carries a tool named restore whose input schema declares a required ref property, and that calling it against an archived card returns the same outcome the CLI returns for the same reference. A second arm asserts the show and contents tool schemas each declare an archived boolean property, and that `dinah path --archived workbench` at the CLI refuses dinah.not-archived rather than printing the live anchor's path.