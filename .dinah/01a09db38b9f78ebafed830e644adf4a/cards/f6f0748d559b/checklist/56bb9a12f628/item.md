---
kind: decision
state: resolved
column: 4b38abe7ebd5
owner: holder
ts: 2026-09-14T02:17:30Z
ordinal: 34
note: "AC-14's text and its note disagree, and the disagreement is not resolvable by choosing one and ignoring the other.\n\nThe TEXT places both Go assertions in `cmd/dinah` under `runCLI`: the field is \"present, absolute and names an existing file, and is absent when the seam is made to fail\". The NOTE and the spec's Files section spell the seam `var executablePath = os.Executable`, unexported, \"beside the existing `var statPath = os.Stat`\", and say \"the test swaps it\". `bench.statPath` is swapped by `internal/bench/path_under_root_test.go`, a test in its own package, which is the only way an unexported package-level var can be swapped. A `cmd/dinah` test cannot reach `verb.executablePath` at all, so the criterion's own two halves cannot both be satisfied.\n\nI took the TEXT, because the location is what the criterion asserts and the spelling is what its reasoning illustrates, and because the assertion the TEXT asks for is the stronger one: `runCLI` drives the whole head and reads the CLI's own stdout, where a `verb`-package test would marshal the report directly and prove less. So `read.go` carries `var ExecutablePath = os.Executable` with a doc comment saying why it is exported, and `cmd/dinah/version_executable_test.go` swaps it and restores it under `t.Cleanup`, which is `path_under_root_test.go`'s own shape.\n\nWhat the export costs: `internal/verb` is an internal package, so nothing outside this module can see it, and no contract, format or ABI carries it. `verb.ToolRelease` is already an exported mutable package var for the same kind of reason. I searched for prior art with `grep -rn \"^var [A-Z]\" internal/verb` and `grep -rn \"verb\\.[A-Z][A-Za-z]* = \" cmd/dinah`: the repository has no exported function-valued seam and no cross-package seam swap today, so this is the first, and a reviewer who would rather have the unexported spelling has to move the absence assertion into `internal/verb` and say so on AC-14."
---
The `os.Executable` seam is exported as `verb.ExecutablePath` rather than unexported as the spec's code block spells it, because AC-14 puts the failure arm in `cmd/dinah` and an unexported seam is out of that package's reach.