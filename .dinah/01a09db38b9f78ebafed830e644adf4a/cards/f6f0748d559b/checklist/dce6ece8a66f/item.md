---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:27Z
ordinal: 9
note: "The first half arms D-4, and it is structural rather than executed, because the executed form cannot fail. The claim worth holding is that no localiser can reach the composition. The check that holds it is a sweep asserting that `editors/vscode/src/mcpServers.ts` imports nothing from `./l10n`, contains no call of the form `t(`, and was actually read, which is its length asserted greater than zero, because a sweep that read nothing proves nothing. The call-form match is `/\\bt\\(/`, a whole-identifier `t`, which does not fire on `split(` or on any other identifier ending in `t`; a guard that fires against correct code is worse than no guard, and that regex was checked against `a.split(\"/\")`, `format(x)` and `isAbsolute(p)` before being written here. WHAT THIS ASSERTION TOUCHES: regex matches against the text of one file, plus that file's length. PLANT: add `import { ENGLISH } from \"./l10n\";` to `mcpServers.ts` and route the `Dinah: ` prefix through it, and the sweep must go red naming the import.\n\nNo eight-tag loop is written, and an earlier draft of this note asked for one. `planMcpServers(targets, executable, toolVersion, caseInsensitive)` takes no `Localizer` and no `t`, which is D-4's whole point, and no module under `src/` holds a module-level localizer, since `createLocalizer(resolveTag(vscode.env.language))` is called once at `extension.ts:361` and is threaded by argument thereafter. A loop over `SUPPORTED_TAGS` would therefore call one function eight times with identical arguments and compare eight identical results, so it could not fail, and asserting the tag count would not save it because that counts iterations rather than anything reaching the function. Its plant could not be performed either: routing the prefix through `t(\"...\")` needs a `t` the test never passes, so performing the recipe means editing the test as well as the code, which is not a plant.\n\nThe second half arms the card's boundary and was rewritten at Spec round 2 on the Agent Design Review finding that the draft version planted a false failure for later cards. The draft froze a copy of `TREE_COMMANDS` as it stands on `65a80ad` inside the suite, which holds this card's boundary and also reddens the next card that legitimately adds a command, with a message saying a command was added that would be correct and unwanted, and a guard that fires against correct code is the shape the workbench instructions refuse. The boundary is a property of this card's diff rather than of the repository's steady state, so it is checked where it is true: Test runs `git diff --unified=0 <merge base>..HEAD -- editors/vscode/src/identity.ts editors/vscode/package.json` and reads it, and the criterion is verified by that diff showing no added line inside the `TREE_COMMANDS` array and no added entry under `contributes.commands`. WHAT THIS HALF TOUCHES: this card's own diff, read by whoever verifies the card rather than by a runner; nothing in the suite asserts it and nothing re-checks it afterwards. The two existing guards continue to hold the rosters against each other, which is the steady-state property and needs no help from this card: `assertCommandsFullyRegistered` refuses a registration that does not match the roster, and \"the manifest declares exactly the commands identity.ts names\" refuses a manifest that drifts from it. FALSE FAILURE: none is available on either half, because the sweep reads one file this card creates and the diff check reads this card's own diff, and a later card's diff is a different diff."
---
The composed label set is byte-identical under all eight display tags, and the card's own diff against its merge base adds no entry to `TREE_COMMANDS` and no entry to `contributes.commands`, so nothing this card adds is translated at run time and nothing it adds is a command.