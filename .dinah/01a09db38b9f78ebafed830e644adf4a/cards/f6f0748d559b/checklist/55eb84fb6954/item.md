---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:27Z
ordinal: 2
note: "The settings assertion already compares the whole key list with `deepEqual`, so it fails the moment a fifth property is added without being declared there, which is the property that makes this criterion non-vacuous. PLANT: set the scope to `resource` and the test must go red naming `window` as what it wanted. SECOND PLANT: set `default` to `false` and it must go red; presence alone would not catch that, which is the failure mode the workbench instructions name, because a reader who never opened settings would silently get no registration while a test asserting only that the key exists stayed green. The corpus clause was added at Agent Design Review, 2026-09-11, and widened at Spec round 2 the same day. The guard at `manifest.test.ts:875` counts its corpus twice, once by the expression derived from the manifest at lines 914-923 and once against the literal 11 at line 925, collecting the strings themselves at lines 887-908, and on `65a80ad` that total is 1 manifest description plus 4 setting markdownDescriptions plus 6 welcome blocks. Those are three separate places and a change reaching the arithmetic alone would move the count without extending the collection. This card adds a fifth setting carrying a markdownDescription, which the derived half picks up on its own and the literal half does not, and it adds one provider label, which neither half sees at all because the collector reads only the manifest description, both description forms of every contributed setting, and the welcome blocks. Round 2 closes that gap rather than declaring the label unguarded: the collector gains a loop over `contributes.mcpServerDefinitionProviders`, pushing each entry's `label` with `where` reading `the <id> provider's label`, and the `declared` expression gains the same array's length as a fourth term. `resolveNls` at `manifest.test.ts:84-105` walks the whole manifest, so the collector receives the English the label's `%key%` resolves to rather than the key itself. The literal therefore reads 1 + 5 + 6 + 1, which is 13. THIRD PLANT: land the new setting and leave the literal at 11, and the guard must go red reporting 13 against 11. FOURTH PLANT: write a provider label that claims the extension carries a dinah binary, such as \"Dinah workbenches, binary included\", and the guard must go red naming the provider's label as the string that said so; this is the plant that proves the collector extension is real rather than a count adjustment, and it must be confirmed to have gone red for the label specifically rather than for the count."
---
`manifest.test.ts:347`'s settings assertion is extended so the contributed property list is exactly the four existing keys plus `SETTING_REGISTER_MCP`, and that property's `scope` is `window`, `type` is `boolean` and `default` is `true`; the corpus at `manifest.test.ts:875` gains every `contributes.mcpServerDefinitionProviders` entry's `label`, on the derived half and the literal half together, and the literal moves from 11 to 13 in the same diff.