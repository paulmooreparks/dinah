---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:28Z
ordinal: 16
note: "Filed at Agent Design Review round 2, 2026-09-11, because the card as it stood could not work. `readReport` at `version.ts:93-107` builds its result field by field, `return { tool: report.tool, profile: report.profile, format: report.format }`, so it drops every key it does not name. It is the only decode of `--json version` in the extension: `activate` calls `classifyVersion(await runDinah(...))` at `extension.ts:363-368`, `classifyVersion` calls `readReport`, and the `ok` arm of `toState` in `binary.ts:38-44` carries that object out as `binary.version`. Adding the field to the `VersionReport` interface in `api.ts` alone therefore leaves `binary.version.executable` undefined for every binary, whatever the CLI reports, and the whole feature publishes nothing. The spec's Files section now names `version.ts` alongside `api.ts` and says so.\n\nTwo other things in that module go stale in the same diff and are part of this criterion's work: the module header at `version.ts:3-4` says `dinah --json version` \"reports three fields with three jobs\", and `readReport`'s own doc comment says \"Reads the three fields off a parsed `--json version` payload\". Both become wrong the moment the field lands, and the header's next clause, \"and this gate reads two of them\", is still true and must survive the edit.\n\nWHAT THIS ASSERTION TOUCHES: the `executable` on the `VersionReport` that `classifyVersion` returned, compared against the value in a payload the test itself wrote, in three cases. It proves the decode carries what it is given and proves nothing about what the CLI sends, which is why AC-17 exists.\n\nA hand-built fixture is ruled out in the criterion text on purpose. AC-15's arms construct a `BinaryState` directly, so they would pass over a decoder that drops the field, which is exactly how this defect reached review. `test/unit/version.test.ts:15` already has the `reported(tool, profile, format)` helper that builds a `CliOutcome` carrying parsed JSON, so the test goes through the real decode.\n\nPLANT: land the field on the interface and leave `readReport` unchanged, and this criterion's first case must go red reporting an undefined `executable` where the payload carried one. SECOND PLANT: return the payload's `executable` without checking its type, feed a number, and the third case must go red carrying a number where absence was wanted."
---
`readReport` in `editors/vscode/src/version.ts` carries the new `executable` field through onto `VersionReport`, accepting a string, treating a value of any other type as absent rather than refusing the whole report, and a test asserts all three cases against a parsed payload rather than against a hand-built `VersionReport`.