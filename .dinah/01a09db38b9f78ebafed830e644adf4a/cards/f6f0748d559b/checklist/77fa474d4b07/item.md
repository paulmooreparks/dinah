---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:27Z
ordinal: 5
note: "Expected: `Dinah: Ledger`; `Dinah: wt` from root `C:/scratch/wt` with an empty title; two plans each carrying the root in parentheses rather than only the second; and `Dinah: Dinah` for the genuinely-titled one, which must NOT collide with the untitled case because the untitled case never produces that string. PLANT: reintroduce `rootsFor`'s substitution by having `mcpTargets` return `UNTITLED_WORKBENCH` for an empty title, and the untitled assertion must go red reading `Dinah: Dinah` where `Dinah: wt` was wanted. That plant is the one that proves D-4's reason for minting `mcpTargets` was real rather than decorative. SECOND PLANT: disambiguate only the second of two colliding plans and the collision assertion must go red on the first plan's label.\n\nTHE CASE FOLD, settled at Implement 2026-09-11 on Agent Code Review's first minor, branch commit c548c7e. The spec's Labels section ends that the collision check uses the same `key(root)` fold the deduplication uses, folding case where `caseInsensitive` is set and not otherwise. The code compared the composed label strings exactly, so two workbenches titled `Ledger` and `ledger` published two bare labels on a platform whose paths fold case, and neither carried a root to tell them apart. Settled in favour of the spec rather than against it: `editors/vscode/src/mcpServers.ts` now folds both the collision set and the lookup through `key(label, caseInsensitive)`, and the function's doc comment says which fold and why.\n\nThis criterion's test gains both halves of that fold, which is what the divergence shipped without. `editors/vscode/test/unit/mcpServers.test.ts:169` asserts that with `caseInsensitive` true the pair renders `[\"Dinah: Ledger (C:/a)\", \"Dinah: ledger (C:/b)\"]`, and `:179` asserts that with it false the same pair renders `[\"Dinah: Ledger\", \"Dinah: ledger\"]`. ARMED by reverting the fix in place, comparing the composed labels exactly again: the true-arm assertion went red reading actual `['Dinah: Ledger', 'Dinah: ledger']` against the expected disambiguated pair, 7 of 8 passing in that file. Restored byte-identically from a copy taken before the plant and the file went green again; the whole extension unit suite is 573 of 573."
---
The label rules are asserted in all four cases: a titled workbench, an untitled one, two workbenches sharing a title, and a workbench whose title is literally `Dinah`.