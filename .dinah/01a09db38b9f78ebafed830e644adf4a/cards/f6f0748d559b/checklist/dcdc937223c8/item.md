---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:28Z
ordinal: 17
note: "Filed at Spec round 3, 2026-09-11, from the walk that asks what each assertion touches at the moment it runs. AC-14's second arm touches the raw JSON a built `dinah` printed and never reaches `readReport`. AC-16 touches `readReport` and never reaches a binary, because the payload it decodes is written by the test. So the CLI could stop sending the field, or the decode could start dropping it, with the criteria on either side of that seam still green. This criterion is the join, and it costs nothing extra: the build and the spawn are AC-14's, already paid in the same file. Agent Design Review round 3 performed its first plant for real, patching `read.go`, building a binary, running it, and pushing the real payload through `runDinah`, `classifyVersion` and `readReport`: raw payload keys `tool,profile,format,executable`, decoded keys `tool,profile,format`, decoded `executable` undefined.\n\nWHAT THIS ASSERTION TOUCHES: `realpathSync.native()` of the `executable` on the `VersionReport` that `classifyVersion` returned, decoded from the payload a built binary actually printed, compared against `realpathSync.native()` of the path `buildBinary` returned. It is the only assertion on the card whose two operands come from opposite sides of the CLI-to-extension seam.\n\nIt runs in the same isolated shape AC-14's second arm does, with `cwd` at the temp root `buildBinary` created and `fixtureEnv(root)` for the environment, so neither the discovery walk nor any write reaches the operator's own workbenches. The comparison is the same one AC-14 uses: `realpathSync.native()` applied to each side and no other fold.\n\nPLANT: land the field on `VersionReport` and in the CLI, leave `readReport` unchanged, and this criterion must go red reporting an undefined `executable` while AC-14's second arm stays green on the same run. That contrast is the point of the criterion, so the run must be read for both, not just for the red. SECOND PLANT: have `readReport` return a constant absolute path instead of the payload's value, and this criterion must go red because the constant does not equal the path `buildBinary` returned, which proves the assertion reads the decoded value rather than merely an absolute string.\n\nFALSE FAILURE: the candidate is a path-spelling difference between what `go build -o` was given and what `os.Executable` reported, and `realpathSync.native()` on both sides removes it by canonicalising each operand with one documented call rather than by guessing which transformations the two spellings differ by. Node's `fs` page documents that call as synchronous `realpath(3)` and POSIX documents `realpath()` as producing an absolute pathname naming the same file with no `.`, no `..` and no symbolic link left in it. An earlier draft named Windows separators and case as the only candidate and compared after `resolve()` with a case fold, which does not reach either real case: the Windows CI leg's `os.tmpdir()` reads a short-name `TEMP` of the form `C:\\Users\\RUNNER~1\\AppData\\Local\\Temp`, and a POSIX host's temporary directory may carry a symlinked component, and neither is a separator difference or a case difference. Settling which way either one falls by measuring it is what the operator's no-undocumented-behaviour rule refuses. A difference that survives canonicalisation on both sides is a real failure and is what the second plant relies on; if the Windows leg ever produces one, that is a finding to report rather than a reason to widen the comparison."
---
In `editors/vscode/test/unit/versionExecutable-live.test.ts`, the payload the built binary actually returned is decoded by `classifyVersion`, and the resulting `version.executable` is asserted to equal the path `buildBinary` returned, both sides through `realpathSync.native()`, so one assertion crosses the CLI and the extension's own decode together.