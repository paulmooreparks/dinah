---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:28Z
ordinal: 15
note: "This is the criterion that holds D-9 and D-11 to the code. Four arms against one fixture, because a callback that always returns an empty array would satisfy any three of them: `binary.state` is `no-binary`, expect none; the state is `ok` and `version.executable` is undefined, which is what an older binary that predates AC-14 reports, expect none; the state is `ok` and `executable` reads `dinah`, a relative answer no documented path produces but the one a future regression would produce, expect none; the state is `ok` and `executable` is an absolute path, expect exactly one definition whose `command` is that path. Asserting only the refusing arms would pass against a provider that never publishes anything, which is the refusal-that-any-value-satisfies shape, so the publishing arm sits beside them in the same test.\n\nWHERE THE DECISION LIVES, rewritten at Agent Design Review round 2, 2026-09-11. Round 2 put this ladder inside the inline `provideMcpServerDefinitions` arrow function in `extension.ts`, and no test on this card can reach it there. `test/unit/layers.test.ts:161-191` allows `extension.ts` alone to import the `vscode` module as a value, its own comment records that no unit test imports `extension.ts`, and a unit test that did would fail at load because `vscode` does not resolve outside an editor host; the sibling test at `193-196` keeps that rule non-vacuous. The spec's own out-of-scope list rules out the integration suite. So as round 2 specified it, the refusal this card's whole seam dissolve rests on could not be exercised by any test the card allows, which is the same failure as a criterion naming a guard that never opens the file it claims to. The repair follows this extension's established shape, which is a pure module holding the decision and `extension.ts` holding only the wiring, as `status.ts`, `diagnostics.ts` and `binary.ts` already do. `mcpServers.ts` imports `BinaryState` from `api.ts` as a type, so it can take the whole state and decide. `extension.ts` reads the setting with `settingOf` and calls in.\n\nThe middle clause of the criterion's text, that no branch of the decision is left in `extension.ts`, is asserted by the second sweep rather than by the second plant, and it was asserted by nothing before Agent Design Review round 3 found that. The second plant moves the absoluteness check up AND deletes it from the pure function, which the third arm catches; an implementer who copies the check up while leaving the original in place trips neither the arms nor the first sweep, and that is exactly where the drift starts. So `isAbsolute` must reach no file under `editors/vscode/src/` other than `mcpServers.ts`. On `65a80ad` `isAbsolute` appears nowhere under `src/` and `node:path` is imported there only by `extension.ts:8`, so the sweep is clean on trunk and non-vacuous once the card lands.\n\nBoth sweeps are claims about a whole tree, so each is produced by a command over the tree rather than by reading `mcpServers.ts`: every `.ts` file under `editors/vscode/src/` is read, the count read is asserted greater than zero so a glob that matched nothing fails, and each sweep names its own offenders. `PATH_NAME` may reach `binary.ts` and no other file, because rung 2 of the ladder spawns the bare name to probe it, and probing it is exactly what produces the absolute path this card publishes instead.\n\nWHAT THESE ASSERTIONS TOUCH: four arrays returned by `publishedMcpServers`, compared against emptiness and, on the publishing arm, against a one-element array whose `command` is the absolute path the test put on the constructed `BinaryState`; and regex matches against the text of every `.ts` file under `src/`, with each sweep's file count compared against zero. Each sweep compares a walked file path against a literal file name through one `relative()` call, which is the shape `layers.test.ts:161-191` already uses, so neither rests on two tools spelling one path alike.\n\nPLANT: make the decision fall back to `PATH_NAME` when `executable` is missing and both the second arm and the `PATH_NAME` sweep must go red, the arm naming the published command and the sweep naming the file and line. SECOND PLANT: move the absoluteness check back into `extension.ts` and delete it from the pure function, and the third arm must go red, which is what proves the decision is where this criterion says it is. THIRD PLANT: copy the absoluteness check into `extension.ts` while leaving it in the pure function, and the `isAbsolute` sweep must go red naming `extension.ts`, which is the case neither the arms nor the second plant can see. This criterion is weaker than it looks and says so: the sweeps catch the fallback and the copy somebody would actually write and cannot catch a bare name composed character by character."
---
A pure function in `editors/vscode/src/mcpServers.ts`, taking the binary state and the setting's value, publishes nothing and no definition carrying a bare name whenever the binary did not resolve, whenever the resolved binary reported no `executable`, and whenever it reported one that is not absolute; it publishes normally when the reported path is absolute; and two repository-wide sweeps, each asserting the number of files it read, require that `PATH_NAME` reaches no file under `editors/vscode/src/` other than `binary.ts` and that `isAbsolute` reaches none other than `mcpServers.ts`, so the absoluteness rung cannot be copied back into `extension.ts` unnoticed.