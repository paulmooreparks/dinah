---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:27Z
ordinal: 4
note: "Three inputs, each its own assertion: `C:/w` and `C:\\w` fold to one plan on both settings; `C:/W` and `C:/w` fold to one plan when `caseInsensitive` is true and stay two when it is false; `C:/w` and `C:/w-old` stay two under both, which is the whole-string property that a text-prefix comparison would get wrong.\n\nWHERE THE FOLD LIVES, rewritten at Spec round 4, 2026-09-11, from the walk that asks what each assertion touches. This criterion's assertion reads the length of `planMcpServers`'s returned array, and the spec had put the deduplication in `DinahTreeProvider.mcpTargets()`, folding on the provider's private `rootKey` at `tree.ts:1691`. So the criterion named one function and asserted a property of another, which is this card's recurring defect, and an implementer following the spec would have made it fail against correct code. The fold moves into `planMcpServers`, which already takes `caseInsensitive` as an argument and is what the test calls. The key is written out rather than shared: `key(root)` replaces every `\\` with `/` and lowercases the result when `caseInsensitive` is true. `rootKey` stays private and unchanged, because it reads `this.deps.caseInsensitive` rather than an argument and belongs to the tree.\n\nBoth operands of every comparison here are roots the fixture supplies, so nothing in this criterion rests on two tools spelling one directory alike. The differing separators are put there on purpose and folding them is what the criterion is about.\n\nPLANT: drop the case fold from `planMcpServers`'s dedup key and the second assertion must go red on the `caseInsensitive` arm while the first stays green, which is what proves the two arms are separately asserted. SECOND PLANT: compare the keys by prefix rather than over the whole string and the third assertion must go red reporting one plan where two were wanted. This criterion pins the accepting case beside the folding one, so a deduplicator written too aggressively, folding `C:/w` and `C:/w-old` together, fails rather than passing by being too strict."
---
`planMcpServers` publishes one plan per distinct root, folding separators always and case only where `caseInsensitive` is set, and the test asserts both the folding case and the non-folding case rather than only one. The fold is `planMcpServers`'s own: `mcpTargets()` returns one target per answered row and folds nothing, so no assertion here rests on a deduplication performed in code this criterion never calls.