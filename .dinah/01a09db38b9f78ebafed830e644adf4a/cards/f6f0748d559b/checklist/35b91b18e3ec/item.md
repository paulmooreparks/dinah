---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:28Z
ordinal: 14
note: "Three assertions, and each one guards a different way this could be got wrong. The absolute-path assertion is the one the whole F1 dissolve rests on: Go documents `os.Executable` as returning \"an absolute path unless an error occurred\", and the test holds this build to that rather than asserting the field is merely present, which is the presence-not-correctness failure the workbench instructions name. The absence assertion needs a seam, so `read.go` gains `var executablePath = os.Executable` beside the existing `var statPath = os.Stat` at `internal/bench/bench.go:1149`, which is this repository's established shape for exactly this, and the test swaps it for a function returning an error and asserts the marshalled JSON carries no `executable` key at all rather than an empty string, which is what `json:\"executable,omitempty\"` gives. The unchanged-human-rendering assertion holds D-11: `render.go` is not edited, no new message key is minted in any of the CLI's catalogues, and the fixtures that key on source line numbers in `render.go` and `docs/quick-start.md` therefore do not move.\n\nWHERE THE PATH ASSERTION RUNS, corrected at Agent Design Review round 2, 2026-09-11. Round 2's note said the assertion \"runs against the path the built `dinah` under test reports rather than against the test binary's, which is what `runCLI` already gives every other CLI test in `cmd/dinah`\". That is false. `runCLI` at `cmd/dinah/main_test.go:198-223` calls `run(argv, in, out, errw)` in the test process and spawns nothing, and no test anywhere in `cmd/dinah` builds a binary, so `os.Executable` under `runCLI` reports the go test binary's own path. That path is absolute and exists, so the assertion passes and proves only that the call works in the process under test. This is the same defect class as round 1's README plant: the sentence implies a check the named harness does not perform.\n\nSo the criterion takes two assertions rather than one, and the second is the one the F1 dissolve needs. In `cmd/dinah`, via `runCLI`, assert the field is present, absolute and names an existing file, and say in the test's own comment that the process under test is the test binary and that this arm proves the field is populated rather than proving what a shipped `dinah` reports. Then a second check against a genuinely built binary: `editors/vscode/test/support/fixtures.ts:40-50` already exposes `buildBinary(repoRoot)`, which runs `go build -o <temp>/dinah ./cmd/dinah`, and `test/unit/layers.test.ts:86-93` carries a named allowlist, `MAY_START_A_PROCESS`, whose three entries each give their reason. A new unit file builds this commit's `dinah` into a temporary directory, runs `--json version` against it, asserts the reported `executable` equals the path it built, and is added to that allowlist with its reason. That is the assertion that proves a shipped binary reports where it is, and it is the only route in the tree that does.\n\nTHE BYTE-IDENTICAL CLAUSE, corrected at Agent Design Review round 4, 2026-09-11. Rounds 2 through 4 asked for the human rendering to be \"byte-identical to what it printed before\", and no assertion anywhere can perform that comparison, because the suite holds no copy of what it printed before. The only existing comparison of that output is `TestEveryHelpSpellingReachesTheSamePage` at `cmd/dinah/main_test.go:7082`, which compares one run of a build against another run of the same build, so it is green whatever the rendering says. `cmd/dinah/compact_test.go:961` compares the compact and canonical machine forms of `version` against each other and never touches the human one. The clause is now what the two stores can honestly carry: `render.go` is untouched in this card's diff, and a minted key would be refused by the CLI catalogue guards on its own. The call-site registries at `cmd/dinah/row_sweep_test.go:1643` and `cmd/dinah/address_sweep_test.go:102` anchor `renderVersion`'s table site rather than its three `s.r.T` lines, so they are not the guard this clause wanted either.\n\nCANONICALISATION, driven at Agent Design Review round 4 rather than read. `realpathSync.native()` is load-bearing and `fs.realpathSync` is not a substitute for it: against an existing file reached through an 8.3 short name, `realpathSync` returns the short spelling unchanged and `realpathSync.native` returns the long one, measured on Windows 11 under node v24.4.1. The end-to-end case was run with this card's own change applied: a `dinah` built by `go build -o` into a short-named directory reports that short spelling from `os.Executable`, so when the spawn uses the same spelling `path.resolve()` on both sides happens to agree, and when the spawn uses the long spelling `path.resolve()` disagrees while `realpathSync.native()` on both sides agrees. So the repair is necessary in a reachable shape and it works. An implementer must not simplify it to `realpathSync` or to `resolve`.\n\nPLANT: drop the `omitempty` and make the seam fail, and the absence assertion must go red reporting a present empty string. SECOND PLANT: return a relative path from the seam and the absolute-path assertion must go red. THIRD PLANT: in the built-binary test, compare against a path other than the one `buildBinary` returned and watch it go red, which proves that test is reading the built binary's own answer rather than any absolute path at all; name a path that exists, because `realpathSync.native()` throws `ENOENT` on one that does not and a throw is a different red from the assertion this plant is arming."
---
`verb.VersionReport` carries an `executable` field, populated from `os.Executable` through a package-level seam and omitted from the JSON when that call returns an error. Two arms assert it, and each says in its own comment which process it observes. In `cmd/dinah` under `runCLI`, which runs in the test process, the field is present, absolute and names an existing file, and is absent when the seam is made to fail. In `editors/vscode/test/unit/versionExecutable-live.test.ts` a binary built by `buildBinary` is spawned and its reported `executable` is compared against the path `buildBinary` returned, with `realpathSync.native()` applied to both sides so the comparison does not rest on two tools spelling one path alike. The human rendering of `dinah version` is unchanged, and the criterion asks for what can actually be checked rather than for a comparison nothing performs: this card's own diff adds and removes no line in `render.go`, read by whoever verifies the card alongside AC-9's diff read, and no key is added to any CLI message catalogue, which the catalogue parity and coverage guards refuse on their own.