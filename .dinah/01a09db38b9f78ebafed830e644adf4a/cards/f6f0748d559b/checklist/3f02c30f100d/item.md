---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:28Z
ordinal: 12
note: "Both halves are asserted against one fixture so a decision that always returns nothing fails the true arm, and one that ignores the setting fails the false arm; asserting only the false arm would pass against a provider that never publishes anything, which is the refusal-that-any-value-satisfies shape. The change half is asserted by counting how many times the comparison answers yes across two resolutions over an unchanged fixture (expected 0) and across one that gained a root (expected 1), because a provider that fired on every checkpoint would prompt the reader to refresh their tools every poll interval and would still pass a test that only checked it fires at all.\n\nWHERE THE DECISION LIVES, rewritten at Agent Design Review round 2, 2026-09-11, for the reason AC-15's note gives in full. Round 2 put both the setting read and the change comparison inside `extension.ts`, which `test/unit/layers.test.ts:161-191` puts out of reach of every unit test, and the spec rules the integration suite out, so neither half could have been asserted anywhere. The setting's value and the two plan sets are passed into pure code in `mcpServers.ts`, and `extension.ts` keeps only the `settingOf` read, the `EventEmitter` and the call.\n\nWHAT THIS ASSERTION TOUCHES: arrays returned by `publishedMcpServers`, compared against emptiness and non-emptiness for one fixture under both settings of the reader's switch, and a count of how many times `mcpPlansDiffer` answered yes, compared against 0 and against 1. The `BinaryState` on every arm is constructed by the test, so nothing here reaches a binary or a decode.\n\nPLANT: make the comparison answer yes unconditionally and the unchanged-fixture count must go red reading 2 where 0 was wanted. SECOND PLANT: ignore the setting argument in the pure function and the false arm must go red returning a non-empty array."
---
The published set is decided by the same pure function AC-15 names, which returns an empty array when `dinah.registerMcpServer` reads false and a non-empty one against the same fixture when it reads true, and a pure comparison decides the change event, answering that it fires when the plan set differs by value and that it does not when a checkpoint resolves the same set.