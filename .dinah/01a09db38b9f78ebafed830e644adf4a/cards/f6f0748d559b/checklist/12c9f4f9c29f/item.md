---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:28Z
ordinal: 13
note: "Repaired at Agent Design Review, 2026-09-11, because the plant this item carried could not go red. The guard at `manifest.test.ts:875` reads three sources and only three: the manifest description, both description forms of every contributed setting, and every welcome block for this view. It collects them at `manifest.test.ts:887-908`, asserts the corpus is 11 strings at `manifest.test.ts:925`, and never opens `README.md`. Writing \"the extension has its own MCP server\" into the README would have left it green, so the criterion credited an existing guard with work that guard does not do. The repair is a new guard beside it, reusing the `claimsToCarryDinah` helper at `manifest.test.ts:512` so one definition of the refusal serves the manifest and the README rather than two that can drift. WHAT THIS ASSERTION TOUCHES: `claimsToCarryDinah`'s return over the text of `README.md`, compared against the empty array, and the key names in `contributes.configuration.properties` compared against occurrences of those names in the README text. PLANT: write \"the extension has its own MCP server\" into `README.md` and the new README guard must go red naming the line; read the run's test count so a plant that failed to build is not mistaken for a clean run. SECOND PLANT: delete the sentence naming `dinah.registerMcpServer` and the settings-coverage assertion must go red naming that key."
---
`editors/vscode/README.md` tells the reader that the extension publishes an MCP server per discovered workbench, that VS Code asks them to trust a server before it starts it, and that `dinah.registerMcpServer` turns it off; a new README guard runs `claimsToCarryDinah` over the README text, and a new assertion requires the README to name every key in `contributes.configuration.properties`. Each half asserts what it read, because both are universal claims and a universal claim over nothing is true: the guard fails when the README text it read is empty, and the coverage assertion fails when `contributes.configuration.properties` yielded no key names to look for.