---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:27Z
ordinal: 10
note: "VERIFIED 2026-09-11 on the branch, after the guard was replaced rather than widened a third time.\n\nTHE RULING, taken at Implement on Agent Code Review's instruction to choose. The text sweep is gone and the guard now reads the construct. `editors/vscode/test/unit/manifest.test.ts` carries `sweepMcpCallSites`, which parses `src/extension.ts` with the TypeScript compiler API, collects the MCP API call sites as CallExpression and NewExpression nodes matched on the callee's own name, collects every AsExpression, TypeAssertionExpression and NonNullExpression in the file, and reports one when its span nests with a call site's in either direction. Comment directives are collected as comment ranges and reported when a line-level one sits on the line above a call or on any line it spans, or when a file-level one sits anywhere. The reason for reading rather than matching is that a cast is a node, and three spellings of a node walked past the two successive patterns. Modeled on `editors/vscode/test/unit/l10n-keys.test.ts`'s `sweepKeys`, which landed on trunk in dinah-406 and walks src/ the same way.\n\nThe one assertion deliberately not reported is a const assertion. `as const` narrows a literal's inferred type and cannot make a call compile against declarations that do not carry the API, so reporting it would fail a developer who froze an argument. That exemption is pinned in the fixture and armed below, so it cannot quietly widen.\n\nTHE FLOOR is now an identity rather than a presence: `assert.deepEqual([...new Set(sweep.sites)].sort(), [...MCP_CALL_NAMES].sort(), ...)` requires both API calls to have been found by name before the absence claim is made.\n\nTHE DETECTING DIRECTION is pinned by `editors/vscode/test/fixtures/mcp-call-site-casts.ts.txt` and the test \"the call-site sweep sees every spelling of a silenced type check, and only those\". Twelve call sites: ten carry a silenced type check, and two are clean (one with no assertion, one with a const assertion) so a sweep reporting every node it meets fails rather than passes.\n\nARMED, every plant performed against the real `editors/vscode/src/extension.ts`, run through `npm run test:unit`, and restored from a byte-identical copy; the file's sha256 read back as 788b6fbe2df98ef9155d0a539b25acd053e1cda9e9a90d7e57ed8de63e7166fa after each of the eleven. Each red is test 265, \"the registration call is in the shipped source and reaches the API uncast\", 573 of 574 passing, and each named the spelling it caught:\n\nFour on the record from the first reviewer. 1, cast to a named type on the provider argument: `740: as vscode.McpServerDefinitionProvider`. 2, cast on the `vscode.lm` object itself: `740: as typeof vscode.lm`. 3, the construction folded onto one line with a trailing cast: `728: as vscode.McpStdioServerDefinition`. 4, cast to `any` on an argument on the call's own line: `740: as any`.\n\nTwo on the record from the second reviewer, both of which walked past the widened pattern. 5, TypeScript's angle-bracket assertion on the registration call's own line: `740: <vscode.McpServerDefinitionProvider>`. 6, `as any` on the closing line of the argument literal: `740: as any`.\n\nFive nobody had tried. 7, a double assertion through `unknown` on a continuation line: `731: as string[]` and `731: as unknown`. 8, a non-null assertion on the receiver, `vscode.lm!`: `740: vscode.lm!`. 9, a line directive on the line above the multi-line call: `740: @ts-ignore`. 10, a cast wrapping the whole registration call rather than sitting inside it, which is the other containment direction: `740: as vscode.Disposable`. 11, a file-level directive at the top of extension.ts: `10: @ts-nocheck`.\n\n`@ts-expect-error` cannot be planted in real source, because tsc reports an unused directive as an error of its own and `test:unit` type-checks before it runs, so the suite would not run at all and a plant that does not run proves nothing. The fixture carries that spelling instead, and plant F1 below is what arms it.\n\nTWO PLANTS ON THE GUARD'S OWN FIXTURE, same discipline. F1, the angle-bracket spelling removed from the fixture: red on test 266 naming `<Spelling5>` as unseen. F2, the const-assertion exemption disabled: red on test 266 naming `as const` as reported when the fixture does not plant it, which is the accepting case refusing to let the sweep pass by reporting everything.\n\nTHE COMMENT THAT WAS FALSE is gone with the sweep it justified. It said both real call sites are single-line; `new vscode.McpStdioServerDefinition(` and `vscode.lm.registerMcpServerDefinitionProvider(` each open on the line naming the API and close six lines later. The replacement states that they are multi-line and says so without citing line numbers, which would drift.\n\nThe version half is unchanged from the round that verified it: `assert.equal(raw.engines.vscode, raw.devDependencies[\"@types/vscode\"])` in the same file."
---
`engines.vscode` and the `@types/vscode` devDependency both read `^1.101.0`, asserted against each other rather than against two literals. `npm run check-types` passes against the installed types, and it is that script rather than `npm run compile` that type-checks, because `compile` is `npm run generate && node esbuild.mjs` and esbuild strips types without checking them while `check-types` is `tsc --noEmit` over `src/**/*.ts`. A sweep over `editors/vscode/src/extension.ts` asserts four things and fails when any one of them is absent, so it cannot pass over a file that never calls the API: that the file contains a `registerMcpServerDefinitionProvider` call and a `new vscode.McpStdioServerDefinition(` construction; that the provider id reaching the first is the identifier `MCP_PROVIDER_ID` rather than a string literal; that the setting the published set is decided from is read through the identifier `SETTING_REGISTER_MCP` rather than a string literal; and that no type assertion other than a const assertion, and no checker-silencing comment directive, nests with either call site's own span in either direction, read off the file's syntax tree through the TypeScript compiler API rather than off its text, because a cast is a node whose spelling does not change which node it is, and because a clean type-check is what a cast produces rather than what it rules out.