---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:27Z
ordinal: 8
note: "VERIFIED 2026-09-11 on the branch, after the two floors that counted without identifying were repaired.\n\nFour floors, each deriving its expected value from `SUPPORTED_TAGS` and the shipped `flags.json` rather than from the roster it counts, and each now asserting the roster's MEMBERSHIP beside its size. A count alone is held steady by a duplicate, so `TRANSLATED_TAGS = [\"de\", \"de\"]` reached the right count while Hindi's check silently stopped running; only `de` and `hi` can satisfy that loop's body, so a duplicate is the one substitution a careless edit or a bad merge produces.\n\nFloor 1, files read by the manifest parity sweep. `editors/vscode/test/unit/l10n.test.ts` asserts `filesRead === SUPPORTED_TAGS.length` inside \"every manifest sibling carries exactly the base catalogue's keys\". Unchanged this round; armed in the previous one by a `continue` on `de` inside the loop, red reading `the parity sweep opened 7 package.nls*.json files and there are 8 tags, so it skipped one`. That roster is `SUPPORTED_TAGS` itself, which AC-11's test pins against the directory listings in both directions, so it cannot carry a duplicate.\n\nFloor 2, the translated roster, in \"the manifest's two translated languages carry no skeleton and its five skeletons carry every key\". Now `assert.equal(translatedChecked, shippedTranslatedTags.length, ...)` followed by `assert.deepEqual([...TRANSLATED_TAGS].sort(), [...shippedTranslatedTags].sort(), ...)`.\n\nFloor 3, the skeleton roster in the same test. Now compares against a new `shippedSkeletonTags`, the complement derived the same way, in both size and membership. The old expected value was an arithmetic expression over the translated set, which named nothing when it failed.\n\nFloor 4, the staleness sweep's own coverage, `editors/vscode/test/unit/l10n-staleness.test.ts`. Already carried the membership assertion; this round its two failure messages were corrected to name the set difference rather than the whole shipped set, and to stop reading \"1 languages\".\n\nARMED, eight plants, each applied to the real test file, run through `npm run test:unit`, restored from a byte-identical copy, and confirmed by sha256. `l10n.test.ts` read back as 65079ba0cb847cc906781b587e8305b40f019830117aa060fdc3dccd8071ae69 after each of its five, and `l10n-staleness.test.ts` as 5c254ac981f310c0263f1fef85c51ffe8548cda5d70545e3b9db4bc392fd422c after each of its two.\n\nThe reviewer's own defeat, planted in each file that carries a floor rather than in one of them:\n- R1, `TRANSLATED_TAGS = [\"de\", \"de\"]` in `l10n.test.ts`: RED, 573 of 574, `the translated sweep reads the right number of tags and not the right ones: it never reads hi`. Before this round it was green.\n- R2, `SKELETON_TAGS = [\"af\", \"af\", \"cs\", \"es\", \"fil\"]` in `l10n.test.ts`: RED, 573 of 574, on the skeleton membership comparison, the diff naming the duplicated `af` and the absent `id`. Before this round it was green.\n- R3, `TRANSLATED_TAGS = [\"de\", \"de\"]` in `l10n-staleness.test.ts`: RED, 573 of 574, on that file's own membership comparison.\n\nThe shrinking defeats, which the size halves still catch:\n- R4, `hi` dropped from `l10n.test.ts`'s roster: RED, `the translated sweep read 1 tags and 2 ship translated, so TRANSLATED_TAGS is short of hi`. The message now names `hi` alone, where the previous round's read `short of de, hi`.\n- R5, `id` dropped from the skeleton roster: RED, expected 5 actual 4.\n- R6, `hi` dropped from `l10n-staleness.test.ts`'s roster: RED at 571 of 572, the test count falling by two being the silent retirement itself, `the staleness sweep covers 1 of the 2 languages that ship translated, so it never reads hi`.\n\nEvery floor still derives from a source outside the roster it guards, so emptying one roster leaves the other floor's expected value unmoved and each red names the roster that actually moved."
---
All eight `package.nls*.json` files carry both new manifest keys, `flags.skeleton` names both keys under each of the five skeleton tags, `flags.source` carries a correct `fingerprint` of each key's English under `de` and `hi`, and each of those three sweeps asserts the number of files or tags it read.