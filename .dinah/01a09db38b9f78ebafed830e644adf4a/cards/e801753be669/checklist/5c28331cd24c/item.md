---
kind: acceptance_criterion
state: verified
column: 6c5b9d6f4414
ts: 2026-09-14T02:17:33Z
ordinal: 21
note: "Both halves run, as the criterion asks.\n\nThe run. A binary built from this branch, against a throwaway workbench under C:\\dinah-scratch\\dinah-436-impl\\ac21 with DINAH_HOME pointed inside it (outside the operator's profile). `dinah link ac21-1 waiting-on-the-tiler ac21-2` was accepted; the raw card.md then read \"links:\\n  - kind: waiting-on-the-tiler\\n    to: 6b93501e2106\", exactly the word given, and `dinah show` rendered it as \"waiting-on-the-tiler  ac21-2\". `dinah unlink ac21-1 waiting-on-the-tiler ac21-2` removed it and the frontmatter went back to carrying no links key at all. The journal carries one linked line and one unlinked line, each with kind and to.\n\nThe tree search. No enumeration of link kinds exists anywhere. The kind parameter on both verbs declares no Vocabulary (internal/verb/definition.go), and the only Vocabulary names in the whole tree are column, column-kind, item-kind, field, detail-field, key and topic, none of them about links. `grep -rn \"LinkKind|linkKind|KnownLink|linkKinds|LinkKinds\"` over the tree returns nothing. The only occurrences of \"duplicates\" or \"relates\" as link kinds outside tests are prose: one help sentence naming them as examples, and one MCP scenario statement. Neither is a set anything reads.\n\nThe shipped surface is the two verbs the operator approved, each writing or removing a single entry on the source card's own definition, with the kind taken as a free word.\n\nFailing cases checked and not present: no kind was rejected for absence from a set, no kind was rewritten to a canonical spelling (Blocks-Drywall and \"came out of\" both survive verbatim in the unit test), and the search found no enumeration. The no-canonicalising half is separately armed: planting a strings.ToLower plus space-to-underscore fold went red naming the rewritten values, and planting a four-member permitted set went red on the first invented word."
---
The shipped command surface is the one the operator approved on 2026-09-09: two verbs, one that records a link and one that removes it, each writing or removing a single entry on the source card's own definition, with the kind taken as a free word. No set of permitted kinds exists anywhere in the tool. Check by running the built binary against a scratch workbench: record a link whose kind is a word this project has never used, confirm it is accepted and reads back exactly as given, then remove it and confirm it is gone. Then search the whole tree for any list, constant or validation that enumerates link kinds and confirm none exists. Failing case: a kind is rejected for not appearing in some set, a kind is silently rewritten to a canonical spelling, or the search finds an enumeration of permitted kinds anywhere.