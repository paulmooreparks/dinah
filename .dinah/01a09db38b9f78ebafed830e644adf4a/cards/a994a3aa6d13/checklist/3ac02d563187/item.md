---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:16:49Z
ordinal: 17
note: "PASS. All four clauses now exist and each was armed before it was trusted.\n\nClause (a), two spellings admitted: `TestTwoSpellingsOfOneDirectoryAreAdmitted` in internal/bench/path_under_root_test.go. The second spelling comes from chdir plus os.Getwd, the mechanism the criterion prescribes and the one `resolvedDir` at cmd/dinah/main_test.go:148 already uses; a local `resolvedDir` mirrors it, since the two live in different packages. Because that mechanism yields one spelling on a platform with no symlink or 8.3 quirk, the subtest \"the root spelled through a child and back out\" carries the clause everywhere: it names the root as `<root>/child/..`, uncleaned, which the filesystem resolves and no string comparison does. ARMED: replacing both `os.SameFile` comparisons in PathUnderRoot with string equality turned that subtest red (\"refused two spellings of one directory\") while the rest stayed green.\n\nClause (b), just outside refused: `TestAPathJustOutsideTheRootIsRefused`, two subtests, a sibling named `<root>-sibling` and the root's own parent. ARMED: adding `strings.HasPrefix(candidate, root)` as an admit rule turned the sibling subtest red, and also turned the existing ancestor subtest of the stat-failure test red, which is the prefix bug's full blast radius.\n\nClause (c), root removed since the server started: `TestARootRemovedSinceTheServerStartedIsRefused`. It builds root/deeper/child, asserts containment holds, removes the root tree, and asserts the second call refuses with an error satisfying `errors.Is(err, fs.ErrNotExist)`. It drives the real os.Stat, no seam, which is what separates it from clause (d). ARMED: making the candidate's stat return `(false, nil)` on ErrNotExist, which is samePath's posture and the failure the spec warns against, turned it red (\"should refuse, got contained=false\").\n\nClause (d) was already asserting and stays. The weakening the retest named is closed at the layer where the refusal exists: `TestARootRemovedSinceTheServerStartedRefusesOutsideRoot` in internal/mcp/mcp_test.go drives a removed root through Serve and asserts `refusal == contract.OutsideRoot` and `context[\"root\"] == root`. Refusal composition stays at internal/mcp/mcp.go:317 rather than moving into internal/bench, so the package comment's separation holds. ARMED: changing that branch to `contract.Refuse(contract.NoWorkbench, abs)` turned it red on both the refusal name and the missing context, which also proves the test reaches the error branch rather than the not-contained branch beside it.\n\nRan: `go test ./internal/bench/ ./internal/mcp/` green, full `go test ./...` green, and the pull request's six checks green on macOS, Ubuntu and Windows (PR #119, run 32969282783). Commit 19c5a61."
---
A Go test drives the containment test four ways: two different spellings of one directory are admitted, so containment is decided by the filesystem rather than by string comparison; a path just outside the root is refused, so the bound acts; a path under a root that has been removed since the server started is refused `dinah.outside-root`, which is the not-exist branch; and a stat that fails with `fs.ErrPermission`, driven through the package's stat seam on the target and again on an ancestor between the target and the root, is refused `dinah.outside-root`, which is the branch that decides safety.