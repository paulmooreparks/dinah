---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:16:50Z
ordinal: 26
note: "PASS. D-13 gives the exception clause the referent it lacked, and with that referent the criterion is falsifiable and holds.\n\nThe four structural points were already confirmed on the earlier reading and are unchanged by this commit: the optional workbench property on every tool but workbenches, the workbenches discovery tool, the --root bound with both refusals firing live, and both forms of the handshake.\n\nThe copy divergences the earlier reading found are now each answered. D-13 items 1 and 2 rule that the no-default handshake and the six rewritten refusal and argument sentences stand as shipped, so those are ratified rather than outstanding. D-13 item 4 sends the first refusal row back to the sketch, and commit 671f109 does that: check.mcp.1 is now the sketch's own sentence, \"--root names a directory that exists\". D-13 item 3 keeps the shipped workbench-argument sentence and requires the documentation to agree, which the same commit does across every surface that describes the argument.\n\nRan, on 671f109. `dinah help mcp --lang <tag>` for all eight tags under internal/msg/locales (af, cs, de, en, es, fil, hi, id), which is AC-24's method: exit 0 in every locale, both refusal names whole and each in its own column, and no line over 80 display columns, measured in display columns rather than bytes so the Hindi rendering is judged correctly. The English refusal table's widest line is 71 columns, which is the figure the sketch's section 5 claims.\n\nThe ceiling was measured rather than assumed, by rendering the page with a synthetic check sentence at each width from 49 to 54: the refusal name stays on the row through 52 display columns and moves to a line of its own at 53. That confirms D-12's corrected figure and falsifies the sketch's \"51\", which this commit corrects in place per D-12's own instruction. The shipped sentence is 36 columns, well inside the ceiling in every locale.\n\nAlso ran: go build ./..., go vet ./..., gofmt -l . (clean), go test ./... for the whole repository, all green, with COLUMNS, DINAH_EDITOR, EDITOR and VISUAL cleared from the environment handed to the suite. PR #119's six checks pass on this commit across macOS, Ubuntu and Windows.\n\nCorrection, added at 4aa2535. The paragraph above says 671f109 corrects the sketch \"in place per D-12's own instruction\", and that overstated what the commit did. D-12 names sections 5 and 7 of the sketch, and 671f109 redrew section 5 only, so the document said 52 in one section and 51 in the other until 4aa2535 redrew section 7 as well. The criterion itself is unaffected, since the sketch's ratified schema and layout are what it asserts against, and both readings of the shipped page above still hold. Re-ran the eight-locale render on 4aa2535: exit 0 in every locale, both refusal names whole and each in its own column, and no page over 80 display columns, measured through the repository's own internal/textwidth.Columns."
---
The shipped surface matches the interface shape the operator accepted: an optional `workbench` property on every tool taking a path, a `workbenches` discovery tool, a `--root` bound with its two refusals, and the handshake in both forms, as drawn in the UX sketch at commit dd67d51, except in the three respects his ruling changed after acceptance.