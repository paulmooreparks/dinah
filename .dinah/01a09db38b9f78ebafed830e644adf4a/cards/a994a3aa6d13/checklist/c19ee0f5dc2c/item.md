---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:16:50Z
ordinal: 25
note: "PASS on both halves.\n\nThe first half is fixed at cmd/dinah/commands.go, in runMCP's switch: the line is now built as `contract.NoWorkbench + \" \" + noWorkbench.Detail`, the way the unknown-root and outside-root lines beside it are built and the way composeRefusal joins a name to its sentence. `isRefusalNamed` became `refusalNamed`, returning the refusal rather than a bool, because the site needs the detail as its own field. I checked every other startup path first: the only two places in cmd/dinah that render an error into a stream are main.go:275 and render.go:71, both of which lead with the `unreachable` outcome token and are already whitespace-separated, so this was the one site and the fix stayed local rather than reworking refusal formatting.\n\nNew test: `TestTheStartupRefusalsMCPRaisesLeadWithTheirName` in cmd/dinah/mcp_startup_test.go holds all three refusals dinah mcp can raise before it serves, in one table, since the criterion is about the three agreeing rather than about any one of them. For each it asserts exit code 2 and that `strings.Fields(stderr)[0]` equals the refusal name, and that a detail follows. The three cases are a root no directory sits at (dinah.unknown-root), a named workbench holding no anchor (dinah.no-workbench), and a named workbench outside the root (dinah.outside-root). Going through runCLI also puts the existing checkRefusalShape guard on all three lines.\n\nARMED: restoring `s.errLine(noWorkbench.Error())` turned exactly the middle subtest red, reporting `wanted \"dinah.no-workbench\", got \"dinah.no-workbench:\"`, while the other two stayed green.\n\nRan: `go test ./cmd/dinah/` green, full `go test ./...` green, PR #119's six checks green on macOS, Ubuntu and Windows. Commit 19c5a61."
---
`dinah --workbench <a directory holding no workbench.md> mcp --root <dir>` writes `dinah.no-workbench` as the first whitespace-delimited token on stderr and exits 2, while `dinah mcp --root <dir>` run from a directory where the ancestor walk finds no workbench serves.