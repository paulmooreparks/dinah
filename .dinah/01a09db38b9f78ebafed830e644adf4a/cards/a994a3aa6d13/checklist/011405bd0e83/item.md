---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:16:48Z
ordinal: 5
note: "PASS against the rewritten criterion. The criterion was the defect, not the code, so it now tests what it was for.\n\nWhat the rewrite does: it asserts that `workbenches` is registered on the surface, and it delegates the count to `TestToolSurfaceIsTheProjection` in internal/mcp/mcp_test.go instead of freezing a literal. The old literal was 27; the surface was already 29 at this card's own merge commit and is 32 today, so a criterion carrying a number goes stale the next time any card adds a command. The criterion says why it is written that way and tells the next reader not to put a literal back.\n\nWhat changed in the suite: `TestToolSurfaceIsTheProjection` gains `workbenches` in its wanted-names list, so the registration half of the criterion is now asserted by the same test that holds the count. It was not asserted before, which is how a criterion whose substance held could still be unverifiable. The test's doc comment also stops naming twenty-six tools, and says instead that the count is the suite's own record and that nothing outside the package should carry a copy of it.\n\nARMED: commenting out the `workbenches` row in internal/mcp/tools.go turned the test red twice, on `wanted thirty-two tools, got 31` and on `the surface is missing the tool workbenches`, which is the assertion the criterion now points at. Restored and green.\n\nRan: `go test ./internal/mcp/` green, full `go test ./...` green, PR #119's six checks green. Commit 19c5a61."
---
A `tools/list` request sent to `dinah mcp` started inside a workbench lists `workbenches` among its tools, and returns exactly as many tools as `TestToolSurfaceIsTheProjection` in `internal/mcp/mcp_test.go` asserts. The criterion names no literal count on purpose. The surface grows whenever a card adds a command, so a frozen number is stale the next time one lands, and this criterion's own frozen number was already wrong on the day it was written. The substance being tested is that `workbenches` is registered and that the head serves the surface the suite records, so the suite holds the count and the criterion points at it. Do not put a literal back.