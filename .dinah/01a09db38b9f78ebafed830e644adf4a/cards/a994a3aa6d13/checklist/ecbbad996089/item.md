---
kind: decision
state: resolved
ts: 2026-09-14T02:16:51Z
ordinal: 37
note: "This declines a major finding, and the finding rests on a premise I checked and found false. Agent Design Review held that the two names ship sentences that are false on this surface, quoting `refusal.dinah.no-workbench` reading \"{detail} was named by --workbench, and it carries no workbench.md\" and `refusal.dinah.no-workbench-found.next` telling the caller to \"run `dinah init` here\". Neither sentence reaches an MCP caller. `verb.Response` carries `outcome`, `verb`, `refusal`, `detail`, `context`, and the affordances, and it carries no message field; `composeRefusal` in cmd/dinah/render.go is the only site that renders a refusal into English, and it belongs to the terminal head. I confirmed it by running the trunk binary: a `tools/call` of `show` for a card that does not exist answered `{\"outcome\":\"refused\",\"verb\":\"show\",\"refusal\":\"unknown-card\",\"detail\":\"pf-99\",\"affordances\":[...]}` and nothing else.\n\nSo minting a third name would give one condition two names for the sake of copy that nobody on this surface reads, and conditioning a next-step fragment would condition a clause that never renders here. The reviewer's three options each pay a published-surface cost to fix a reader who does not exist.\n\nTwo things the finding is right about survive the decline, and both are in the spec. The names now answer on a second surface, so the translator's note on each of the two entries gains that second raise site, which changes no rendered text and keeps the entry honest for whoever translates it. And startup case 2, where the server reports a discovery refusal on stderr and then serves anyway, is a place where a sentence really is rendered and really would be wrong, since telling a reader to run `dinah init` while the process carries on serving is advice about a state that no longer applies. That report gets its own key, `mcp.no-default`, rather than splicing a refusal's next-step clause.\n\nRepository claims verified for this decision: 3 (the fields of `verb.Response`, that `composeRefusal` in cmd/dinah is the only renderer of refusal English, and the shape of a real MCP refusal answered by a binary built from f065620)."
---
Per-call steps 1 and 4 keep dinah.no-workbench-found and dinah.no-workbench, and their catalog sentences are left alone.