---
kind: decision
state: resolved
ts: 2026-09-14T02:16:50Z
ordinal: 30
note: "An agent cannot ask for a workbench it cannot name, so something has to answer the question the workbench argument raises. The command that already answers it is `workbenches`, and giving the head a second, differently shaped answer would put two vocabularies on one question.\n\nThe exclusion rule is amended rather than contradicted. The comment above `tools` says a command that exists only because a shell and a filesystem exist gets no tool, and it names path, edit, init, extract, config, and mcp itself, with guide served as a resource. That is six commands rather than the seven the card's framing says, and `workbenches` is not among them: it is absent from the table without the comment mentioning it, which is the drift this card fixes. The amended comment states the rule, states that `workbenches` sat inside it while the head served one workbench, and states why it sits outside it now.\n\nWhat the tool returns: the same `bench.Candidate` rows the terminal listing prints, carrying title, slug, and path, ordered by path so two calls in one session answer alike.\n\nWhat it must not leak: any workbench outside the root, and anything that would require opening a workbench. `describe` in internal/bench/bench.go reads the anchor without opening the workbench, which is what keeps cards, the operator's name, the state list, and the instruction chain out of the answer.\n\nRepository claims verified for this decision: 3 (the exclusion comment names six commands and not `workbenches`, `describe` reads the anchor rather than opening the workbench, and a throwaway build serving the tool returned the same three fields the terminal prints)."
---
A workbenches tool joins the MCP surface, and the shell-only exclusion rule in internal/mcp/tools.go is amended to say why.