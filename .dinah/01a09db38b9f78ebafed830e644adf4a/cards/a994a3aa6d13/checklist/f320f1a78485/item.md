---
kind: decision
state: resolved
ts: 2026-09-14T02:16:51Z
ordinal: 33
note: "`runWorkbenches` in cmd/dinah/commands.go already rules this for the terminal, and its comment says why: it opens nothing and never refuses over what the search found, because a question about what is reachable is answered by zero rows as truthfully as by several. Answering the same question two ways on two heads would be the drift this codebase spends its tests preventing.\n\nA root that holds no workbench yet is a plausible steady state on a machine where the workbenches are about to be created, so the server serves, the listing is empty, and every call naming a workbench is refused by the per-call order.\n\nThe half of this note that ruled on a default workbench outside the root has moved to D-9, which splits that case on where the default came from. What stays here is the case this decision is about, and the one startup case that still refuses. A root naming no directory at all is a typo, and the other half of the same rule says that a listing softening a typo into an empty result hides it. That sentence is in `bench.Reachable`'s doc comment in internal/bench/bench.go rather than in `runWorkbenches`, which an earlier draft of this note misattributed; both claims are true of the codebase and neither the decision nor D-4 depends on which comment carries which half.\n\nRepository claims verified for this decision: 3 (the comment and behaviour of `runWorkbenches`, the sentence about a softened typo in `bench.Reachable`'s doc comment, and that `bench.Reachable` returns rows rather than a refusal when the walk finds nothing, confirmed by running `dinah workbenches` in a scratch directory holding none, which printed `no workbench is reachable from here` and exited 0)."
---
A root holding no workbench starts a server that answers with an empty listing, rather than refusing at startup.