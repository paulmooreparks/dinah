---
kind: acceptance_criterion
state: verified
column: aa6cd1c6ae5f
ts: 2026-09-14T02:16:50Z
ordinal: 24
note: "PASS. The eight tags were enumerated from internal/msg/locales/ rather than assumed: af, cs, de, en, es, fil, hi, id. `dinah help mcp --lang <tag>` exits 0 for each, and each prints an arguments table and a refusal table. Width was measured in display columns, skipping combining marks and counting East-Asian wide cells as two, because a byte count would give a wrong answer on the Hindi rendering: maximum 80 columns in every locale, nothing over. Both refusal names appear exactly once, whole, at one consistent column offset per locale.\n\nAs the criterion's own note says, seven of the eight carry English text today and only hi is translated, so this is evidence about layout and not about translations nobody has written. The row-1 continuation wart appears in the six English-text locales; German and Hindi have shorter middle cells and fit on one line.\n\nCORRECTION appended at Agent Code Review cycle 4, by the reviewer, leaving the paragraph above intact so the record shows what was observed and when. Two statements in it describe a page no build on this branch renders any more, and D-12 named this note as one of the four places to redraw.\n\nFirst, the row-1 continuation wart is gone in every locale. Commit 671f109 shortened the English `check.mcp.1` to \"--root names a directory that exists\", which is 36 display columns, and every locale's rendering of that row now keeps its refusal name beside the sentence. I re-rendered to confirm the mechanism: the refusal name stays on the row through a sentence of 52 display columns and moves to a line of its own at 53.\n\nSecond, German is translated as well as Hindi, so \"only hi is translated\" undercounts by one. Both `de.json` and `hi.json` carry translated `check.mcp.1`, `check.mcp.2` and `schema.workbench.description` with a current `source` fingerprint; the other five catalogs carry the English text under `\"skeleton\": true`.\n\nThe criterion's own text is unaffected and stays verified. What changed is the evidence beneath it, which had gone stale against the branch it was written on."
---
`dinah help mcp --lang <tag>` run for each of the eight tags under `internal/msg/locales/` prints an arguments table and a refusal table in which every refusal name stands in its own column, and no line exceeds 80 columns.